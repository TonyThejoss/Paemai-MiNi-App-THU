# เช็คลิสต์ Deploy v2.5 — ตลาดแพไม้ (วันอังคาร + วันพฤหัสบดี)

**วันที่:** 28 กรกฎาคม 2026
**ขอบเขต:** บั๊ก A/B/C/D + ฟีเจอร์ย้ายล็อคจร + ปิดช่องโหว่รหัสผ่าน + ด่านตรวจใหม่ 3 ตัว
**อ่านประกอบ:** `Paemai-Market-Unified-Operating-Manual-v2.5.md` หัวข้อ 9 (ขั้นตอน deploy) และหัวข้อ 15 (สิ่งที่เปลี่ยน)

---

## ⚠️ 0. ต้องทำก่อนอื่นใด — เรื่องความปลอดภัย

- [ ] **เปลี่ยนรหัสผ่านของทั้ง 4 บัญชี** (`tony2568`, `fon12345`, `too56789`, `aew98765`)
      ผ่านเมนู "เปลี่ยนรหัสผ่าน" ในแอพ **ทั้งสองตลาด**
      > รหัสเดิมเคยอยู่เป็น plain text ใน `pamai_apps_script.js` ซึ่งถูกอัปขึ้น GitHub public repo
      > **การลบโค้ดออกใน v2.5 ไม่ได้ลบมันออกจาก git history** — ใครที่เคย clone repo ไว้ยังมีรหัสเดิมอยู่
- [ ] ตรวจว่า Script Properties > `API_TOKEN` ถูกตั้งค่าไว้ทั้งสองโปรเจกต์ GAS (ถ้าไม่ตั้ง = โหมดผ่อนผัน ใครก็เรียก API ได้)

---

## 1. ด่านตรวจก่อน deploy (ต้องผ่านครบทั้ง 6)

```bash
cd ระบบรวมสองตลาด/

python3 tools/patch_market_maps.py               # idempotent — รันซ้ำได้ปลอดภัย
python3 tools/check_js.py                        # ✅ JS + CSS + โครงสร้าง HTML
python3 build.py                                 # ✅ build เสร็จ + ✅ verify ผ่าน
node tools/test_responsive.js dist/tuesday       # 76/76
node tools/test_responsive.js dist/thursday      # 76/76
node tools/test_map_reach.js  dist/tuesday dist/thursday    # ทุกล็อคเลื่อนไปหาได้
node tools/test_map_vendor_sync.js dist/tuesday dist/thursday # ทุกโซนรับข้อมูลผู้ค้า
node tools/test_offline_map.js dist/tuesday dist/thursday   # ~2 นาที (จำลอง timeout จริง)
```

- [ ] `check_js.py` ✅ 20 ไฟล์
- [ ] `build.py` ✅ build + ✅ verify
- [ ] `test_responsive.js` 76/76 ทั้งสองตลาด
- [ ] `test_map_reach.js` ผ่านทุกโซน ทุกขนาดจอ (360/390/430px)
- [ ] `test_offline_map.js` ผ่านทั้ง 3 สถานการณ์ ทั้งสองตลาด

---

## 2. Backend — Google Apps Script (⚠️ รอบนี้ต้อง deploy ทั้งสองตลาด)

`core/pamai_apps_script.js` **มีการแก้ไข** — เพิ่ม action ใหม่ 2 ตัวและแก้ `initSheets()`
ถ้าไม่ deploy ปุ่มย้ายล็อคจะขึ้น `Unknown action` และย้ายไม่ได้เลย

สำหรับแต่ละตลาด:

- [ ] เปิดโปรเจกต์ GAS ให้ถูกตลาด — **ยืนยันด้วย `SHEET_ID` ในโค้ด** (วันอังคารลงท้าย `…WJu4` · วันพฤหัสฯ ลงท้าย `…pMN8`)
- [ ] วางเนื้อหาทั้งหมดจาก `dist/<tuesday|thursday>/pamai_<thu_>apps_script.js` ทับ `Code.gs`
- [ ] บันทึก → **ทำให้ใช้งานได้ → เวอร์ชันใหม่**
- [ ] **ตรวจตามหัวข้อ 9.3 ของคู่มือ:** Web App URL ของ deployment ที่เพิ่งอัปเดต **ต้องตรงกับ**
      ค่าใน `localStorage` (`pm-tue-api-url` / `pm-thu-api-url`) ที่หน้าเว็บใช้จริง
      > โปรเจกต์เดียวมีได้หลาย deployment — กดเวอร์ชันใหม่ผิดตัวคือเข้าใจผิดว่า deploy สำเร็จ
- [ ] ยิงทดสอบ: `<WebAppURL>?action=getVendors&token=<token>` ต้องได้ `{"status":"ok",...}`

> **ไม่ต้องรัน `?action=initSheets` รอบนี้** — ไม่มีชีตใหม่ และการรันซ้ำจะไม่สร้างผู้ใช้ทับของเดิม
> (ถ้าเผลอรันก็ไม่เสียหาย เพราะชีต `users` ไม่ว่างแล้ว)

---

## 3. Frontend — GitHub Pages (คนละ repo ต่อตลาด)

ไฟล์ที่เปลี่ยนรอบนี้: **`pamai(_thu)_market_map.html`** และ **`pamai(_thu)_apps_script.js`**
(ไฟล์อื่นไม่เปลี่ยน แต่อัปทั้งโฟลเดอร์ก็ได้ ปลอดภัยกว่า)

- [ ] repo วันอังคาร `Paemai-Market-Tuesday` ← อัปจาก `dist/tuesday/`
- [ ] repo วันพฤหัสฯ `Paemai-MiNi-App-THU` ← อัปจาก `dist/thursday/`
- [ ] ตรวจว่าไฟล์ฝั่งวันพฤหัสฯ **ทุกไฟล์ขึ้นต้น `pamai_thu_`**
- [ ] รอ GitHub Pages deploy 1–2 นาที แล้วกด hard refresh (Ctrl/Cmd+Shift+R)

---

## 4. ตรวจหน้างานจริงหลัง deploy (ห้ามข้าม — กฎเหล็กข้อ 11)

### 4.1 บั๊ก A — แถบสถานะอยู่ด้านบน

- [ ] เปิดผังตลาดบนเบราว์เซอร์ **กดดูให้ครบทุกโซน** ทั้งสองตลาด
- [ ] แถบ legend/ค้นหา/สถิติ ต้องอยู่ **ใต้แถบโซนทันที** ทุกโซน ไม่ลอยกลางจอ
- [ ] โดยเฉพาะ **โซนข้างโลตัส** และ **โซนข้าง 7-Eleven** ของฝั่งวันพฤหัสฯ (โซนที่ผังแคบ = โซนที่เคยพัง)

### 4.2 บั๊ก B — ผังไม่โกหกเมื่อโหลดข้อมูลไม่สำเร็จ

- [ ] เปิดผังปกติ → ต้อง **ไม่มี** แถบเตือน และเห็นผู้ค้าประจำครบตามจริง
- [ ] **ทดสอบโหมดออฟไลน์:** เปิด DevTools → Network → Offline → รีเฟรช
      → ต้องเห็นแถบเตือนสีส้ม/แดง และล็อคเป็นลายทางจาง ๆ **ไม่ใช่คำว่า "ว่าง"**
- [ ] กดปุ่ม **"ลองใหม่"** หลังปิด Offline → ข้อมูลกลับมาครบและแถบเตือนหายไป
- [ ] **ทดสอบบนมือถือจริง** อย่างน้อย 1 เครื่องต่อตลาด (เครื่องที่เคยเจอปัญหายิ่งดี)
      → ถ้าขึ้น "เครื่องนี้ยังไม่ได้เชื่อมต่อฐานข้อมูล" ให้กด **"ตั้งค่าการเชื่อมต่อ"** แล้ววาง Web App URL

### 4.3 บั๊ก C — ผังไม่ล้นขอบบนมือถือ

- [ ] เปิดบนมือถือจริง → **โซนโรงหน้า (SA)** ของวันอังคาร: ปัดนิ้วซ้าย-ขวาต้องเห็นล็อคครบถึง SA33
- [ ] กรอบการ์ดต้องล้อมรอบล็อคทั้งหมด ไม่มีล็อคทะลุออกนอกเส้น
- [ ] ตรวจครบทุกโซนทั้งสองตลาด

### 4.4 ฟีเจอร์ย้ายล็อคจร (⚠️ ต้องทดสอบกับข้อมูลจริงและเปิดชีตดู)

- [ ] ขายล็อคจรหนึ่งรายการ → คลิกล็อคนั้นในผัง → ต้องเห็นปุ่ม **"🔄 ย้ายล็อคจร"** (สีเหลือง)
- [ ] คลิกล็อคว่างธรรมดา → **ต้องไม่มี** ปุ่มย้ายใด ๆ
- [ ] กดย้ายไปล็อคว่างอีกล็อค → ขึ้น "กำลังย้าย…" แล้วขึ้น toast เขียว
- [ ] **เปิด Google Sheet ชีต `daily_bookings` ดูด้วยตา:** แถวเดิม `cancelled = TRUE` และมีแถวใหม่ที่ล็อคปลายทาง
- [ ] **กด F5 รีเฟรช** → ล็อคจรต้องยังอยู่ที่ล็อคใหม่ (ไม่เด้งกลับ = ยืนยันว่าบันทึกจริง)
- [ ] เปิดจากอีกเครื่อง/อีกเบราว์เซอร์ → ต้องเห็นตรงกัน
- [ ] ถ้ารายการนั้นมาจากคิว: เปิดหน้า **คิวจองล็อคจร** → ช่อง `assigned_lock` ต้องเป็นล็อคใหม่แล้ว
- [ ] ทดสอบ **"ย้ายผู้ค้าประจำ"** 1 ล็อค → เปิดชีต `vendors` ดู: ล็อคใหม่มีข้อมูลครบ · ล็อคเดิม `status = terminated`
- [ ] ตรวจ **บันทึกกิจกรรม** ในแอพ → ต้องเห็นรายการ "ย้ายล็อคจร" / "ย้ายล็อคผู้ค้าประจำ"
- [ ] ทำครบ **ทั้งสองตลาด**

### 4.5 ไม่มี regression

- [ ] เข้าสู่ระบบแล้วกดทุกเมนูที่จอมือถือ (390px) — ทั้ง 13 เมนู ต้องเปิดเต็มจอ ไม่มีจอดำ
- [ ] แจ้งลา / ขาดล็อค / ปลดสถานะ ยังทำงานปกติ
- [ ] ยกเลิกการเช่า → ผังขึ้นเป็นล็อคว่างทันที
- [ ] รายงาน 01–08 ยังดูข้อมูลได้ครบ
- [ ] หน้าสถานะสาธารณะยังแสดงถูกต้องและ **ไม่หลุดเบอร์โทร/ยอดเงิน**

---

## 5. หลัง deploy

- [ ] เก็บ `dist/` ของรอบนี้ไว้เป็นทางถอย (zip ไว้ 1 ชุด)
- [ ] แจ้งทีมงานเรื่อง **ปุ่มย้ายล็อคจร** และ **แถบเตือนเมื่อโหลดข้อมูลไม่สำเร็จ** (ของใหม่ที่ต้องรู้จัก)
- [ ] แจ้งทีมงานว่าถ้าเปิดแอพบนมือถือเครื่องใหม่แล้วขึ้น "ยังไม่ได้เชื่อมต่อฐานข้อมูล"
      ให้กดปุ่มตั้งค่าในแถบเตือนแล้ววาง Web App URL — ไม่ต้องรอผู้ดูแลระบบ
- [ ] อ่าน `Supabase-Migration-Plan-v3.0.md` แล้วนัดคุยว่าจะเริ่มย้ายฐานข้อมูลเมื่อไร

---

## 6. ย้ายฐานข้อมูลไป Supabase (v3.0) — ทำ *หลัง* ข้อ 1–5 ผ่านหมดแล้ว

ไฟล์ `dist/` รอบนี้ใช้ **โหมด `supabase` เป็นค่าเริ่มต้น** และ migration 0008/0009 ถูก apply แล้วทั้งสองโปรเจกต์
จึงต้อง deploy frontend รุ่นนี้พร้อมกันทั้งสองตลาด; เก็บทางถอยกลับเป็น `PM_API.setMode('sheets')` เฉพาะกรณีฉุกเฉิน

### 6.1 ก่อนสลับ (ห้ามสลับลำดับ)

- [ ] apply `supabase/migrations/0008_api_sessions_and_authorization.sql` ทั้งสองโปรเจกต์
      แล้ว deploy frontend ชุดเดียวกันทันที — migration นี้บังคับ session token สำหรับทุก action
      ยกเว้นหน้าสถานะสาธารณะ จึงห้ามเปิดโหมด Supabase ด้วย frontend รุ่นเก่า
- [ ] apply `supabase/migrations/0009_public_status_and_market_state.sql` ทั้งสองโปรเจกต์
      แล้วเปิดหน้าสาธารณะตรวจว่าลา/ขาดแสดงถูกต้อง และล็อคค้างชำระแสดงเฉพาะเลขล็อค
- [ ] ล็อกอินด้วย admin และ viewer ทดสอบการอ่านข้อมูล / ลองให้ viewer แก้ข้อมูล (ต้องถูกปฏิเสธ)
      และเปิด `pamai(_thu)_public_status.html` โดยไม่ล็อกอิน (ต้องยังใช้งานได้)
- [ ] push `.github/workflows/supabase-keepalive.yml` ขึ้น GitHub แล้วกด **Run workflow** ให้เห็นเขียวหนึ่งครั้ง
      (แพลนฟรีหยุดโปรเจกต์ที่เงียบ 7 วัน — ตลาดเปิดสัปดาห์ละครั้ง จึงเฉียดเส้นตลอด)
- [ ] push `.github/workflows/supabase-backup.yml` (ควรอยู่ repo **private**) แล้วกด Run workflow
      → ดาวน์โหลด artifact มาเปิดดูว่ามีข้อมูลจริง
- [ ] ใส่ secret 6 ตัว: `SUPABASE_URL_TUE/THU` · `SUPABASE_ANON_TUE/THU` · `SUPABASE_DB_URL_TUE/THU`
      **ห้ามใส่ `service_role` key ที่ใดทั้งสิ้น**

### 6.2 ทดสอบคู่ขนาน (2 วันตลาด)

- [ ] เปิด console ที่หน้าแอพ แล้วสั่ง `PM_API.setMode('parallel')` → รีเฟรช
- [ ] ใช้งานตามปกติทั้งวันตลาด (ขายจร · รับชำระ · แจ้งลา · ย้ายล็อค)
- [ ] ปลายวันเทียบตัวเลขทุกฉบับ: `await PM_API.compare('getPayments',{date:'<วันตลาด>'})`
      แล้วทำซ้ำกับ `getVendors` · `getDailyBookings` · `getLeaveLog` · `getFloatingQueue`
- [ ] เปิดรายงาน 01–09 ทีละฉบับ เทียบยอดรวมกับของเดิม — ต้องตรงทุกตัวเลข

### 6.3 ตัดมาใช้จริง

- [ ] `PM_API.setMode('supabase')` ทุกเครื่องที่ทีมงานใช้ (มือถือแต่ละเครื่องต้องสั่งเอง —
      ค่านี้อยู่ใน localStorage ของแต่ละเบราว์เซอร์)
- [ ] ตรวจ 5 อย่างนี้ที่หน้างานจริง: ล็อกอินด้วยบัญชีจริง · ผังขึ้นล็อคผู้ค้าประจำครบ ·
      ขายล็อคจร 1 รายการ · เปิดรายงานที่กรองด้วยวันที่ · หน้าสถานะสาธารณะ
- [ ] ทดสอบยกเลิกลา/ขาดเพียง 1 ล็อค แล้วตรวจว่าล็อคอื่นยังคงสถานะเดิม; ทดสอบยกเลิกขายล็อคจรแล้วสถานะหายหลังบันทึกสำเร็จ
- [ ] ตรวจรายงาน 09 ว่ารวมค่าเช่า+ค่าไฟถูกต้อง และไม่แสดงผู้ค้าที่ถูกยกเลิก
- [ ] **เก็บ Apps Script + Google Sheet ไว้อีก 1 เดือน** เป็นทางถอย
      (สั่ง `PM_API.setMode('sheets')` แล้วรีเฟรช = กลับไปใช้ของเดิมทันที — ทดสอบไว้แล้ว)

### 6.4 หลังตัดแล้ว

- [ ] เปลี่ยนชื่อโปรเจกต์ Supabase ฝั่งวันอังคารเป็น `paemai-market-tue` (Settings → General)
- [ ] ตรวจว่า cron สำรองข้อมูลรันเขียวติดกันครบ 7 วัน
- [ ] แก้ข้อความในแอพที่ยังเขียนว่า "Google Sheets" ให้เป็น "ฐานข้อมูล" (ปุ่มตั้งค่าที่หน้าล็อกอิน ฯลฯ)
- [ ] พิจารณาลบบัญชีสาธิต `demo_admin`/`demo_viewer` ออกจาก `core/pamai_app.html`
      (ตอนนี้ใช้ได้เฉพาะตอนไม่มีฐานข้อมูลเลย แต่รหัสยังอยู่ใน public repo)
