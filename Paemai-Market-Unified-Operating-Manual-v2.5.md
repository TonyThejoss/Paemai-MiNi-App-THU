# คู่มือการทำงานของระบบบริหารจัดการตลาดแพไม้ ฉบับรวมสองตลาด (Unified Operating Manual)

**เวอร์ชัน:** 3.0.1 (โค้ดแอพ) + 3.4 (ฐานข้อมูล — ดูหัวข้อ 16–22)
**วันที่จัดทำ:** 17 กรกฎาคม 2026 (อัปเดตล่าสุด: 5 สิงหาคม 2026 — ซ่อมรายงานผู้ค้าปัจจุบันและเพิ่มตัวกรองหลายโซน)
**ครอบคลุม:** ตลาดแพไม้วันอังคาร (ธีมเขียว) + แพไม้มินิวันพฤหัสบดี (ธีมส้ม)
**แทนที่:** Paemai-Market-Tuesday-Operating-Manual-v1.4.md และ แพไม้มินิ-วันพฤหัสบดี-Operating-Manual-v1.1.md ในส่วนของ "วิธีแก้ไขและพัฒนาระบบ" (เอกสารเดิมยังใช้อ้างอิง flow การใช้งานรายวันได้)

---

## สารบัญ

- 0. กฎเหล็กสำหรับ AI และผู้พัฒนา (อ่านก่อนเริ่มทำงานทุกครั้ง)
- 1. บทสรุปสำหรับผู้อ่านใหม่
- 2. หลักการ "แก้ที่เดียว ได้สองแอพ" (Core + Config + Build)
- 3. โครงสร้างโฟลเดอร์ของระบบรวม
- 4. สิ่งที่สองตลาด "เหมือนกันเสมอ" และ "ต่างกันเสมอ"
- 5. ขั้นตอนการแก้ไขระบบ (Workflow มาตรฐาน)
- 6. กลไก MARKET_BLOCK (ข้อมูลเฉพาะตลาดในไฟล์ core)
- 7. กฎการแปลงอัตโนมัติใน build.py
- 8. ฐานข้อมูลและการแยกข้อมูลสองตลาด
- 9. การ Deploy ขึ้น Google Apps Script + GitHub Pages
- 10. ประวัติการรวมระบบ บัคที่แก้ไปแล้ว และหมายเหตุ Migration
- 11. ตารางเวอร์ชันเอกสาร
- 12. เครื่องมือใน `tools/` และลำดับการรันที่ถูกต้อง (เพิ่ม v2.3)
- 13. กฎธุรกิจเรื่องค่าธรรมเนียมและส่วนลดค่าเช่า (เพิ่ม v2.3)
- 14. บทเรียนจาก v2.4: ชื่อคลาสชนกัน และ "ตัวเลือกที่ไม่ได้ผูกกับอะไร" (เพิ่ม v2.4)
- 15. บทเรียนจาก v2.5: แท็กที่ไม่ได้ปิด และ "ความล้มเหลวที่เงียบ" (เพิ่ม v2.5)
- 16. ย้ายฐานข้อมูลไป Supabase v3.0 — สถานะ สิ่งที่แก้ และงานที่เหลือ (เพิ่ม 28 ก.ค. 2026)
- 17. บทเรียนจาก v2.6: "ด่านที่ปิดสวิตช์ตัวเอง" และรอบแก้หน้าสาธารณะ/คิวจอง (เพิ่ม 3 ส.ค. 2026)
- 18. รอบ v2.7: คิวจร ค่าไฟ ผู้ค้าพิเศษ และขายล็อคฟรี (เพิ่ม 3 ส.ค. 2026)
- 19. รอบ v2.8: บัญชียอดค้างและการรับชำระผู้ค้าประจำ (เพิ่ม 4 ส.ค. 2026)
- 20. รอบ v2.9: สิทธิ์ผู้ใช้ ยกเลิกรายการรับเงิน และรวมค่าไฟ (เพิ่ม 5 ส.ค. 2026)
- 21. รอบ v3.0: บัญชีกลาง ป้องกันค่าเช่าซ้ำ และจัดเมนูการเงิน (เพิ่ม 5 ส.ค. 2026)
- 22. รอบ v3.0.1: ซ่อมรายงานผู้ค้าปัจจุบันและตัวกรองหลายโซน (เพิ่ม 5 ส.ค. 2026)

---

## 0. กฎเหล็กสำหรับ AI และผู้พัฒนา (อ่านก่อนเริ่มทำงานทุกครั้ง)

1. **ห้ามแก้ไฟล์ใน `dist/` โดยตรงเด็ดขาด** — โฟลเดอร์ `dist/tuesday/` และ `dist/thursday/` เป็นผลลัพธ์ที่สร้างจากสคริปต์ การแก้ตรงนั้นจะถูกเขียนทับทันทีที่ build ใหม่
2. **แก้โค้ดที่ `core/` ที่เดียวเท่านั้น** แล้วรัน `python3 build.py` — ระบบจะสร้างแอพทั้งสองตลาดให้เองโดยอัตโนมัติ
3. **ห้ามใช้ find-and-replace คำว่า "อังคาร" หรือ "พฤหัสบดี" แบบเหมาทั้งไฟล์** — ในอดีตเคยทำให้ array ชื่อวัน (`const DAYS=[...]`) พังมาแล้ว (แอพวันพฤหัสฯ เคยแสดง "วันพฤหัสบดี" ในตำแหน่งวันอังคาร) กฎการแปลงคำถูกกำหนดไว้ใน `build.py` แล้ว อย่าแปลงเอง
4. **ข้อมูลเฉพาะตลาด (เลขล็อค ผังโซน) ต้องอยู่ในกรอบ `MARKET_BLOCK` เท่านั้น** — ถ้าจะเพิ่มข้อมูลลักษณะนี้ใหม่ ให้ครอบ marker และสร้างไฟล์ block ฝั่งวันพฤหัสฯ คู่กันเสมอ (ดูหัวข้อ 6)
5. **ห้ามรวมฐานข้อมูลสองตลาดเข้าด้วยกันไม่ว่ากรณีใด** — คนละ Google Sheet คนละ Apps Script deployment คนละ Web App URL
6. **ทุกครั้งที่ build เสร็จ ต้องเห็นข้อความ `✅ verify ผ่าน`** — ถ้า verify ฟ้อง ❌ แปลว่ามีข้อมูลของตลาดหนึ่งรั่วไปอีกตลาด ห้าม deploy จนกว่าจะแก้เสร็จ
7. **`pamai_market_map.html` เป็นข้อยกเว้น** — เป็นไฟล์เฉพาะของแต่ละตลาด (ผังตลาดต่างกันทั้งฉบับ) เก็บอยู่ที่ `core/market_specific/` ถ้าแก้ฟีเจอร์ที่หน้าแผนที่ ต้องแก้ทั้งไฟล์ .TUE และ .THU
8. รักษามาตรฐานความปลอดภัยเดิม: รหัสผ่านต้อง hash, ตรวจสิทธิ์ role ก่อนทุก action, log กิจกรรมลง activity_log
9. **เพิ่มไฟล์ core ใหม่ทุกครั้งต้องอัปเดต `FILE_NAMES` ใน `build.py`ด้วย** — ไม่งั้นไฟล์ใหม่จะไม่ถูกแปลงชื่อ/เนื้อหาให้ฝั่งวันพฤหัสฯ ตอน build (ดูหมายเหตุในโค้ด build.py)
10. **ห้ามแก้ไฟล์ `core/` ด้วยมือในส่วนที่ถูกสร้างโดยสคริปต์ใน `tools/`** (บล็อคที่คั่นด้วย marker
    `PM_MOBILE_KIT`, `PM_STATUS_COLORS`, `PM_REPORT_FILTER`) — ต้องแก้ที่ไฟล์สคริปต์ใน `tools/` แล้วรันใหม่
    เสมอ มิฉะนั้นการรันสคริปต์ครั้งถัดไปจะเขียนทับงานที่แก้มือทิ้งทั้งหมด (ดูหัวข้อ 12)
11. **ทุกครั้งก่อน deploy ต้องรัน 5 คำสั่งนี้ให้ผ่านทั้งหมด** (ถ้ารอบนั้นแตะฐานข้อมูล Supabase
    ต้องรัน `tools/test_e2e_supabase.js` และ `tools/test_sheets_guard.js` เพิ่มด้วย — ดูหัวข้อ 16.6) (เพิ่ม 2 ตัวใน v2.5 —
    `tools/test_map_reach.js` และ `tools/test_offline_map.js` ดูหัวข้อ 12):** `python3 tools/check_js.py` (ไวยากรณ์ JS
    ทุกไฟล์), `python3 build.py` (ต้องเห็น ✅ ทั้งสองบรรทัด), `node tools/test_responsive.js dist/tuesday`
    และ `dist/thursday` (ต้องได้ 76/76 ทั้งสองฝั่ง) — ระบบนี้ไม่มี CI/linter อื่นเลย สามคำสั่งนี้คือด่าน
    ตรวจเดียวที่มี ถ้าข้ามแล้วมีไวยากรณ์ผิดหนึ่งจุด หน้านั้นจะขาวทั้งหน้าที่หน้างานทันที
    **และยังไม่พอ** — ต้องทดสอบบนเว็บไซต์จริงที่ deploy แล้วด้วยเสมอ โดยเฉพาะจุดที่แตะข้อมูลในชีต
    เพราะ Google Sheets แปลงค่าที่เราเขียนลงไปโดยที่โค้ดไม่รู้ (เช่นแปลงข้อความวันที่ไทยเป็น Date ปี พ.ศ.
    และแปลงเบอร์โทรเป็นตัวเลขจนเลข 0 นำหน้าหาย) — ความจริงพวกนี้ดูจากโค้ดอย่างเดียวไม่มีวันเห็น
    วิธีที่เร็วที่สุดคือยิง API จริงแล้วดูค่าที่ได้กลับมา (ดูกรณีศึกษาในหัวข้อ 10 ข้อ ก. ของ v2.3)
12. **ห้ามเขียนกฎ CSS ในสคริปต์ `tools/` ด้วย "ชื่อคลาสกว้าง ๆ" แบบตัวเลือกลอย ๆ** (เช่น `.layout`,
    `.card`, `.header`) — สคริปต์ `inject_*` ฝังโค้ดชุดเดียวกันลง **ทุกไฟล์** ถ้าชื่อคลาสไปตรงกับ
    คลาสของอีกหน้าหนึ่งที่ใช้คนละความหมาย หน้านั้นจะพังแบบเงียบ ๆ โดยไม่มี error ใด ๆ ให้เห็น
    (ดูกรณีจริงในหัวข้อ 14) ให้ใช้คลาสเฉพาะแบบ opt-in เช่น `.pm-stack-mobile` แล้วให้หน้าที่ต้องการ
    ติดคลาสนั้นเอง
13. **`<select>` ทุกตัวที่แสดง "เดือน/ปี" ห้าม hardcode `<option>` ในไฟล์เด็ดขาด** — ต้องสร้างรายการ
    ด้วย JavaScript จากวันที่จริงเสมอ และต้องมีโค้ดที่ **อ่านค่าจากช่องนั้นไปใช้จริง** ไม่ใช่แค่แสดงผล
    ตัวเลือกที่ไม่ได้ผูกกับตรรกะใดคือ "ป้ายที่โกหกผู้ใช้" ซึ่งอันตรายกว่าไม่มีช่องนั้นเลย (ดูหัวข้อ 14)
15. **ห้ามปล่อยให้ "โหลดข้อมูลไม่สำเร็จ" หน้าตาเหมือน "ไม่มีข้อมูล" เด็ดขาด** — ทุกจุดที่ดึงข้อมูล
    จากฐานข้อมูลมาแสดง ถ้าล้มเหลวต้อง (ก) แจ้งผู้ใช้ให้เห็นชัด (ข) มีปุ่มลองใหม่ (ค) ไม่แสดงค่าเริ่มต้น
    ที่ผู้ใช้อาจเข้าใจผิดว่าเป็นข้อมูลจริง ห้ามเขียน `if(!ok) return;` เปล่า ๆ อีก (ดูหัวข้อ 15.2)
16. **ทุกการกระทำที่เปลี่ยนข้อมูลต้องเรียก backend และต้องรอผลก่อนแตะหน้าจอ** — ห้ามอัปเดต DOM
    หรือ localStorage ก่อนได้ `status==='ok'` กลับมา เพราะหน้าจอที่เปลี่ยนแล้วแต่ฐานข้อมูลไม่เปลี่ยน
    คือความผิดพลาดที่ผู้ใช้จับไม่ได้จนกว่าจะสายเกินไป (เกิดซ้ำแล้ว 2 รอบ: v2.2 ข้อ 2 และ v2.5 บั๊ก D)
17. **ห้ามฝังรหัสผ่าน/ข้อมูลผู้ค้าจริงในโค้ดทุกไฟล์** — `dist/` ถูกอัปขึ้น GitHub **public repo**
    ทุกครั้ง สิ่งที่ฝังไว้จะอยู่ใน git history ตลอดไปแม้ลบออกภายหลัง (บั๊ก #20 แก้ที่ app.html ไปแล้ว
    แต่ตกหล่น apps_script.js จนถึง v2.5 — ดูหัวข้อ 15.4)
18. **ห้ามใช้ "มี URL ของ Apps Script หรือยัง" เป็นตัวตัดสินว่าเชื่อมต่อฐานข้อมูลแล้วหรือยัง** —
    ทั้งระบบเคยเขียน `if(!getApiUrl())` ไว้ **89 จุดใน 20 ไฟล์** พอย้ายฐานข้อมูลไป Supabase
    ด่านเหล่านี้กลายเป็นสวิตช์ปิดฟีเจอร์เงียบ ๆ ทั้งระบบ (ผังขึ้นว่างทั้งตลาด · ล็อกอินตกไปใช้บัญชี
    สาธิตในไฟล์) ต้องถามผ่านฟังก์ชันกลางตัวเดียวเสมอ (`getApiUrl()` ที่ถูกครอบใน `PM_API_CLIENT`
    หรือ `pmHasBackend()` ในไฟล์ผัง) **และห้ามอ่าน `localStorage['pm-*-api-url']` ตรง ๆ ที่ไหนอีก**
19. **ทางเข้าฐานข้อมูลต้องมีทางเดียวต่อไฟล์** — ห้ามมีโค้ดที่ `fetch` ไป backend เองคู่ขนานกับ
    `apiGet`/`apiPost` (เคยเกิดกับ `pmFetchVendors()` ซึ่งยิงเองจนย้ายฐานข้อมูลแล้วผังว่างทั้งตลาด)
    ถ้าต้องการ timeout/retry ให้ครอบทับ `apiGet` ไม่ใช่เขียน `fetch` ใหม่
20. **สคริปต์ใน `tools/` ต้อง "เขียนทับบล็อคเดิมเสมอ" ห้ามเป็นแบบ "เจอ marker แล้วข้าม"** —
    ไม่งั้นแก้โค้ดในสคริปต์แล้วรันใหม่จะไม่มีอะไรเปลี่ยน และจะเข้าใจผิดว่าแก้แล้ว (เกิดจริงกับ
    `add_vendor_sync()`) ทุกบล็อคต้องมี marker เปิด–ปิด และแทนที่ระหว่างสองจุดนั้นทุกครั้งที่รัน
21. **ข้อความในฐานข้อมูลห้ามมีแท็ก HTML** — หน้ารายงานเอาค่าจากฐานข้อมูลไปต่อกับ `innerHTML` ตรง ๆ
    หลายจุด ค่าที่มีแท็กปนจึงกลายเป็นโค้ดที่รันได้ (พบจริง: `payments.method` เก็บ `<svg>` มาจาก
    ยุค Google Sheets) — ป้องกันด้วย trigger `sanitize_text_cols` ที่ฐานข้อมูล (migration 0006)
    ห้ามพึ่งการกรองที่หน้าเว็บอย่างเดียว
22. **โค้ดกลางที่ฝังทุกไฟล์ ห้ามมีเงื่อนไข "ต้องมีของครบทุกอย่างถึงจะทำงาน"** — ถ้าบล็อคหนึ่งทำหลายหน้าที่
    ต้องแยกเงื่อนไขของแต่ละหน้าที่ออกจากกัน ไฟล์ที่ขาดของชิ้นเดียวต้องเสียเฉพาะหน้าที่นั้น ไม่ใช่เสียทั้งบล็อค
    (เกิดจริง: `PM_API_CLIENT` บังคับต้องมี `apiPost` ทำให้หน้าอ่านอย่างเดียว 10 ไฟล์ตายเงียบ — ดูหัวข้อ 17.1)
23. **โค้ดที่ฝังท้ายไฟล์แล้วครอบฟังก์ชันของหน้า ต้องคิดเรื่องลำดับเวลาด้วยเสมอ** — หน้าอาจเรียกฟังก์ชันนั้น
    ไปแล้วก่อนที่เราจะครอบ ต้องมีกลไกตรวจว่า "หน้านี้ได้ข้อมูลจริงหรือยัง" แล้วสั่งใหม่ให้ (ดูหัวข้อ 17.2)
24. **ห้ามตัดฟิลด์ออกจาก payload ของ endpoint ที่มีผู้ใช้มากกว่าหนึ่งหน้า** — ต้องไล่ดูให้ครบก่อนว่าใครอ่านอยู่บ้าง
    การซ่อนข้อมูลจากผู้ใช้ทำที่ "หน้าที่แสดง" ไม่ใช่ที่ต้นทาง (ดูหัวข้อ 17.7)
25. **ทุกครั้งที่แก้เงื่อนไขใน `tools/inject_*` ต้องรัน `tools/test_api_shim_coverage.js`** — ด่านนี้เป็นตัวเดียว
    ที่พิสูจน์ว่าทุกหน้ายังคุยกับฐานข้อมูลได้จริง ซึ่งการอ่านโค้ดอย่างเดียวไม่มีวันเห็น
26. **ยอดเงินทุกหน้าและทุกรายงานต้องอ่านจากบัญชีลูกหนี้กลางชุดเดียวกัน** — หน้าแสดงผล เช่น ผังตลาด
    และหน้าค่าไฟ ห้ามสร้างหนี้หรือคำนวณยอดค้างซ้ำเอง; การออก/ปรับหนี้รายเดือนให้ผ่าน
    `ensure_vendor_charges_split()` เท่านั้น และแยกองค์ประกอบ `rent` / `electric` ด้วย `charge_code`
    ห้ามใช้คำอธิบายภาษาไทยหรือ `payments.type` เดาว่าเงินเป็นค่าอะไร (ดูหัวข้อ 21)
14. **ก่อนถือว่า deploy backend เสร็จ ต้องตรวจสอบว่า Web App URL ที่ frontend ใช้งานจริงตรงกับ deployment ที่เพิ่งอัปเดต** — โปรเจกต์ GAS หนึ่งโปรเจกต์อาจมี "การทำให้ใช้งานได้" (deployment) มากกว่าหนึ่งตัว แต่ละตัวมี Web App URL คนละอันและอาจค้างอยู่คนละเวอร์ชันของโค้ด การกด "เวอร์ชันใหม่" ใน deployment ที่ไม่ใช่ตัวที่ frontend เรียกจริง จะทำให้เข้าใจผิดว่า deploy สำเร็จทั้งที่หน้าเว็บยังใช้โค้ดเก่าอยู่ (ดูหัวข้อ 9.3 วิธีตรวจสอบ)

---

## 1. บทสรุปสำหรับผู้อ่านใหม่

ระบบนี้คือเว็ปแอพบริหารจัดการตลาดนัด 2 ตลาดของเจ้าของเดียวกัน ได้แก่ ตลาดแพไม้วันอังคาร และแพไม้มินิวันพฤหัสบดี แต่ละตลาดมีแอพครบชุด 20 ไฟล์ (หน้าหลัก, ลงทะเบียนผู้ค้า, แผนที่ตลาด, รับชำระเงิน, ระบบค่าไฟ, ผ่อนชำระ, ประวัติการลา, คิวจองล็อคจร, กฎระเบียบตลาด, หน้าสถานะสาธารณะ, รายงาน 01–08, ศูนย์รวมรายงาน และ backend Apps Script) ทำงานบน Google Apps Script + Google Sheets โดยฝั่ง frontend (ไฟล์ .html) โฮสต์อยู่บน GitHub Pages คนละ repo ต่อตลาด ส่วนฝั่ง backend (Apps Script) รันแยกโปรเจกต์กันคนละตัว

ก่อนหน้านี้สองแอพถูกดูแลแยกกันจนโค้ด "แยกทางกัน" (drift) เกือบพันบรรทัด เวอร์ชัน 2.0 จึงรวมเป็นระบบเดียว: มี **โค้ดกลาง (core) ชุดเดียว** ใช้แอพวันอังคารเป็นต้นแบบ ผสานฟีเจอร์ที่ดีของฝั่งวันพฤหัสฯ กลับเข้ามาครบ (รายงาน 08, ตัวกรอง lockId ใน backend) แล้วใช้สคริปต์ `build.py` สร้างแอพทั้งสองตลาดออกมาโดยอัตโนมัติ ต่อมาวันที่ 21 กรกฎาคม 2026 ได้เพิ่ม 3 ฟีเจอร์ใหม่เข้า core: ระบบคิวจองล็อคจร, หน้ากฎระเบียบตลาด (แก้ไขได้ผ่านหน้าเว็บ), และหน้าสถานะล็อคว่างสาธารณะ (ไม่ต้อง login)

**ผลลัพธ์:** แก้โค้ดครั้งเดียวที่ core → ได้การแก้ไขทั้งสองแอพเสมอ โดยที่ 5 สิ่งที่ต้องต่างกัน (ผังล็อค, ธีมสี, สูตรนับวัน, ฐานข้อมูล, คีย์จัดเก็บในเครื่อง) ไม่มีวันปนกัน

---

## 2. หลักการ "แก้ที่เดียว ได้สองแอพ" (Core + Config + Build)

```
                    ┌──────────────────────┐
                    │   core/  (แก้ที่นี่)   │  ← โค้ดกลาง มาตรฐานวันอังคาร
                    │  20 ไฟล์ + markers    │
                    └──────────┬───────────┘
                               │  python3 build.py
              ┌────────────────┴────────────────┐
              ▼                                 ▼
   ┌─────────────────────┐          ┌──────────────────────────┐
   │  dist/tuesday/       │          │  dist/thursday/           │
   │  = core ตรง ๆ        │          │  = core + แทน MARKET_BLOCK │
   │  ธีมเขียว, นับวันอังคาร │          │    + กฎแปลงอัตโนมัติ        │
   │  Sheet วันอังคาร      │          │  ธีมส้ม, นับวันพฤหัสฯ        │
   └─────────────────────┘          │  Sheet วันพฤหัสฯ           │
                                    └──────────────────────────┘
```

- **แอพวันอังคาร** = core โดยตรง (core เขียนด้วยมาตรฐานวันอังคาร)
- **แอพวันพฤหัสฯ** = core ผ่านการแปลง 2 ชั้น:
  1. แทนที่เนื้อหาใน `MARKET_BLOCK` ด้วยข้อมูลของวันพฤหัสฯ (จาก `config/thu_blocks/`)
  2. ใช้กฎแปลงอัตโนมัติ (สี, วัน, ชื่อตลาด, SHEET_ID, ชื่อไฟล์, คีย์ localStorage)

---

## 3. โครงสร้างโฟลเดอร์ของระบบรวม

```
ระบบรวมสองตลาด/
├── build.py                    ← สคริปต์สร้างสองแอพ (รันทุกครั้งหลังแก้ core)
├── core/                       ← ★ โค้ดกลาง — แก้ไขที่นี่เท่านั้น ★
│   ├── pamai_app.html
│   ├── pamai_apps_script.js
│   ├── pamai_batch_payment.html
│   ├── pamai_electric_system.html
│   ├── pamai_floating_queue.html   ← เพิ่ม 2026-07-21: คิวจองล็อคจร
│   ├── pamai_installment.html
│   ├── pamai_leave_history.html
│   ├── pamai_market_rules.html     ← เพิ่ม 2026-07-21: กฎระเบียบตลาด (แก้ไขได้)
│   ├── pamai_public_status.html    ← เพิ่ม 2026-07-21: หน้าสถานะสาธารณะ (ไม่ต้อง login)
│   ├── pamai_report_01.html … pamai_report_08.html
│   ├── pamai_reports_launcher.html
│   ├── pamai_vendor_registration.html
│   └── market_specific/        ← ข้อยกเว้น: แผนที่ตลาดแยกไฟล์ต่อตลาด
│       ├── pamai_market_map.TUE.html
│       └── pamai_market_map.THU.html
├── config/
│   └── thu_blocks/             ← ข้อมูลเฉพาะตลาดวันพฤหัสฯ (16 ไฟล์)
│       └── <ชื่อไฟล์core>__<ชื่อBLOCK>.txt
├── dist/                       ← ผลลัพธ์ (ห้ามแก้ตรงนี้)
│   ├── tuesday/   (20 ไฟล์ pamai_* + logo.png)
│   └── thursday/  (20 ไฟล์ pamai_thu_* + logo.png)
└── tools/
    └── prepare_core.py         ← สคริปต์ one-off ที่ใช้สร้าง core ครั้งแรก (เก็บไว้อ้างอิง)
```

---

## 4. สิ่งที่สองตลาด "เหมือนกันเสมอ" และ "ต่างกันเสมอ"

### 4.1 เหมือนกันเสมอ (แก้ที่ core แล้วได้ทั้งคู่)

| ด้าน | รายละเอียด |
|---|---|
| ระบบแสดงผล / UI | โครงหน้าจอ ตำแหน่งปุ่ม ฟอนต์ องค์ประกอบทุกหน้า |
| Workflow | flow ลงทะเบียน, รับชำระ, แจ้งลา, จองล็อคจร, ผ่อนชำระ |
| คิวจองล็อคจร | ลงคิว/ขายล็อคจร/ติดตามสถานะล็อคว่างแบบเรียลไทม์ (`pamai_floating_queue.html`, เพิ่ม 21 ก.ค. 2026) |
| กฎระเบียบตลาด | หน้าแก้ไขกฎระเบียบ + ลิงก์สาธารณะสำหรับส่งเข้ากลุ่มไลน์ (`pamai_market_rules.html`, เพิ่ม 21 ก.ค. 2026) |
| หน้าสถานะสาธารณะ | ดูล็อคว่าง/คิว/สถานะขายแล้วโดยไม่ต้อง login (`pamai_public_status.html`, เพิ่ม 21 ก.ค. 2026 — whitelist ข้อมูลที่แสดง ห้ามหลุดเบอร์โทร/LINE/ยอดค้างชำระ/รหัสผ่าน) |
| รายงาน | รายงาน 01–08 โครงสร้างและสูตรเดียวกัน |
| ความปลอดภัย | hash รหัสผ่าน, ระบบ role, activity log, session |
| Backend API | ชุด action ทั้งหมดใน apps_script (รวม lockId filter, ระบบผ่อน, คิวจองล็อคจร, กฎระเบียบตลาด, getPublicStatus) |
| ชื่อชีทในฐานข้อมูล | vendors, leave_log, daily_bookings, payments, activity_log, users, installment_plans, floating_queue, market_rules, **rent_discounts**, **settings** (2 ชีทหลังเพิ่ม v2.3) |
| ระบบสีสถานะล็อค | ชุดสีเดียวกันทั้งสองตลาด (`PM_STATUS_COLORS`) — เฉพาะ "ล็อคผู้ค้าประจำ" ที่ใช้สีแบรนด์ของตลาดนั้น |
| ชุดปรับหน้าจอมือถือ | `PM_MOBILE_KIT` ฝังทุกไฟล์เหมือนกัน (ดูหัวข้อ 12) |
| แถบตัวกรองรายงาน | `PM_REPORT_FILTER` ใช้ร่วมกันทั้ง 8 ฉบับ — โซน + วันเดียว/ช่วงวันที่/เดือน-ปี/ทั้งหมด |

### 4.2 ต่างกันเสมอ (ห้ามทำให้เหมือนกัน!)

| # | ด้าน | วันอังคาร | วันพฤหัสบดี | กลไกที่ควบคุม |
|---|---|---|---|---|
| 1 | เลขล็อค + ผังโซน | SA, SB–SF, A–D, F (โรงหน้า/โรงใหญ่/โซนนอก) | A–F, G–U (ข้าง 7-Eleven), O–AB (ข้างโลตัส), TA–TC (เปิดท้าย) | `MARKET_BLOCK` + `market_specific/` |
| 2 | ธีมสี | เขียว (`--g:#1a3d2b`, `--gm:#2d6a4f`, `--gb:#40916c`, `--gt:#52b788`) | ส้ม (`--g:#7c2d12`, `--gm:#c2410c`, `--gb:#ea580c`, `--gt:#f97316`) | ตารางสีใน build.py |
| 3 | สูตรนับวันตลาด/เดือน | `getDay()===2` (นับวันอังคาร) | `getDay()===4` (นับวันพฤหัสฯ) | กฎแปลงใน build.py |
| 4 | ฐานข้อมูล | Sheet ID `1c1pag…WJu4` | Sheet ID `1o-pBC…pMN8` | กฎแปลงใน build.py |
| 5 | คีย์ localStorage | `pm-tue-api-url`, `pm-tue-api-token`, `pm-t`, `pm-users`, `pm-session`, `pamai-theme` | `pm-thu-api-url`, `pm-thu-api-token`, `pmthu-t`, `pmthu-users`, `pmthu-session`, `pamai-thu-theme` | กฎแปลงใน build.py |

**ระบบ API Token (B-13 — ครบทุกไฟล์แล้วใน v2.0):** backend ตรวจ `token` ทุก request ถ้าตั้งค่า Script Properties > `API_TOKEN` ไว้ (ไม่ตั้ง = โหมดผ่อนผัน) ฝั่งหน้าเว็บทุกไฟล์ส่ง token จาก localStorage อัตโนมัติ ตั้งค่าครั้งแรกผ่านลิงก์ `?api=<url>&token=<ค่า>` ได้เลย ห้าม hardcode ค่า token ในโค้ด (repo เป็น public)

หมายเหตุ: สี `#7c2d12` / `#c2410c` ปรากฏในแอพวันอังคารด้วยในบทบาท "สีสถานะค้างชำระ" — ไม่ใช่สีธีม ห้ามสับสน

---

## 5. ขั้นตอนการแก้ไขระบบ (Workflow มาตรฐาน)

**เมื่อได้รับคำสั่งแก้ไข/เพิ่มฟีเจอร์ ให้ทำตามลำดับนี้ทุกครั้ง:**

1. อ่านคู่มือเล่มนี้ โดยเฉพาะหัวข้อ 0 และ 4
2. แก้ไขไฟล์ใน `core/` (ถ้าเป็นหน้าแผนที่ตลาด → แก้ทั้ง `.TUE.html` และ `.THU.html` ใน `market_specific/`)
3. ถ้าการแก้ไขแตะข้อมูลเฉพาะตลาด (เลขล็อค/โซน) → อัปเดตไฟล์ใน `config/thu_blocks/` คู่กันด้วย
4. ถ้าเพิ่มไฟล์ core ใหม่ → เพิ่มชื่อไฟล์ใน `FILE_NAMES` ของ `build.py` ด้วย (ดูกฎเหล็กข้อ 9)
5. รัน `python3 build.py` ในโฟลเดอร์ `ระบบรวมสองตลาด/`
6. ตรวจว่าเห็น `✅ build เสร็จ` และ `✅ verify ผ่าน` ครบทั้งสองบรรทัด
7. นำไฟล์จาก `dist/tuesday/` และ `dist/thursday/` ไป deploy (หัวข้อ 9) — **หลัง deploy backend ให้ตรวจสอบ URL จริงที่ frontend ใช้ตามหัวข้อ 9.3 เสมอ ก่อนถือว่างานเสร็จ**

**ตัวอย่างคำสั่งที่ AI ควรตีความถูก:**
- "เพิ่มปุ่ม export Excel ในรายงาน 05" → แก้ `core/pamai_report_05.html` ไฟล์เดียว → build → ได้ทั้งสองตลาด
- "เพิ่มล็อคใหม่ SA34 ในตลาดวันอังคาร" → แก้เฉพาะข้อมูลใน core (ฝั่ง TUE เป็นมาตรฐาน) + ไฟล์แผนที่ .TUE — **ไม่แตะ** thu_blocks
- "โซนใหม่ TD ที่ตลาดวันพฤหัสฯ" → แก้เฉพาะ `config/thu_blocks/*` + แผนที่ .THU — **ไม่แตะ** core ส่วนกลาง

---

## 6. กลไก MARKET_BLOCK (ข้อมูลเฉพาะตลาดในไฟล์ core)

ในไฟล์ core จะพบ marker 2 รูปแบบ:

```js
//== MARKET_BLOCK:ชื่อบล็อค ==//        ← ในโค้ด JavaScript
...เนื้อหาเวอร์ชันวันอังคาร...
//== END_MARKET_BLOCK ==//
```
```html
<!--== MARKET_BLOCK:ชื่อบล็อค ==-->     ← ในโค้ด HTML
...เนื้อหาเวอร์ชันวันอังคาร...
<!--== END_MARKET_BLOCK ==-->
```

ตอน build ฝั่งวันพฤหัสฯ เนื้อหาระหว่าง marker จะถูกแทนด้วยไฟล์ `config/thu_blocks/<ชื่อไฟล์>__<ชื่อบล็อค>.txt`

### บล็อคทั้งหมดในระบบ (16 บล็อค)

| ไฟล์ core | บล็อค | เนื้อหา |
|---|---|---|
| pamai_app.html | SEED | ข้อมูลผู้ค้าตัวอย่าง (THU = โหลดจาก Sheets เท่านั้น) |
| pamai_app.html | VDB_ZONES | ZONE_TOTALS/ZONE_GROUPS/TOTAL_LOCKS ของแท็บทะเบียนผู้ค้า |
| pamai_app.html | VDB_ZONE_OPT | ตัวเลือกโซนใน dropdown ทะเบียนผู้ค้า |
| pamai_batch_payment.html | ZONE_OPT | ตัวเลือกโซนใน dropdown กรอง |
| pamai_electric_system.html | ZONE_OPT / SEED / EXPORT_ZONES | dropdown โซน / ข้อมูลตัวอย่าง / รายชื่อโซนใน export |
| pamai_floating_queue.html | LOCKS | ผังเลขล็อคทั้งหมดสำหรับให้เลือกตอนขายล็อคจร (เพิ่ม 21 ก.ค. 2026) |
| pamai_installment.html | MAP_DEFAULT / MAP_TABS / ZONE_GROUPS | โซนเริ่มต้น / ปุ่มแท็บแผนที่ / การจัดกลุ่มโซน |
| pamai_public_status.html | LOCKS | ผังเลขล็อคทั้งหมดสำหรับแสดงในหน้าสถานะสาธารณะ (เพิ่ม 21 ก.ค. 2026) |
| pamai_vendor_registration.html | LOCKS | ผังเลขล็อคทั้งหมดสำหรับลงทะเบียน |
| pamai_report_01.html | ZONES | ZONE_TOTALS + ZONE_GROUPS |
| pamai_report_03.html | ZONES | ZONE_LOCKS (เลขล็อคจริงรายโซน) + ZONE_GROUP_NAME |
| pamai_report_04.html | ZONES | ZONE_ORDER + ZONE_GROUP_NAME |
| pamai_report_07.html | ZONES | ZONE_ORDER + ZONE_GROUP_NAME |
| pamai_report_08.html | ZONES | ZONE_ORDER + ZONE_GROUP_NAME |

หมายเหตุ: `pamai_market_rules.html` **ไม่มี** MARKET_BLOCK เพราะเนื้อหากฎระเบียบไม่ผูกกับเลขล็อค/โซน — ข้อความกฎระเบียบเก็บและแก้ไขได้จาก Google Sheets โดยตรง (ชีท `market_rules`) เหมือนกันทั้งสองตลาด

**กฎ:** ห้ามลบ/ย้าย marker และถ้าเพิ่มบล็อคใหม่ต้องสร้างไฟล์ thu_blocks คู่กันทันที มิฉะนั้น build จะเตือน `⚠ no THU block` และคงข้อมูลวันอังคารไว้ (ข้อมูลผิดตลาด!)

---

## 7. กฎการแปลงอัตโนมัติใน build.py

ลำดับการแปลง core → thursday (ลำดับสำคัญ ห้ามสลับ): แทน MARKET_BLOCK ก่อน แล้วจึงแปลงข้อความ

1. **สี** — จับคู่เขียว→ส้ม 10 คู่ รวมโหมดมืด (ดูตาราง `COLORS` ใน build.py)
2. **คำ** — `วันอังคาร→วันพฤหัสบดี`, `ตลาดแพไม้→แพไม้มินิ`, `Tuesday→Thursday` (เจาะจงคำเต็ม ไม่แตะ `DAYS` array)
3. **สูตรนับวัน** — `getDay()===2 → getDay()===4`
4. **SHEET_ID** — สลับเป็นของวันพฤหัสฯ
5. **คีย์ localStorage** — เติม prefix `thu` ตามตารางหัวข้อ 4.2
6. **ชื่อไฟล์และลิงก์** — `pamai_*.html → pamai_thu_*.html` (รายชื่อไฟล์กำหนดใน `FILE_NAMES` — **ต้องอัปเดตทุกครั้งที่เพิ่มไฟล์ core ใหม่** เช่น `pamai_floating_queue.html`, `pamai_market_rules.html`, `pamai_public_status.html` ที่เพิ่มเมื่อ 21 ก.ค. 2026)

หลัง build ระบบ verify อัตโนมัติ: สแกน dist/thursday หา SHEET_ID เขียว, สีเขียวธีม, คำว่า "วันอังคาร", คีย์ `pm-tue-`, `getDay()===2` และสแกน dist/tuesday หาสิ่งของวันพฤหัสฯ — ต้องเป็นศูนย์ทั้งหมด

---

## 8. ฐานข้อมูลและการแยกข้อมูลสองตลาด

> ✅ **อัปเดต 29 ก.ค. 2026:** ทั้งสองแอพใช้ Supabase จริงแล้ว โดยแยกโปรเจกต์ตลาดละ 1 ตัว
> Google Sheets/Apps Script เก็บไว้เป็นทางถอยชั่วคราวเท่านั้น — สถานะ รายละเอียดโปรเจกต์ใหม่
> และวิธีตรวจสอบอยู่ที่ **หัวข้อ 16**

| | ตลาดวันอังคาร | แพไม้มินิวันพฤหัสฯ |
|---|---|---|
| Google Sheet | `1c1pagnhTt3c4AUUgMeI15m14PKgoXuco_r8TJRUWJu4` | `1o-pBCouIVr2d8UEI33rVXOZf1Ij3b2JxcjrbgBmpMN8` |
| Apps Script deployment | ของตัวเอง (จาก dist/tuesday/pamai_apps_script.js) | ของตัวเอง (จาก dist/thursday/pamai_thu_apps_script.js) |
| Web App URL | บันทึกในเครื่องที่คีย์ `pm-tue-api-url` | บันทึกในเครื่องที่คีย์ `pm-thu-api-url` |

- แต่ละแอพเชื่อมต่อ Supabase ของตัวเองโดยอัตโนมัติ; ตลาดวันอังคารและวันพฤหัสฯ **ห้ามใช้ข้อมูลข้ามกัน**. ค่า Web App URL เดิมใช้ได้เฉพาะทางถอยกลับไป Sheets.
- Schema ชีททั้ง 9 (vendors, leave_log, daily_bookings, payments, activity_log, users, installment_plans, floating_queue, market_rules) เหมือนกันทั้งสองตลาด ดูรายละเอียดคอลัมน์ได้จากฟังก์ชัน `initSheets()` ใน `core/pamai_apps_script.js`
- backend ทั้งสองฝั่งรองรับพารามิเตอร์ `lockId` ใน `getLeaveLog` / `getDailyBookings` (ฟีเจอร์จากฝั่งวันพฤหัสฯ ที่ถูกรวมเข้า core แล้ว)
- ระบบผ่อนชำระ (installment_plans + saveInstallmentPlan/deleteInstallmentPlan) มีครบทั้งสองตลาดแล้ว (เดิมมีเฉพาะวันอังคาร)
- ระบบคิวจองล็อคจร (`floating_queue` + getFloatingQueue/saveFloatingQueueEntry/sellFloatingQueueEntry/editFloatingQueueEntry/cancelFloatingQueueEntry) และกฎระเบียบตลาด (`market_rules` + getMarketRules/saveMarketRules) เพิ่มเข้ามาเมื่อ 21 ก.ค. 2026 มีครบทั้งสองตลาด
- `getPublicStatus` เป็น action เดียวที่**ไม่ต้องมี token ก็เรียกได้** (อยู่ใน `PUBLIC_ACTIONS` whitelist ของ backend) เพราะใช้แสดงหน้าสถานะสาธารณะ — คืนเฉพาะข้อมูลล็อคว่าง/คิว/สถานะขาย/ลา-ขาด และตามที่ผู้ดูแลอนุมัติ **หมายเลขล็อคที่ค้างชำระเท่านั้น**. ห้ามคืนชื่อ เบอร์โทร LINE ยอดเงิน รหัสผ่าน หรือ salt เด็ดขาด; ถ้าจะเพิ่มข้อมูลต้องตรวจ whitelist ทุกครั้ง

---

## 9. การ Deploy ขึ้น Google Apps Script + GitHub Pages

Deploy แบ่งเป็น 2 ส่วนที่แยกกันเด็ดขาด: **backend** (Apps Script, ต่อ Google Sheets) กับ **frontend** (ไฟล์ .html, โฮสต์บน GitHub Pages) — ทำแยกกันทีละตลาดเสมอ ห้ามสลับโฟลเดอร์ ตรวจง่าย ๆ ว่าไฟล์ฝั่งพฤหัสฯ ทุกไฟล์ขึ้นต้น `pamai_thu_` และ SHEET_ID ใน Code.gs ต้องลงท้าย `…pMN8`

### 9.1 Backend — Google Apps Script (คนละโปรเจกต์ต่อตลาด)

1. เปิดโปรเจกต์ GAS ของตลาดนั้น ๆ (ยืนยันตัวโปรเจกต์ถูกต้องด้วยการเช็ค `SHEET_ID` ในโค้ด ให้ตรงกับตารางหัวข้อ 8)
2. เปิดไฟล์ `Code.gs` แล้ววางเนื้อหาทั้งหมดจาก `dist/<tuesday|thursday>/pamai_<thu_>apps_script.js` ทับของเดิม (ไฟล์ยาวและมีอักษรไทยเยอะ — ถ้าคัดลอกด้วยการพิมพ์/Ctrl+A จำลองแล้วเนื้อหาไม่ครบ ให้ใช้วิธีรัน JavaScript ฝั่ง console เรียก `monaco.editor.getEditors()[0].getModel().setValue(newContent)` แทน แล้วตรวจว่า `ed.getValue() === src`)
3. บันทึก (Ctrl+S)
4. เปิด "การทำให้ใช้งานได้" → **"จัดการการทำให้ใช้งานได้"**
5. **สำคัญ: โปรเจกต์เดียวอาจมี deployment (การทำให้ใช้งานได้) มากกว่า 1 ตัวที่ยังเปิดใช้งานอยู่พร้อมกัน** — แต่ละตัวมี Web App URL ของตัวเองและอาจค้างคนละเวอร์ชันโค้ด ต้องเลือก **ทุกตัวที่ยังมีการใช้งานจริง** (ดูวิธีหาตัวที่ frontend ใช้จริงในข้อ 9.3) ไม่ใช่แค่ตัวที่เห็นเป็นตัวแรก/ตัวล่าสุด
6. สำหรับ deployment แต่ละตัวที่ต้องอัปเดต: กดไอคอนดินสอ (แก้ไข) → ตรง "เวอร์ชัน" เลือก **"เวอร์ชันใหม่"** (หรือถ้ามีการอัปเดตตัวอื่นไปแล้วในรอบเดียวกัน จะเลือกเวอร์ชันที่เพิ่งสร้างจากรายการก็ได้ ไม่ต้องสร้างซ้ำ) → ใส่คำอธิบายสั้น ๆ → กด "การทำให้ใช้งานได้" — วิธีนี้ **Web App URL เดิมจะไม่เปลี่ยน**
7. ทำซ้ำจนครบทุก deployment ที่ยังใช้งานอยู่ (ข้อ 5)

### 9.2 Frontend — GitHub Pages (คนละ repo ต่อตลาด)

| | ตลาดวันอังคาร | แพไม้มินิวันพฤหัสฯ |
|---|---|---|
| Repo | `TonyThejoss/Paemai-Market-Tuesday` (public) | `TonyThejoss/Paemai-MiNi-App-THU` (public) |
| หน้าเว็บ | `https://tonythejoss.github.io/Paemai-Market-Tuesday/` | `https://tonythejoss.github.io/Paemai-MiNi-App-THU/` |

1. เข้า `https://github.com/<repo>/upload/main/` (อัปโหลดที่ root ของ repo เท่านั้น — เคยมีบัคตอนอัปโหลดใส่ subfolder)
2. ลากไฟล์ทั้งหมดจาก `dist/tuesday/` (หรือ `dist/thursday/`) รวม `logo.png` เข้าไปแทนที่ไฟล์เดิม
3. ใส่ commit message อธิบายการเปลี่ยนแปลง → เลือก "Commit directly to the main branch" → Commit changes
4. รอ GitHub Pages build ~35–70 วินาที (deployment ใหม่จะขึ้นในแท็บ "Deployments" ของ repo)
5. **ห้าม hardcode** รหัสผ่าน, Sheet ID จริง, หรือ Web App exec URL จริงลงในไฟล์ที่ push เข้า repo public เหล่านี้ (GitHub มีระบบตรวจจับและบล็อกอัตโนมัติ) — ใช้กลไกลิงก์ตั้งค่าครั้งเดียว `?api=<url>&token=<ค่า>` ที่เก็บลง localStorage แทน (ดูหัวข้อ 4.2)

### 9.3 การตรวจสอบว่า deploy สำเร็จจริง (ทำทุกครั้งหลัง deploy backend)

ห้ามถือว่า deploy backend เสร็จแค่เพราะกด "การทำให้ใช้งานได้" แล้วไม่ error — ต้องตรวจว่า Web App URL ที่ **หน้าเว็บจริงเรียกใช้งานอยู่** ตรงกับเวอร์ชันที่เพิ่งอัปเดต:

1. เปิดหน้าเว็บจริงบน GitHub Pages (เช่น `pamai_app.html`) แล้วอ่านค่าใน localStorage ของโดเมนนั้น (คีย์ `pm-tue-api-url` / `pm-thu-api-url`) — นี่คือ URL ที่ frontend ใช้จริง ไม่ใช่ URL ที่เพิ่งคัดลอกมาจากหน้า deploy
2. เทียบ URL นั้นกับ URL ของ deployment ที่เพิ่งอัปเดตในข้อ 9.1 — ถ้าไม่ตรงกัน แปลว่ายังมี deployment อื่นที่ frontend ใช้งานอยู่จริงแต่ยังไม่ได้อัปเดต ให้กลับไปทำข้อ 9.1 กับ deployment ตัวนั้นเพิ่ม
3. ยิง request ทดสอบตรง ๆ ไปที่ URL นั้นด้วย action ที่เพิ่งเพิ่มใหม่ (เช่น `?action=getMarketRules`) — ถ้าได้ `Unknown action` แปลว่ายังเป็นโค้ดเก่า ถ้าได้ผลลัพธ์ปกติหรือ `Unauthorized` (กรณีตั้ง API_TOKEN ไว้) แปลว่าโค้ดใหม่ทำงานแล้วจริง
4. เปิดหน้าฟีเจอร์ใหม่จริงบน GitHub Pages (เช่น `pamai_floating_queue.html`, `pamai_public_status.html`) แล้วดูว่าโหลดข้อมูลจาก Google Sheets ได้โดยไม่ค้างที่ "โหลดข้อมูลไม่สำเร็จ"

**เหตุการณ์จริงที่พบ (21 ก.ค. 2026):** โปรเจกต์ GAS ของตลาดวันอังคารมี deployment ที่ใช้งานจริงอยู่ 2 ตัว (`v2.0 unified core+b...` กับ `v1.1 - hash password...`) แต่ frontend ที่ deploy ไว้จริงบน GitHub Pages ใช้ URL ของตัว "v1.1" ไม่ใช่ตัวที่อัปเดตก่อน ทำให้ตอนแรกดูเหมือน deploy เสร็จ (โค้ดถูกอัปเดตและทดสอบผ่านทาง URL ที่คัดลอกมาสำเร็จ) แต่หน้าเว็บจริงยังเรียก endpoint ใหม่ไม่ได้ (หน้า `pamai_public_status.html` ค้างที่ "โหลดข้อมูลไม่สำเร็จ") จนกระทั่งตรวจ localStorage เจอ URL อีกตัว แล้วอัปเดต deployment ที่สองด้วย จึงใช้งานได้ครบ — จึงเป็นที่มาของกฎเหล็กข้อ 10 และหัวข้อนี้

---

## 10. ประวัติการรวมระบบ บัคที่แก้ไปแล้ว และหมายเหตุ Migration

### สิ่งที่เปลี่ยนในการรวมระบบ (17 ก.ค. 2026)

- รวมโค้ดสองแอพ (drift ~933 บรรทัดใน app.html) เป็น core เดียว โดยใช้วันอังคารเป็นต้นแบบ
- **ฟีเจอร์ที่ฝั่งวันพฤหัสฯ ได้เพิ่ม:** ระบบผ่อนชำระใน backend, migrateHashPasswords, ข้อมูลตัวอย่างในหน้า electric/app แบบมาตรฐานเดียวกัน
- **ฟีเจอร์ที่ฝั่งวันอังคารได้เพิ่ม:** รายงาน 08 (รายได้ค่าไฟฟ้ารับชำระจริงแยกโซน) + การ์ดในศูนย์รวมรายงาน, ตัวกรอง lockId ใน backend
- **บัคที่แก้:** DAYS array ของแอพวันพฤหัสฯ ที่เพี้ยนเป็น `['อาทิตย์','จันทร์','พฤหัสบดี','พุธ',…]` กลับมาถูกต้อง
- **ความปลอดภัยข้อมูลในเครื่อง:** คีย์ localStorage ของสองแอพแยก prefix กันสมบูรณ์ ป้องกัน session/ข้อมูลค้างปนกันเมื่อเปิดสองแอพในเบราว์เซอร์เดียวกัน

### สิ่งที่เพิ่มในเวอร์ชัน 2.1 (21 ก.ค. 2026)

- **เพิ่มไฟล์ core ใหม่ 3 ไฟล์** (ทั้งสองตลาดได้พร้อมกันผ่าน build.py):
  - `pamai_floating_queue.html` — ระบบคิวจองล็อคจร: ลงคิว/ขายให้ผู้ค้า/แจ้งลาล็อค/ติดตามล็อคว่างแบบเรียลไทม์ พร้อม backend actions ชุดใหม่ (`getFloatingQueue`, `saveFloatingQueueEntry`, `sellFloatingQueueEntry`, `editFloatingQueueEntry`, `cancelFloatingQueueEntry`) และชีท `floating_queue`
  - `pamai_market_rules.html` — หน้าแก้ไขกฎระเบียบตลาด (เก็บในชีท `market_rules`) พร้อมลิงก์เวอร์ชันสาธารณะสำหรับส่งเข้ากลุ่มไลน์ backend actions ใหม่ (`getMarketRules`, `saveMarketRules`)
  - `pamai_public_status.html` — หน้าสถานะล็อคว่าง/คิว/ขายแล้ว สำหรับสาธารณะ ไม่ต้อง login ใช้ `getPublicStatus` (action เดียวที่ไม่บังคับ token) กรองข้อมูลด้วย whitelist ห้ามหลุดข้อมูลอ่อนไหว
- อัปเดต `build.py`: เพิ่มทั้ง 3 ไฟล์ใน `FILE_NAMES` เพื่อให้ build แปลงชื่อไฟล์/เนื้อหาให้ฝั่งวันพฤหัสฯ ถูกต้อง
- เพิ่ม MARKET_BLOCK ใหม่ 2 บล็อค: `pamai_floating_queue.html__LOCKS` และ `pamai_public_status.html__LOCKS` (ผังเลขล็อคสำหรับสองหน้านี้)
- **ค้นพบระหว่าง deploy:** โปรเจกต์ GAS อาจมี deployment ที่ใช้งานจริงมากกว่า 1 ตัว การอัปเดตแค่ตัวเดียวไม่พอ ต้องตรวจ localStorage (`pm-tue-api-url` / `pm-thu-api-url`) เทียบกับ URL ของ deployment ที่อัปเดต ก่อนถือว่า deploy เสร็จ (รายละเอียดเต็มในหัวข้อ 9.3) — เพิ่มเป็นกฎเหล็กข้อ 10 และขั้นตอน 9.3 ในคู่มือฉบับนี้

### สิ่งที่แก้ไขในเวอร์ชัน 2.2 (23 ก.ค. 2026)

**ต้นเหตุของคำร้องเรียน 4 เรื่อง — ตรวจสอบและแก้ไขครบทุกจุด:**

1. **ยืนยันความปลอดภัยของระบบรีเซ็ต 3 วันหลังวันตลาด** — ตรวจโค้ดใน `pamai_market_map.THU.html`/`.TUE.html` ยืนยันว่ากลไก `STATE_KEY`/`STATE_EXPIRE_DAYS=3` เก็บเฉพาะ `lockAbsent` (สถานะลา/ขาด) และ `dailyBookings` (สถานะขายล็อคจรวันนั้น) ไว้ใน localStorage เท่านั้น ไม่แตะข้อมูลผู้ค้าประจำ/การจองล็อคถาวรซึ่งอยู่ใน Google Sheet และถูกดึงสดใหม่ทุกครั้งผ่าน `getVendors()` แยกจากตัวนับ 3 วันโดยสิ้นเชิง — **สรุป: ข้อมูลผู้ค้าประจำจะไม่หายจากการรีเซ็ตอัตโนมัติ หายได้เฉพาะเมื่อกดยกเลิกสัญญาเช่าเท่านั้น**

2. **แก้บั๊ก "ยกเลิกการเช่า" กดไม่ติด/ข้อมูลไม่ persist** (ทั้งสองตลาด รวมล็อค a1/a2 ฝั่งวันพฤหัสฯ):
   - **วันพฤหัสฯ:** `confirmCancelLease()` เรียก `apiPost` โดยไม่ห่อข้อมูลด้วย `{data:{...}}` ตามสัญญาที่ backend (`doPost`) กำหนด ทำให้ `body.data === undefined` เกิด `TypeError` ที่ backend แต่ error ถูกดักไว้เงียบๆ หน้าเว็บเลยดูเหมือนสำเร็จทั้งที่ไม่ได้บันทึกจริง
   - **วันอังคาร:** `confirmCancelLease()` ไม่มีโค้ดเรียก backend เลย มีแค่เปลี่ยนหน้าจอ (DOM) ชั่วคราว ข้อมูลเดิมจึงกลับมาแสดงทันทีที่รีเฟรช — เพิ่มการเรียก `apiPost('saveVendor', ...)` และ `apiPost('logLeave', ...)` ที่ขาดไปทั้งหมด
   - **ทั้งสองตลาด:** ใช้ค่าสถานะ `'cancelled'` ซึ่งไม่ตรงกับค่ามาตรฐานของระบบ (`'terminated'` ที่หน้าแดชบอร์ดหลัก/ทะเบียนผู้ค้า/backend ใช้ตรงกัน) ทำให้ล็อคที่ยกเลิกแล้วไม่ถูกซ่อนออกจากผังตลาดจริง — แก้เป็น `'terminated'` ทั้งหมด พร้อมเพิ่ม error toast แจ้งผู้ใช้เมื่อบันทึกไม่สำเร็จ (เดิมไม่มี ทำให้ผู้ใช้ไม่รู้ตัวว่าบันทึกล้มเหลว)
   - ทดสอบยิงจริงกับ backend ที่ deploy อยู่แล้วก่อนคืนค่ากลับ ยืนยัน fix ใช้งานได้จริง

3. **เพิ่มระบบรีเฟรชข้อมูลข้ามแท็บอัตโนมัติ (ไม่ต้องกด F5)** — ต้นเหตุคือสถาปัตยกรรม shell ของ `pamai_app.html`/`pamai_thu_app.html` โหลดแต่ละเมนู (ผังตลาด/ทะเบียนผู้ค้า/ล็อคจร ฯลฯ) เป็น `<iframe>` ครั้งเดียวแล้วแคชไว้ (`loadedModules` Set) สลับแท็บแค่ซ่อน/โชว์ ไม่ได้ดึงข้อมูลใหม่ — เพิ่มกลไก `postMessage` สองทาง: ทุก `apiPost()` ที่บันทึกสำเร็จจะส่งสัญญาณ `pm-data-changed` ไปยัง shell หลัก, shell รับแล้วรีเฟรชแดชบอร์ดตัวเอง (`loadVendorsFromSheets()`) และส่งสัญญาณ `pm-refresh` ต่อให้ทุก iframe โมดูลอื่นที่เปิดค้างไว้ (ยกเว้นตัวที่เพิ่งบันทึก) ให้โหลดข้อมูลใหม่ทันที ครอบคลุมทุกจุด: บันทึกผู้ค้า, แจ้งลา, แจ้งขาด, ขายล็อคจร, ยกเลิกการเช่า

4. **แก้บั๊กหน้าล็อกอินอ่านไม่ออกในธีมมืด** — สาเหตุคือ CSS `.login-field input:focus` เดิมบังคับ `background:#fff` ทุกธีมโดยไม่ได้ reset สีตัวอักษร ในธีมมืดตัวอักษรสีอ่อน (#d8ead0) จึงกลายเป็นมองไม่เห็นตอนโฟกัส/พิมพ์ (ขาวบนขาว) — เพิ่ม override เฉพาะธีมมืดสำหรับ `:focus` พร้อมป้องกัน autofill ของเบราว์เซอร์ (`:-webkit-autofill`) ทับสีธีมด้วย (จุดที่ไม่เคยมี override มาก่อน)

**Google Apps Script (backend):** ตรวจสอบแล้วไม่ต้องแก้ — ต้นเหตุทั้งหมดอยู่ที่โค้ดฝั่ง frontend (รูปแบบ payload ผิด/ค่าสถานะไม่ตรงมาตรฐาน) ไม่ใช่ backend

**Deploy:** อัปเดต `dist/thursday/` และ `dist/tuesday/` (ไฟล์ `pamai(_thu)_app.html`, `pamai(_thu)_market_map.html`, `pamai(_thu)_floating_queue.html`, `pamai(_thu)_vendor_registration.html`) ขึ้น GitHub ผ่านหน้าเว็บ "Add files via upload" ของทั้งสอง repo (`Paemai-MiNi-App-THU`, `Paemai-Market-Tuesday`) บน branch `main` โดยตรง — commit `cac742b` (วันพฤหัสฯ) และ `eb056b2` (วันอังคาร) GitHub Pages deploy อัตโนมัติภายใน 1-2 นาทีหลัง commit

### รอบแก้ผังตลาดและรายงานผู้ค้าปัจจุบัน (29 ก.ค. 2026)

- หน้าแสดงสถานะสาธารณะของทั้งสองตลาดเพิ่มส่วน “ล็อคที่ค้างชำระ” โดยแสดง **เฉพาะหมายเลขล็อค** ไม่มีชื่อ เบอร์โทร หรือยอดเงิน และเพิ่มสถานะแจ้งลา/แจ้งขาดจากแหล่งข้อมูลเดียวกัน
- ผังตลาดดึงสถานะลา/ขาดและล็อคจรจากฐานข้อมูลใหม่ทุกครั้งแล้วแทนที่ชุดเดิมทั้งชุด จึงยกเลิกเฉพาะล็อคที่เลือกได้ ไม่รีเซ็ตล็อคอื่น และการยกเลิกขายล็อคจรจะรอผลบันทึกสำเร็จก่อนเปลี่ยนหน้าจอ
- สถานะลา/ขาดและล็อคจรของรอบตลาดจะหมดอายุอัตโนมัติเมื่อถึง 00:00 ของวันที่สามหลังวันตลาด: วันศุกร์สำหรับตลาดวันอังคาร และวันอาทิตย์สำหรับตลาดวันพฤหัสฯ. ประวัติยังคงอยู่ในฐานข้อมูล
- การยกเลิกผู้ค้าประจำบันทึกเป็น `terminated` แล้วโหลดข้อมูลใหม่จากฐานข้อมูล; ผังวันพฤหัสฯ จะเคลียร์ข้อมูลผู้ค้าที่ถูกยกเลิกก่อนลงข้อมูลชุดล่าสุด จึงไม่ค้างบนผัง
- เพิ่มรายงาน 09 “ข้อมูลผู้ค้าปัจจุบันทั้งหมด” ทั้งสองตลาด: โซน, เลขล็อค, ชื่อ, เบอร์โทร, สินค้า, ค่าเช่า, ค่าไฟ และยอดรวมค่าเช่า+ค่าไฟ (ไม่นับผู้ค้าที่ `terminated`).
- แก้การย้ายโหมดบนเครื่องผู้ใช้เดิม: แอพรุ่นก่อนเคยบันทึก `sheets` ใน localStorage ทำให้ค่านี้ชนะค่าเริ่มต้น `supabase` ของรุ่นใหม่. รุ่น v3.2 วางตัวเริ่ม Supabase ไว้ใน `<head>` ก่อนโค้ดแอพเดิม แล้วเปลี่ยนเป็น Supabase และล้าง session ของ Sheets ให้อัตโนมัติ **ครั้งเดียว** ต่อเครื่อง จึงต้องล็อกอินใหม่ 1 ครั้งเพื่อรับ session token ที่ถูกต้อง; หากต้องใช้ทางถอยฉุกเฉิน ผู้ดูแลยังสั่ง `PM_API.setMode('sheets')` ได้ตามเดิม.
- แก้ผังตลาดที่อยู่ใน iframe ให้เรียกตัวเชื่อมต่อ Supabase โดยตรงและอ่านได้ทั้งผลลัพธ์แบบ array/`{data: array}`; เดิมผังอาจรับ `null` จากทางเชื่อมเดิมแม้หน้าหลักแสดงยอดผู้ค้าถูกต้อง จึงแสดงล็อคว่างทั้งหมด. ผลที่ตั้งใจ: วันอังคารยังว่างตามฐานข้อมูลจริง ส่วนวันพฤหัสฯ ต้องวาดผู้ค้าจริง 115 ล็อค.
- แก้สถานะผังตลาดทั้งสองให้โหลด snapshot เดียวจาก `getPublicStatus` ซึ่งรวมผู้ค้าประจำ สถานะค้างจ่าย ลา/ขาด และขายล็อคจร ลดปัญหาคำขอหลายชุดให้ผลไม่ตรงกัน และห้ามล้างสถานะที่เห็นอยู่เมื่อโหลดฐานข้อมูลล้มเหลว
- เพิ่ม “วันที่และเวลาของระบบ” ในเมนูหลัก **ตั้งค่าระบบ** ผู้ดูแลเลือกใช้เวลาอุปกรณ์หรือกำหนดเองได้ ค่าถูกเก็บใน `settings` และส่งให้ iframe ผังตลาดทันที เพื่อให้การคำนวณวันตลาดและรอบหมดอายุ 3 วันไม่ขึ้นกับนาฬิกาของอุปกรณ์เพียงอย่างเดียว
- เพิ่ม `tools/test_map_statuses.js` เป็น regression test บังคับให้ทั้งผังวันอังคารและวันพฤหัสบดีแสดง 4 สถานะหลัก (ค้างจ่าย/ลา/ขาด/จร) ครบก่อน deploy

**ผลตรวจใน Browser ก่อนบันทึกบัค (29 ก.ค. 2026):** เปิด build ล่าสุดของทั้งสองตลาดผ่านเว็บเซิร์ฟเวอร์ภายในเครื่องแล้ว หน้าเมนูแสดงชื่อ **ตั้งค่าระบบ** ตั้งแต่ก่อนล็อกอิน มีช่องวันที่ ช่องเวลา และตัวเลือกใช้เวลาที่ตั้งเองครบ วันที่หน้าหลักแสดง `29 ก.ค. 2569` ตรงกันทั้งสองตลาด หลังพบว่า build รอบแรกยังแสดงชื่อเมนูเดิม `ตั้งค่าค่าธรรมเนียม` ก่อนล็อกอิน ได้แก้ให้เปลี่ยนชื่อเมนูตั้งแต่เริ่มเปิดหน้าและตรวจซ้ำผ่านแล้ว นอกจากนี้ regression test ใน Browser จำลองข้อมูลยืนยันว่าผังทั้งสองแสดงค้างจ่าย/ลา/ขาด/จรครบทั้ง 4 สถานะ

**บัค URL หน้าแรกที่พบระหว่างตรวจ Browser:** URL รากของ GitHub Pages ทั้งสองตลาดตามตารางหัวข้อ 9.2 ตอบ `404 File not found` เพราะผล build เดิมไม่มี `index.html`; ผู้ใช้ต้องรู้ชื่อไฟล์ `pamai_app.html`/`pamai_thu_app.html` จึงจะเข้าได้ แก้โดยเพิ่ม `core/index.html` ให้ build สร้างหน้าเริ่มต้นและส่งต่อไปไฟล์แอปที่ถูกต้องของแต่ละตลาดอัตโนมัติ

**Deploy และตรวจเว็บจริงหลังแก้ (29 ก.ค. 2026):** อัปไฟล์ขึ้น branch `main` ผ่าน GitHub สำเร็จ — ตลาดวันอังคาร commit `9c97fba1c170b587dfc53b7195b4a391f4c82e51` และตลาดวันพฤหัสฯ commit `ce76185c1de9658176ac42c0b65ef116d7cf08c6` จากนั้นรอ GitHub Pages แล้วเปิดตรวจใน Browser ซ้ำ: URL รากทั้งสองส่งต่อเข้า `pamai_app.html`/`pamai_thu_app.html` ถูกต้อง, เมนูแสดง **ตั้งค่าระบบ**, วันที่หน้าหลักเป็น `29 ก.ค. 2569`, ช่องวันที่/เวลาปรากฏครบ และไฟล์ผังที่เผยแพร่มีระบบ snapshot `getPublicStatus`, นาฬิการะบบ และการคงข้อมูลล่าสุดเมื่อโหลดล้มเหลวครบทั้งสองตลาด (ผัง TUE 216 ล็อค · THU 298 ล็อค)
- แก้ซ้ำจากการทดสอบเว็บจริง: iframe ผังอาจไม่มี session token แม้หน้าหลักล็อกอินและอ่านผู้ค้าได้แล้ว และ migration เดิมมีโอกาสลบ token ร่วมของหน้าแม่. ตัวเชื่อมต่อจึงอ่าน token จากหน้าแม่เมื่อจำเป็น และอนุญาตให้ล้าง session เฉพาะหน้าหลักเท่านั้น; หากเกิดปัญหาอีก แถบเตือนผังจะแสดงข้อความจาก API แทนคำว่า “payload ไม่ถูกต้อง” อย่างเดียว.
- สำหรับ WebView/Safari ที่แยก `sessionStorage` ของ iframe แม้ origin เดียวกัน หน้าหลักจะส่ง session ให้ผังผ่าน `postMessage` ที่ล็อก `targetOrigin` เป็น origin เดียวกันเท่านั้น. ห้ามส่ง token ใน URL หรือเก็บลง localStorage; วิธีนี้ทำให้ผังเรียก Supabase ได้โดยไม่เปิดเผย token.
- หลัง iframe รับ session ต้องเรียกโหลดผู้ค้าใหม่ทันที เพราะการโหลดรอบแรกอาจเริ่มก่อนข้อความมาถึง; ไม่เช่นนั้น retry ทั้งสามครั้งจะจบด้วย “ต้องเข้าสู่ระบบก่อนใช้งาน” แม้ token มาถึงระหว่างนั้นแล้ว.
- **บั๊ก iframe-session (29 ก.ค. 2569):** หน้าหลักอ่านผู้ค้าได้ แต่ผังที่เป็น iframe ถูก RPC ปฏิเสธและแสดงล็อคว่างทั้งหมด. วิธีป้องกันที่ใช้ทั้งสองตลาดคือ (1) ไม่ล้าง session จาก iframe, (2) ส่ง session ด้วย `postMessage` จำกัด origin, (3) ให้ iframe ขอ session ซ้ำเองเมื่อเริ่มทำงาน และ (4) มี session bridge ของหน้าแม่เป็น fallback. ตรวจเว็บจริงแล้ว: วันพฤหัสฯ แสดงผู้ค้าจริง 115 ราย และวันอังคารโหลดผัง 216 ล็อคว่างตามฐานข้อมูลโดยไม่มี error.

**Hotfix สถานะผังและวันตลาดจากการทดสอบ Supabase จริง (29 ก.ค. 2026):**

- แก้วันตลาดพฤหัสฯ จากตรรกะ “พฤหัสฯ ที่ผ่านมา” เป็นรอบตลาดจริง: วันอาทิตย์–พุธเลือกพฤหัสฯ ที่กำลังจะถึง และวันพฤหัสฯ–เสาร์เลือกพฤหัสฯ ของสัปดาห์นั้น จึงเปลี่ยนวันที่บันทึกจาก `23 ก.ค. 2569` ที่ผิด เป็น `30 ก.ค. 2569` ที่ถูกต้อง. ฝั่งวันอังคารใช้หลักเดียวกันตามรอบศุกร์–พฤหัสฯ เพื่อให้ตรงกับหน้าคิวล็อคจร.
- หน้า `floating_queue` ใช้นาฬิกากลางจากเมนู **ตั้งค่าระบบ** แล้ว ทั้งวันที่ขายล็อคจร เวลา และวันตลาดถัดไป ไม่อ่านนาฬิกาเครื่องแยกเองอีก.
- ตัวเชื่อมต่อ iframe ของตลาดพฤหัสฯ เดิมฟังข้อความ `pm-session` แต่หน้าหลักส่ง `pmthu-session` ทำให้ token หลุดในบาง Browser; แก้ generator ให้กำหนดชื่อ session ตามตลาด และเมื่อรับ session ให้โหลดผู้ค้า สถานะลา/ขาด และข้อมูลคิวใหม่ครบทุกชุด.
- `getPublicStatus` ของ Supabase ส่งลา/ขาดเป็น `leaveStatus` และล็อคค้างชำระเป็น `unpaidLocks`; ผังเดิมยังอ่านชื่อฟิลด์เก่า จึงเพิ่มตัวแปลงรูปแบบให้ทั้งสองผัง.
- แก้ race condition ที่พบจากระบบจริง: ข้อมูลผู้ค้าแบบล็อกอินวาด `unpaid` ถูกต้องแล้ว แต่ snapshot สาธารณะที่ตั้งใจปกปิดสถานะค้างชำระวิ่งตามหลังและทับกลับเป็น `occupied`. หลังแก้ ข้อมูลผู้ค้าแบบล็อกอินเป็นแหล่งหลักและ snapshot สาธารณะไม่มีสิทธิ์ทับ.
- การบันทึกแจ้งลา/ขาดเปลี่ยนเป็นรอผล `status === 'ok'` จาก Supabase ก่อนวาด badge และแจ้งสำเร็จ; ถ้าฐานข้อมูลปฏิเสธ หน้าจอไม่แสดงสถานะสำเร็จปลอม.

**ผลตรวจ Browser บน GitHub Pages + Supabase จริง:** ล็อกอินแอปตลาดพฤหัสฯ แล้วผังแสดงรอบ `30 ก.ค. 2569`; จำลอง A1 เป็นแจ้งลาแล้วได้คลาส `is-leave`/badge `🟣ลา`, เปลี่ยนเป็นขาดล็อคแล้วได้ `is-absent`/badge `🔴ขาด`, เปลี่ยนผู้ค้า A1 เป็นค้างชำระแล้วได้คลาส `unpaid`, และล็อคจร A4 เดิมของรอบนี้แสดง `is-daily`/badge `🏷️จร`. หลังทดสอบได้กดยกเลิกลา/ขาดซึ่ง backend ลบแถว `leave_log` ของวันนั้นจริง และคืน A1 จาก `unpaid` เป็น `active`; ตรวจโหลดใหม่จาก Supabase แล้วเหลือ 0 ลา, 0 ขาด, 0 ค้างชำระ และ 1 ล็อคจรเดิมของระบบ. เวลาหน้าหลักเป็นเขต `Asia/Bangkok` และวันที่ `29 ก.ค. 2569` ตรงกับวันตรวจ.

**Deploy hotfix:** ตลาดวันพฤหัสฯ commit `1a0a7885264295d557b0cb2e73574e7dcda33979` แล้วตามด้วย commit แก้ race condition `bdfabef7ce3c3e955bc665b95ada9fe160b632cb`; ตลาดวันอังคาร commit `c4be18f3edb2ed03dd2b646ee4a4eca791732374` แล้วตามด้วย commit `d83bb5161bfa48b46a2ee48eda07eb051f4a372b`. GitHub Pages workflow ของตลาดวันพฤหัสฯ run #58 ผ่านสำเร็จก่อนตรวจ Browser รอบสุดท้าย.

### หมายเหตุ Migration สำหรับผู้ใช้ฝั่งวันพฤหัสฯ (ครั้งแรกหลัง deploy v2.0)

1. คีย์เก็บ Web App URL เปลี่ยนจาก `pm-api-url` → `pm-thu-api-url` — **ระบบย้ายค่าให้อัตโนมัติ**: แอพจะอ่านค่าจากคีย์เก่าเป็น fallback เอง เครื่องที่เคยตั้งค่าไว้แล้วใช้งานต่อได้ทันทีโดยไม่ต้องทำอะไร (กลไกนี้ฝังใน build.py ข้อ 5.1 เฉพาะฝั่งวันพฤหัสฯ — ฝั่งวันอังคารไม่มี fallback นี้เพื่อกันหยิบ URL ผิดตลาด)
2. session เดิม (`pmthu-t` ยังใช้ได้ / `pm-t` เดิมจะไม่ถูกอ่าน) — ผู้ใช้อาจต้อง login ใหม่หนึ่งครั้ง
3. ฐานข้อมูล Google Sheets **ไม่ต้องย้ายอะไรทั้งสิ้น** — ใช้ชีทเดิมทุกอย่าง (backend เพิ่มชีทใหม่ให้เองอัตโนมัติเมื่อถูกเรียกครั้งแรก เช่น `installment_plans`, `floating_queue`, `market_rules`)

### ข้อจำกัดที่ทราบ

- `pamai_market_map.html` ยังเป็นไฟล์แยกต่อตลาด — ฟีเจอร์ใหม่บนหน้าแผนที่ต้องแก้สองไฟล์ (แลกกับความถูกต้องของผังตลาดที่ต่างกันทั้งฉบับ)
- รายงาน 04 ฝั่งวันพฤหัสฯ ถูกยกระดับเป็นโครงเดียวกับวันอังคาร (ของเดิมเป็นเวอร์ชันย่อ) — ตรวจผลรายงานหลัง deploy ครั้งแรก
- โปรเจกต์ GAS อาจมี deployment เก่าที่ไม่มีใครใช้แล้วค้างอยู่ในรายการ "จัดการการทำให้ใช้งานได้" (เช่นที่พบในโปรเจกต์วันพฤหัสฯ มี deployment ที่ไม่มีชื่อหลายตัว) — ไม่จำเป็นต้องอัปเดตทุกตัว อัปเดตเฉพาะตัวที่พิสูจน์แล้วว่า frontend เรียกใช้จริง (ดูหัวข้อ 9.3)

### สิ่งที่แก้ไขในเวอร์ชัน 2.3 (26 ก.ค. 2026)

รอบนี้ผู้ดูแลตลาดแจ้งมา 25 รายการในคราวเดียว ครอบคลุมทั้งบั๊ก ฟีเจอร์ใหม่ และงาน UI/UX
สรุปตามต้นเหตุ (root cause) ไม่ใช่ตามอาการ เพื่อให้คนที่มาแก้ต่อเข้าใจว่าทำไมต้องแก้ที่จุดนั้น:

**ก. ต้นเหตุร่วมของ "รายงานดูข้อมูลไม่ได้" ทั้ง 3 ฉบับ (บั๊ก #28) — ⚠️ ลึกกว่าที่คิดหนึ่งชั้น**

> **บทเรียนสำคัญของรอบนี้:** ตอนแรกวิเคราะห์จากการอ่านโค้ดว่าเป็นเรื่อง "รูปแบบวันที่ไม่ตรงกันระหว่างโมดูล"
> แล้วแก้ไปแล้ว — แต่พอทดสอบบนเว็บไซต์จริงที่ deploy แล้ว (ตามกฎหลักข้อ 5 ของคู่มือวันอังคาร)
> กลับพบว่า **ยังหาข้อมูลไม่เจออยู่ดี** เพราะต้นเหตุจริงลึกกว่านั้น ถ้าเชื่อการอ่านโค้ดอย่างเดียว
> ระบบจะ deploy ออกไปทั้งที่รายงานยังพังเหมือนเดิม

**สิ่งที่พบจากข้อมูลจริงในชีต (27 ก.ค. 2026):** ค่าในคอลัมน์ `date` ไม่ได้เป็นข้อความไทยอย่างที่โค้ดทุกไฟล์
สมมติไว้ แต่เป็น **Date object ที่มีปีเป็น พ.ศ.** — อ่านผ่าน JSON ออกมาเป็น `"2569-07-06T07:00:00.000Z"`
สาเหตุคือสเปรดชีตตั้ง locale เป็นไทย Google Sheets จึง *แปลง* ข้อความ `"6 ก.ค. 2569"` ที่เราเขียนลงไป
ให้กลายเป็นค่าวันที่จริง โดยตีความ `2569` เป็นปี ค.ศ. ตรง ๆ

ผลคือการกรองด้วย `r.date === date` (เทียบสตริงตรง ๆ) **ไม่เคยตรงเลยแม้แต่ครั้งเดียว** ไม่ว่าหน้าเว็บจะส่ง
วันที่ไทยหรือ ISO มาก็ตาม → รายงานที่กรองตามวันที่จึงว่างเปล่ามาตลอด ไม่ใช่แค่บางกรณี
(ยืนยันด้วยการยิง API จริง: ชีต `payments` มี 18 แถว แต่ค้นด้วย `"21 ก.ค. 2569"` ได้ 0 แถว
และค้นด้วย `"2026-07-21"` ก็ได้ 0 แถว)

`_normDate()` จึงต้อง **ลบ 543 ออกจากปีที่มากกว่า 2400 ทุกกรณี** รวมทั้ง Date object และสตริง ISO
ไม่ใช่เฉพาะข้อความวันที่ไทย — และ `cancelDailyBooking()` ที่เทียบวันที่ตรง ๆ ก็ต้องใช้ `_normDate` ด้วย
(เดิม "ยกเลิกผู้ค้าจร" จึงไม่มีผลจริงในฐานข้อมูลด้วยเหตุผลเดียวกัน)

**ประเด็นรอง (ยังจริงอยู่):** `pamai_installment.html` บันทึกวันที่เป็น ISO ปี ค.ศ. ต่างจากโมดูลอื่น —
`_normDate` รองรับทั้งสองแบบพร้อมกันแล้ว จึงไม่ต้องไล่แก้ทุกโมดูลให้เขียนเหมือนกัน
ซ้ำร้ายค่าเริ่มต้นของรายงานคือ "วันนี้" ทั้งที่ตลาดเปิดสัปดาห์ละวัน — 6 ใน 7 วันรายงานจึงว่างเปล่าเสมอ
โดยผู้ใช้ไม่มีทางแยกได้ว่า "ไม่มีข้อมูล" หรือ "ระบบพัง"
→ แก้ที่ backend: เพิ่ม `_normDate()` / `_matchDate()` normalize ทุกค่าเป็น `YYYY-MM-DD` ก่อนเทียบเสมอ
  (backward compatible — หน้าเว็บเดิมที่ส่งวันที่ไทยมายังใช้ได้) และเพิ่มพารามิเตอร์ `from`/`to`/`month`/`zone`
→ แก้ที่ frontend: แถบตัวกรองกลาง `PM_REPORT_FILTER` ทั้ง 8 ฉบับ + ปุ่มลัด "วันตลาดล่าสุด"

**ข. สถานะลา/ขาด/จร เห็นแต่ในมือถือ ไม่เห็นในเบราว์เซอร์ (บั๊ก #31)**
สถานะรายวันเก็บใน `localStorage` ของเครื่องที่กดบันทึก (กลไกวงจร 3 วัน) เครื่องอื่นจึงไม่เห็น —
ฝั่งวันพฤหัสฯ **ไม่มีฟังก์ชันดึงสถานะกลับจาก Sheets เลย** ส่วนฝั่งวันอังคารมีแต่ค้นด้วย `nowTH()`
(= วันนี้) ซึ่งไม่ใช่วันตลาด จึงไม่เจอข้อมูล → เพิ่ม/แก้ `loadFromSheets()` ให้อ้างอิง **วันตลาดล่าสุด**
(`marketDayTH()`) ทั้งสองไฟล์ และเปลี่ยน `logLeave` ให้บันทึกด้วยวันตลาดแทนวันที่กดปุ่ม

**ค. เลข 0 หน้าเบอร์โทรหายหลังกดบันทึก (บั๊ก #29)**
ไม่ใช่บั๊กของหน้าเว็บ (ช่องกรอกเป็น `type=tel` เก็บสตริงถูกต้องแล้ว) — Google Sheets แปลงสตริง
"0812345678" เป็น *ตัวเลข* อัตโนมัติ เลข 0 นำหน้าจึงถูกตัด → แก้ 2 ชั้นที่ backend:
บังคับ number format ของคอลัมน์เบอร์โทรเป็นข้อความ (`_forceTextCol`) ก่อนเขียนทุกครั้ง
และ `_normPhone()` เติม 0 คืนตอนอ่าน เพื่อกู้ข้อมูลเก่าที่เสียไปแล้วโดยไม่ต้องแก้มือในชีต

**ง. ประวัติการลา/ขาดซ้ำซ้อน + ปลดสถานะแล้วประวัติไม่หาย**
เปลี่ยนกติกาเป็น **1 ล็อค + 1 วัน = 1 รายการ** ที่ `logLeave`: แจ้งซ้ำวันเดิม = เขียนทับแถวเดิม,
ปลดสถานะ = **ลบแถวของวันนั้นออกจริง** (ไม่ใช่เพิ่มแถว `cancel` ทับ) และเพิ่ม action `clearLeaveForDate`
รวมทั้งรองรับค่า `leave_cancel`/`absent_cancel` ของแอพวันพฤหัสฯ เดิมที่เคยทำให้สถานะค้าง

**จ. บันทึกกิจกรรมไม่ครบ (บั๊ก #30)**
เดิม `ACT_LOG` เป็นอาเรย์ในหน่วยความจำ — หายทุกครั้งที่รีเฟรช, ไม่เห็นของผู้จัดคนอื่น, และโมดูลใน
`<iframe>` เรียก `logAct()` ของ shell ไม่ได้ → ย้ายไปบันทึกที่ **"ประตูเดียว" คือ `doPost` ของ backend**
(`_autoLog`) ทุก action ที่เปลี่ยนข้อมูลถูกบันทึกอัตโนมัติไม่ว่าหน้าไหนเรียกมา จึงครบถ้วนถาวรและ
ไม่พลาดเมื่อเพิ่มหน้าใหม่ในอนาคต + ลบรายการเก่ากว่า 30 วันอัตโนมัติ (`ACTIVITY_RETENTION_DAYS`)

**จ.2 เวลาในบันทึกกิจกรรม/รายงานเพี้ยนเป็น "1899-12-30" (บั๊ก #32 — พบตอนตรวจหลัง deploy)**
บั๊กตระกูลเดียวกับ #28/#29 อีกตัวหนึ่ง: Sheets แปลงข้อความ `"10:15"` เป็น **ค่าเวลา** ซึ่งเก็บภายในเป็น
วันที่ 30 ธ.ค. 1899 + เวลา `getValues()` จึงคืน Date object และ `JSON.stringify` ส่งออกเป็น
`"1899-12-30T03:15:56.000Z"` — กระทบคอลัมน์ `time` ของชีต `activity_log`, `leave_log`,
`daily_bookings`, `payments`
→ แก้: อ่านคอลัมน์ `time` จาก **`getDisplayValues()`** (ค่าที่ตาเห็นในชีต ไม่ผ่านการแปลงชนิด) ผ่านตัวช่วย
`_rowsWithTime()` + `_normTime()` และบังคับ number format คอลัมน์เป็นข้อความใน `initSheets()`
⚠️ **ห้ามใช้ `Utilities.formatDate` กับ Date ปี 1899** เพื่อกู้เวลา — โซนเวลาไทยยุคนั้นเป็น LMT (+6:42)
ผลลัพธ์จะคลาดเคลื่อนเป็นหลักนาที ต้องใช้ display value เท่านั้น

**จ.3 แถวซ้ำที่ยังค้างจากข้อมูลเก่า (พบตอนตรวจหลัง deploy)**
กติกา "1 ล็อค + 1 วัน = 1 ประวัติ" บังคับที่ `logLeave` ตั้งแต่ v2.3 แล้ว แต่ **แถวที่เกิดก่อนหน้านั้น
ยังซ้ำอยู่ในชีต** (พบล็อค H12 วันเดียวกัน 4 แถว) เช่นเดียวกับ `daily_bookings` ที่มีหลายแถวต่อล็อค
จากการบันทึกซ้ำ/แก้ไข (พบล็อค A4 ซ้ำ 3 แถว) ทำให้หน้าสาธารณะแสดงล็อคเดียวกันหลายรายการ
→ แก้แบบไม่ทำลายข้อมูล: **กรองซ้ำตอนอ่าน** (เก็บรายการล่าสุด) ทั้งใน `getLeaveLog` และ
`getPublicStatus.sold` — ไม่ลบแถวเก่าทิ้ง เพราะกฎเหล็กข้อ 6 ห้ามลบข้อมูลถาวรโดยไม่ได้รับอนุญาต

**จ.4 ไอคอน SVG ระเบิดเต็มจอในรายงาน 08 (พบตอนตรวจหน้าจอจริงหลัง deploy)**
รายงาน 08 ถูกเขียนใหม่ทั้งฉบับ (เดิมเป็นรายงานค่าไฟซ้ำ) แต่ **ลืมยก CSS `.ic` มาด้วย** —
`<svg class="ic" viewBox="0 0 24 24">` ที่ไม่มีกฎ `width:1em;height:1em` จะยืดเต็มพื้นที่ของ
element แม่ ไอคอนจึงกลายเป็นรูปทึบสีดำสูง 400–950px
→ แก้: เพิ่มกฎ `.ic{...}` ชุดเดียวกับรายงานฉบับอื่น และ **ไล่สแกนทั้ง 20 ไฟล์** ว่าไฟล์ใดมี
`class="ic"` แต่ไม่มีกฎ `.ic` (ผลลัพธ์: ไม่มีไฟล์อื่นขาด)
📌 บทเรียน: ด่านตรวจอัตโนมัติ (ไวยากรณ์ JS + responsive + pageerror) **จับบั๊ก CSS ล้วนไม่ได้**
ต้องเปิดหน้าจอจริงดูด้วยตาอย่างน้อยหนึ่งรอบต่อหน้าที่เขียนใหม่

**จ.5 รายงาน 07 ยังไม่มีตัวกรองช่วงเวลา + ป้ายผลลัพธ์ติดลบ**
รายงาน 07 ผูก `date:false` ไว้ (โซนอย่างเดียว) จึงยึด "เดือนปัจจุบัน" เสมอ ขัดกับข้อกำหนดที่ว่า
**ทุกรายงานต้องมีทุกโซน + ตัวกรองโซน แล้วตามด้วยช่วงวันที่/เดือน-ปี** และ `thisMonthTuesdays()`
ก็นับวันตลาดจากเดือนปัจจุบันเสมอ (ดูเดือนย้อนหลังแล้วยอด "ควรเก็บ" จะผิด)
นอกจากนี้เมื่อรับชำระมากกว่ายอดที่ควรเก็บ ป้ายยังเขียนว่า "ยังเก็บไม่ครบ ฿-200" ซึ่งอ่านแล้วสับสน
→ แก้ทั้งสามจุด: เปิด `date:true`, ให้ `thisMonthTuesdays()` อ่านเดือนจาก `PM_FILTER.monthKey()`,
และสลับป้ายเป็น "รับชำระเกินยอดที่ควรเก็บ" พร้อมแสดงค่าสัมบูรณ์เมื่อผลต่างติดลบ
หมายเหตุ: รายงาน 01/03/04 ยังคง `date:false` โดยเจตนา — อ่านจาก `getVendors` ซึ่งเป็นข้อมูล
สถานะปัจจุบัน ไม่มีคอลัมน์วันที่ให้กรอง การใส่ตัวกรองช่วงเวลาจึงไม่มีความหมาย

**ฉ. ฟีเจอร์ใหม่: ส่วนลดค่าเช่า + ค่าธรรมเนียมรายปีเดือนมีนาคม** — ดูกฎธุรกิจเต็มในหัวข้อ 13
ชีตใหม่ `rent_discounts` + `settings`, action ใหม่ `getDiscounts`/`saveDiscount`/`deleteDiscount`/
`getSettings`/`saveSettings`, UI ที่หน้ารับชำระหลายล็อค, และรายงานส่วนลดฉบับใหม่

**ช. รายงานค่าไฟซ้ำกัน 2 ฉบับ** — รายงาน 07 และ 08 มีชื่อเกือบเหมือนกัน (07 = ประมาณการจากอุปกรณ์
ที่บันทึกไว้, 08 = ยอดรับชำระจริง) → รวมเป็นฉบับเดียวที่ **รายงาน 07** แสดง 3 คอลัมน์เทียบกัน
(ควรเก็บ / รับชำระจริง / คงเหลือ) แล้วนำ **สล็อตรายงาน 08 มาใช้เป็นรายงานส่วนลดค่าเช่า** —
จึงได้รายงานใหม่ที่ผู้ใช้ขอโดยไม่ต้องเพิ่มไฟล์ core และไม่ต้องแก้ `FILE_NAMES` ใน build.py

**ซ. ระบบสีและผังตลาด**
- สีสถานะทั้ง 6 แบบผ่านการวัดด้วย **CIEDE2000** ΔE ต่ำสุด 13.0 (light) / 10.5 (dark) — ทุกคู่แยกออกได้
  ชัดด้วยตา, contrast ตัวอักษร/พื้น 7.9–11.8:1 (เกิน WCAG AA ทุกคู่) — **ห้ามใช้ค่า contrast ratio
  ตัดสินว่าสองสถานะแยกกันได้** เพราะมันวัดแค่ความสว่าง ต้องใช้ ΔE2000 เท่านั้น
- เลขล็อคใหญ่ขึ้นเท่าตัวและ **เท่ากันทั้งสองแอพ** (เดิม TUE .81rem / THU .58rem → 1.6rem ทั้งคู่)
- แอพวันพฤหัสฯ เป็นเฉดส้มล้วน: ไล่เก็บสีเขียวที่ hardcode ค้างอยู่ **26 เฉด** เพิ่มเข้าตาราง `COLORS`
  ของ build.py (เดิมมีแค่ 10 คู่ = สีธีมหลัก) โดยแปลงด้วยการย้าย hue ไปโซนส้มและ **คงค่า Lightness เดิม**
  จึงรับประกันว่า contrast ทุกคู่ของแอพวันพฤหัสฯ เท่ากับของวันอังคารเป๊ะ ไม่มีจุดใดอ่านยากขึ้น
- ฝั่งวันอังคาร: ยกเลิกการเช่าแล้วผังแสดงเป็น "ล็อคว่าง" ทันที (เดิมเป็น `cancelled` สีแดง) และเปลี่ยนคำว่า
  "ไม่มีข้อมูล" เป็น "ล็อคว่าง" ทุกจุด

**ฌ. Mobile-First ทุกหน้า** — `pamai_batch_payment.html` เดิม **ไม่มี `@media` แม้แต่บรรทัดเดียว**
(layout เป็น flex 2 คอลัมน์ + `height:100vh` + `overflow:hidden` ฝั่งขวาจึงหลุดออกนอกจอและเลื่อนดูไม่ได้)
หน้าสถานะสาธารณะและศูนย์รวมรายงานก็ไม่มีเช่นกัน ส่วนรายงานมีแต่ `@media print`
→ ฝัง `PM_MOBILE_KIT` ทุกไฟล์: กัน horizontal overflow, ตารางเลื่อนแนวนอนได้เอง, ฟอนต์ไทยตัดคำถูกต้อง,
พื้นที่กด ≥40px, ช่องกรอก 16px กัน iOS ซูมเอง, safe area ขอบล่าง — **ทดสอบจริงด้วย Chromium ผ่าน
76/76 (19 ไฟล์ × 4 ขนาดจอ) ทั้งสองตลาด**

**ญ. บั๊กที่ค้นพบเองระหว่างทดสอบ (ผู้ใช้ไม่ได้แจ้ง)**
`pamai_thu_vendor_registration.html` พังทั้งหน้าด้วย `TypeError: Cannot read properties of undefined
(reading 'data')` เพราะโค้ด hardcode โซนเริ่มต้นเป็น `'SA'` ซึ่งเป็นโซนของตลาดวันอังคารเท่านั้น
ฝั่งวันพฤหัสฯ ไม่มีโซน SA → `LOCKS['SA'].data` เป็น undefined **หน้าลงทะเบียนผู้ค้าใหม่ของตลาดวันพฤหัสฯ
ใช้งานไม่ได้เลย** แก้เป็น `Object.keys(LOCKS)[0]` (โซนแรกที่มีจริงในผังของตลาดนั้น) พร้อม guard
— บั๊กนี้ตรวจพบเพราะ `tools/test_responsive.js` อ่าน `pageerror` ของเบราว์เซอร์จริง ไม่ใช่จากการอ่านโค้ด

**ฎ. หน้าสถานะสาธารณะ (สำหรับกลุ่มไลน์ผู้ค้า)** — เพิ่มโลโก้ตลาด, ปุ่มรีเฟรชขนาดใหญ่พร้อมหมายเหตุ
"ผู้ค้าโปรดกดรีเฟรชหน้านี้ทุกครั้ง...", แยกช่องล็อคว่างเป็น 2 ประเภท (ไม่มีผู้ค้าประจำ / ลา-ขาดล็อค)
แสดงเฉพาะเลขล็อคเป็นชิปใหญ่, และแสดงล็อคจรที่จำหน่ายแล้วทั้ง **2 ช่องทาง** (ผ่านคิว / ผู้จัดขายตรง)
โดยเพิ่ม `sold` จาก `daily_bookings` เข้าใน `getPublicStatus` (whitelist เฉพาะเลขล็อค/ชื่อร้าน/สินค้า —
**ห้ามหลุดเบอร์โทร/ยอดเงิน/ช่องทางชำระ**) และลดเวลาแคชจาก 20 วิ เป็น 10 วิ ให้ใกล้เรียลไทม์ขึ้น
พร้อมแก้ `build.py` ให้คัดลอก `logo.png` ลง `dist/` ทุกครั้ง (เดิม build ลบทิ้งแล้วไม่คัดลอกกลับ)

**หมายเหตุ Deploy รอบนี้ (สำคัญ):** `core/pamai_apps_script.js` **มีการแก้ไขจำนวนมาก** — ต้อง deploy
ขึ้น Google Apps Script ของ **ทั้งสองตลาด** ตามขั้นตอนหัวข้อ 9 มิฉะนั้นฟีเจอร์ส่วนลด/รายงาน/บันทึกกิจกรรม
/การกู้เลข 0 เบอร์โทร จะไม่ทำงาน (หน้าเว็บจะได้ `Unknown action`) และหลัง deploy ให้เปิด
`?action=initSheets` หนึ่งครั้งต่อตลาด เพื่อสร้างชีต `rent_discounts` + `settings` และตั้ง number format
ของคอลัมน์เบอร์โทรเป็นข้อความ

---

## 11. ตารางเวอร์ชันเอกสาร

| เวอร์ชัน | วันที่ | สาระสำคัญ |
|---|---|---|
| Tuesday v1.3–v1.4 | มิ.ย.–ก.ค. 2026 | คู่มือแยกของตลาดวันอังคาร (อ้างอิง flow รายวัน) |
| Thursday v1.1 | ก.ค. 2026 | คู่มือแยกของแพไม้มินิวันพฤหัสฯ (อ้างอิง flow รายวัน) |
| Unified v2.0 | 17 ก.ค. 2026 | รวมสองตลาดเป็นระบบ Core + Config + Build เล่มนี้เป็นเอกสารหลักสำหรับการแก้ไขระบบ |
| Unified v2.1 | 21 ก.ค. 2026 | เพิ่มระบบคิวจองล็อคจร, กฎระเบียบตลาด, หน้าสถานะสาธารณะ (3 ไฟล์ core ใหม่ + 2 MARKET_BLOCK ใหม่) — เขียนหัวข้อ 9 (Deploy) ใหม่ทั้งหมดให้ตรงกับขั้นตอนจริง (backend ผ่าน GAS หลาย deployment ต่อโปรเจกต์ได้ + frontend ผ่าน GitHub Pages) และเพิ่มขั้นตอนตรวจสอบหลัง deploy (หัวข้อ 9.3) หลังพบเคสจริงที่ deployment ไม่ตรงกับที่ frontend ใช้ |
| Unified v2.2 | 23 ก.ค. 2026 | ยืนยันความปลอดภัยของระบบรีเซ็ต 3 วัน (ไม่กระทบข้อมูลผู้ค้าประจำ) + แก้บั๊กยกเลิกการเช่าไม่ persist จริงทั้งสองตลาด (payload ผิดรูปแบบ/ขาดการเรียก backend/ค่าสถานะไม่ตรงมาตรฐาน) + เพิ่มระบบรีเฟรชข้อมูลข้ามแท็บอัตโนมัติผ่าน postMessage (ไม่ต้องกด F5) + แก้บั๊กหน้าล็อกอินอ่านไม่ออกในธีมมืด (CSS focus + autofill) — deploy ขึ้น GitHub ทั้งสอง repo ผ่านหน้าเว็บ Add files via upload สำเร็จแล้ว |
| **Unified v2.3** | **26 ก.ค. 2026** | **รอบแก้ไขใหญ่ 25 รายการจากผู้ดูแลตลาด (ดูรายละเอียดเต็มในหัวข้อ 10):** แก้ต้นเหตุร่วมของรายงานที่ดูข้อมูลไม่ได้ (normalize วันที่ที่ backend + แถบตัวกรองกลางทุกรายงาน: โซน/วันเดียว/ช่วงวันที่/เดือน-ปี) · สถานะลา-ขาด-จร ซิงก์ข้ามอุปกรณ์ได้จริงโดยอ้างอิงวันตลาดล่าสุด · กู้เลข 0 หน้าเบอร์โทร · ประวัติลา-ขาด 1 ล็อค 1 วัน 1 รายการ และปลดสถานะแล้วประวัติหายจริง · บันทึกกิจกรรมครบทุกกิจกรรมที่ backend + ลบอัตโนมัติเมื่อเกิน 30 วัน · **ฟีเจอร์ใหม่: ส่วนลดค่าเช่ารายเดือน (รายล็อค/ทั้งตลาด) + ค่าธรรมเนียมรายปีบวกอัตโนมัติในบิลเดือนมีนาคม + รายงานส่วนลดค่าเช่า** · รวมรายงานค่าไฟ 2 ฉบับที่ซ้ำกันเป็นฉบับเดียว 3 คอลัมน์ · ระบบสีสถานะล็อคชุดเดียวกันทั้งสองตลาด (ΔE2000 ≥10.5 ทุกคู่) + เลขล็อคใหญ่ขึ้นเท่าตัวเท่ากันทั้งสองแอพ + แอพวันพฤหัสฯ เป็นส้มล้วน (ไล่เก็บเขียว 26 เฉด) · Mobile-First ทุกหน้า ผ่านการทดสอบจริง 76/76 ทั้งสองตลาด · หน้าสถานะสาธารณะยกระดับ (โลโก้ + ปุ่มรีเฟรชเด่น + แยกล็อคว่าง 2 ประเภท + แสดงล็อคจรที่ขายตรงไม่ผ่านคิว) · **พบและแก้บั๊กที่ผู้ใช้ไม่ได้แจ้ง: หน้าลงทะเบียนผู้ค้าใหม่ของตลาดวันพฤหัสฯ พังทั้งหน้าเพราะ hardcode โซน 'SA' ของตลาดวันอังคาร** · เพิ่มเครื่องมือทดสอบ/สร้างโค้ดใน `tools/` 5 ตัว (หัวข้อ 12)** |

| **Unified v2.4** | **27 ก.ค. 2026** | **รอบแก้บั๊กที่ผู้ใช้เจอหน้างาน + งานตั้งค่าค่าธรรมเนียมกลาง (ดูหัวข้อ 14):** แก้บั๊ก **"เปิดเมนูแล้วจอดำ" บนมือถือ** ซึ่งทำให้ใช้งานไม่ได้ 9 เมนูจาก 12 (ต้นเหตุ: คลาส `.layout` ของ shell ชนกับกฎใน mobile kit) · แก้ **ช่องเลือกเดือนที่ hardcode พ.ศ. 2568** ทั้งหน้ารับชำระและหน้าค่าไฟฟ้า พร้อมผูกสองทางกับวันที่รับเงินและให้ตัวคูณจำนวนวันตลาดอิงเดือนรอบบิลจริง · แก้ **Dashboard ยังนับล็อคที่ยกเลิกการเช่าไปแล้ว** · **ฟีเจอร์ใหม่: หน้า "ตั้งค่าค่าธรรมเนียม" ในเมนูหลักหมวดระบบ** (แรกเข้า + รายปี อัตรากลางทั้งตลาด สิทธิ์ admin) และหน้าลงทะเบียนผู้ค้าใหม่ดึงไปใช้อัตโนมัติแบบล็อคค่าไว้ · **โหมดมืดครบทุกหน้าแล้ว** (เพิ่มให้รายงาน 01–07 และหน้ารับชำระที่เดิมไม่มีเลย) ผ่านสคริปต์ใหม่ `tools/inject_theme_kit.py` · แก้แถบแท็บ/แถบขั้นตอนที่ถูกบีบจนตัวอักษรไทยทับกันบนจอ 390px · เพิ่มการตรวจความสมบูรณ์ของ CSS เข้าไปใน `tools/check_js.py` |

| **Unified v2.5** | **28 ก.ค. 2026** | **รอบแก้บั๊กหน้างาน 4 เรื่อง + ฟีเจอร์ย้ายล็อคจร + ปิดช่องโหว่ความปลอดภัย (ดูหัวข้อ 15):** แก้ **`<div class="toolbar">` ที่ไม่เคยถูกปิดแท็กทั้งสองตลาด** ทำให้ผังตลาดกลายเป็นลูกของ toolbar และแถบสถานะลอยไปอยู่กลางจอในโซนที่ผังแคบ · แก้ **ผังแสดงล็อคผู้ค้าประจำเป็น "ว่าง" ทั้งตลาดเมื่อโหลดข้อมูลไม่สำเร็จ** ด้วยชุด `PM_VENDOR_SYNC` (แถบเตือน + retry + timeout + แคชในเครื่อง + สถานะ "ยังไม่ทราบ" ที่ดูไม่เหมือนล็อคว่าง) · แก้ **ผังล้นกรอบและเลื่อนไปดูไม่ได้บนมือถือ** ด้วย `PM_MAPFIT` · **ฟีเจอร์ใหม่: ย้ายล็อคจร** (ปุ่มแยกจากย้ายผู้ค้าประจำ แสดงเฉพาะล็อคที่ขายจรแล้ว) พร้อม action ใหม่ `moveDailyBooking` / `moveVendorLock` ที่ backend — **และซ่อมต้นเหตุว่า `confirmMove()` เดิมไม่เคยเรียก backend เลยทั้งสองตลาด** · **ลบรหัสผ่าน plain text ของทีมงาน 4 บัญชีออกจาก `pamai_apps_script.js`** (ตกค้างจากบั๊ก #20) และลบข้อมูลผู้ค้าจริงที่ hardcode ในผังฝั่ง TUE 34 จุด · เพิ่มด่านตรวจใหม่ 3 ตัว (โครงสร้าง HTML ใน `check_js.py`, `test_map_reach.js`, `test_offline_map.js`) · เพิ่มกฎเหล็กข้อ 15–17 · แนบ **แผนย้ายฐานข้อมูลไป Supabase v3.0** (`Supabase-Migration-Plan-v3.0.md`) |
| **Unified v2.5 + ฐานข้อมูล v3.0** | **28 ก.ค. 2026 (รอบบ่าย)** | **ย้ายฐานข้อมูลไป Supabase — ดูรายละเอียดเต็มในหัวข้อ 16:** สร้างโปรเจกต์แยกตลาดละ 1 ตัว (ทางเลือก A ตามที่ผู้ดูแลตลาดเลือก) · schema 11 ตาราง + RLS ถอนสิทธิ์ทุกตารางจาก anon เหลือทางเข้าเดียวคือฟังก์ชัน `api()` 33 action · ย้ายข้อมูลจริงฝั่งวันพฤหัสฯ ครบทุกตาราง โดยไม่ใช้ service_role key เลยสักครั้ง · `tools/inject_api_client.py` ทำ 3 โหมด (`sheets`/`parallel`/`supabase`) สลับได้โดยไม่ต้อง deploy ใหม่ **ค่าเริ่มต้นยังเป็น `sheets`** · **ทดสอบ end-to-endหน้าเว็บจริงในเบราว์เซอร์จริงแล้วเจอบั๊กเพิ่ม 6 เรื่องที่ระดับ SQL มองไม่เห็น** (ผังว่างทั้งตลาด · ล็อกอินตกไปใช้บัญชีสาธิตในไฟล์ · ฟีเจอร์ถูกปิดเงียบ 89 จุด · สคริปต์ patch แก้แล้วไม่มีผล · stored XSS จากข้อมูลเก่าที่มีแท็ก HTML · หน้าสาธารณะบอกว่าล็อคไหนค้างชำระ) แก้ครบทั้งหมด · เพิ่มชุดทดสอบใหม่ 2 ตัว (`test_e2e_supabase.js`, `test_sheets_guard.js`) · เพิ่มกฎเหล็กข้อ 18–21 และหัวข้อ 15.7 · เขียนไฟล์ cron 2 ตัวใน `.github/workflows/` (ของเดิมในแผน v3.0 ใช้ไม่ได้จริงทั้งคู่) |
| **Unified v2.5 + แก้ล็อคจร** | **30 ก.ค. 2026** | แก้ยกเลิกล็อคจรจากผังทั้งสองตลาด โดยใช้วันตลาดจากระบบเมื่อ API สาธารณะไม่ส่งวันที่ · แสดงปุ่ม **ย้ายล็อคจร/ยกเลิกจรเฉพาะล็อคที่ขายแล้ว** · ปิดรายการใน `daily_bookings` และ `floating_queue` พร้อมกัน · เพิ่ม regression test จำลองขาย/ตรวจปุ่ม/ยกเลิก/ตรวจวันที่ |
| **Unified v2.5 + หน้าสถานะสาธารณะ** | **30 ก.ค. 2026** | แก้หน้าสาธารณะอ่าน Google Sheets ทั้งที่แอพหลักใช้ Supabase ทำให้คิว/ขายจร/ลา-ขาด/ค้างชำระไม่ตรงกัน · จัดลำดับ 5 หมวดใหม่ · ส่งและแสดงราคาขายจรถูกต้อง · เพิ่มหมายเหตุรักษาสิทธิผู้ค้าประจำ · เพิ่มชุดทดสอบ `test_public_status.js` |
| **Unified v2.6 + ฐานข้อมูล v3.1** | **3 ส.ค. 2026** | **รอบแก้ 5 เรื่องจากผู้ดูแลตลาด + บั๊กร้ายแรงที่ผู้ใช้ไม่ได้แจ้ง (ดูหัวข้อ 17):** พบว่า **`pamai_leave_history.html` และรายงาน 01–09 รวม 10 ไฟล์ เชื่อมต่อ Supabase ไม่ได้เลยตั้งแต่วันตัดจริง** จาก 2 สาเหตุซ้อนกัน — บล็อค `PM_API_CLIENT` บังคับว่าต้องมีทั้ง `apiGet` และ `apiPost` แต่หน้าอ่านอย่างเดียวไม่มี `apiPost` จึง `return` ทิ้งทั้งก้อน (กฎเหล็กข้อ 18 ซ้ำรอบที่สอง) · และรายงานเรียก `render()` ก่อน shim ติดตั้ง จึงตัดสินใจตอน `getApiUrl()` ยังคืนค่าว่าง — แก้ทั้งคู่พร้อมเพิ่ม `kickIfNeverFetched()` · **หน้าสาธารณะ:** ล็อคที่ยกเลิกการเช่าแล้วขึ้นเป็นล็อคว่างได้จริง (เดิมนับว่ายังมีผู้ค้าตลอดไป) · แยกหมวดตามโซนทุกช่อง + หมายเหตุโซนเปิดท้ายที่ TA/TB/TC · เอาป้าย "ผู้จัดขายตรง" ราคาขายจร และหมายเหตุ "แสดงเฉพาะหมายเลขล็อค" ออก · **ประวัติ:** เปลี่ยนชื่อเป็น "ประวัติแจ้งลา/ขาด/ยกเลิก" + เพิ่มแท็บ "ยกเลิก" รวม 4 ประเภทจาก 4 แหล่ง · **คิวจอง:** แก้ไขคิวรอได้ทุกช่อง + เลือกได้หลายโซน · พบว่า `floating_queue` ไม่มีคอลัมน์ `line`/`note` เลย (ตกหล่นตอน migrate) · `migration 0014` + ด่านตรวจใหม่ 3 ตัว + กฎเหล็กข้อ 22–25 · ซ่อม `seed_test_db.sql` ที่พัง truncate มาตั้งแต่ `0008` |
| **Unified v2.7 + ฐานข้อมูล v3.1** | **3 ส.ค. 2026** | **รอบแก้ 7 เรื่องจากผู้ดูแลตลาด (ดูหัวข้อ 18):** เอาช่องกำหนดค่าธรรมเนียมรายปีออกจากหน้าส่วนลด (คงจุดตั้งค่าหลัก) · เรียงคิวรอ → ขายแล้ว → ล็อคว่าง พร้อมตัวกรองโซนแบบติ๊ก · แก้ข้อความเลือกโซนล้นกล่อง · ซ่อมหน้าค่าไฟให้รอผลฐานข้อมูล กรองผู้ค้ายกเลิก ล้างข้อมูลค้าง และคิดตามเดือนบิล · กลับผัง TUE F6→F1 เพื่อให้ F1 ขวาสุด · เพิ่มผู้ค้าพิเศษจากผู้บริหาร (ฟรีค่าเช่า/ค่าธรรมเนียม แต่ค่าไฟ/ค่าอื่นคิดจริง) · รองรับขายล็อคจรฟรีด้วยราคา 0 อย่างชัดเจน · เพิ่ม `test_aug2026_bugfixes.js` และปรับ `test_sheets_guard.js` ให้ตรงกับโหมด Supabase ที่ใช้จริง |
| **Unified v2.8 + ฐานข้อมูล v3.2** | **4 ส.ค. 2026** | เพิ่มบัญชียอดค้างและรอบการเช่าผู้ค้าประจำ รับชำระจากยอดค้างเก่าสุดก่อน ป้องกันรับเกินยอด ยกเลิกระบบผ่อน และเพิ่มรายงานประวัติรับชำระรายรอบ (ดูหัวข้อ 19) |
| **Unified v2.9 + ฐานข้อมูล v3.3** | **5 ส.ค. 2026** | เพิ่มสิทธิ์รายฟังก์ชัน บัญชีหลัก `paemai` การยกเลิกรายการรับเงินพร้อมหลักฐาน และแยกค่าเช่า/ค่าไฟในหน้ารับชำระ (ดูหัวข้อ 20) |
| **Unified v3.0 + ฐานข้อมูล v3.4** | **5 ส.ค. 2026** | รวมแหล่งยอดเงินเป็นบัญชีลูกหนี้กลาง ป้องกันค่าเช่า/ค่าไฟซ้ำถาวร ซ่อมข้อมูล TUE 166 กลุ่ม แยกเมนูส่วนลดไว้ในหมวดการเงิน เรียงล็อคค้างชำระแบบอักษร+ตัวเลข และกลับแถว SF ให้ SF1 อยู่ขวาสุด (ดูหัวข้อ 21) |
| **Unified v3.0.1 + ฐานข้อมูล v3.4** | **5 ส.ค. 2026** | ซ่อมรายงาน 09 ที่เปิดจากหน้า launcher แล้วไม่ได้รับ session ทำให้ RPC ตอบไม่มีสิทธิ์/ไม่มีข้อมูล เพิ่มตัวติ๊กเลือกหลายโซน โดยไม่เลือกจะแสดงผู้ค้าปัจจุบันทุกล็อค และเพิ่มทางเปิดสำรองสำหรับ WebView/PWA ที่บล็อกหน้าต่างใหม่ (ดูหัวข้อ 22) |

---

## 12. เครื่องมือใน `tools/` และลำดับการรันที่ถูกต้อง (เพิ่ม v2.3)

ระบบนี้เป็น static HTML ที่แต่ละไฟล์ self-contained ไม่มี build step ที่รวม CSS/JS ได้ (ตามกฎ
"ห้ามเพิ่มความซับซ้อน" — ไม่มี framework ไม่มี bundler) แต่มี CSS/JS บางชุดที่ต้อง **เหมือนกันทุกไฟล์**
เช่นชุดปรับหน้าจอมือถือและแถบตัวกรองรายงาน ถ้าก๊อปด้วยมือ 20 ไฟล์ ไม่นานไฟล์จะต่างเวอร์ชันกัน (drift)
ซึ่งเป็นปัญหาเดิมที่ระบบรวมสองตลาดตั้งใจกำจัด จึงใช้สคริปต์เป็น **แหล่งจริงแหล่งเดียว** ของโค้ดชุดนั้น
แล้วฝังลง `core/` ให้อัตโนมัติ

| สคริปต์ | หน้าที่ | ฝังบล็อคชื่อ | แก้ที่ไหน |
|---|---|---|---|
| `tools/inject_mobile_kit.py` | ชุดปรับหน้าจอมือถือ + ฟอนต์ไทย + ครอบตารางให้เลื่อนแนวนอน | `PM_MOBILE_KIT` | แก้ในสคริปต์ แล้วรันใหม่ |
| `tools/inject_status_colors.py` | ระบบสีสถานะล็อค + ขนาดเลขล็อค + ไล่สีเขียวออกจากผังวันพฤหัสฯ | `PM_STATUS_COLORS` | แก้ในสคริปต์ แล้วรันใหม่ |
| `tools/inject_report_filter.py` | แถบตัวกรองกลางของรายงาน 01–08 (รายงาน 09 เป็นรายงานข้อมูลปัจจุบัน ไม่ต้องกรองวัน) | `PM_REPORT_FILTER` | แก้ในสคริปต์ แล้วรันใหม่ |
| `tools/inject_theme_kit.py` **(ใหม่ v2.4)** | ชุดโหมดมืดกลาง — ฝังให้หน้าที่ยังไม่มี (รายงาน 01–07 + หน้ารับชำระ) ใช้ชุดสีและปุ่มสลับเดียวกับหน้าอื่น | `PM_THEME_KIT` / `PM_THEME_JS` | แก้ในสคริปต์ แล้วรันใหม่ |
| `tools/wire_reports.py` | ต่อสายรายงานให้ใช้ตัวกรองกลาง (เปลี่ยนแหล่งพารามิเตอร์ ไม่เขียน render ใหม่) | marker `PM_WIRED_MOUNT` | แก้ในสคริปต์ |
| `tools/inject_api_client.py` **(ใหม่ v3.0)** | ตัวเชื่อมต่อฐานข้อมูลชุดกลาง 3 โหมด (`sheets`/`parallel`/`supabase`) + บังคับให้ทุกจุดถามผ่าน `getApiUrl()` ที่เดียว + ปิดทางบัญชีสาธิตเมื่อเชื่อมต่อฐานข้อมูลแล้ว | `PM_API_CLIENT` + `pm-api-shim` | แก้ในสคริปต์ แล้วรันใหม่ |
| `tools/patch_market_maps.py` | ฝังบล็อค legend/sync ที่มี marker ให้ผังทั้งสองไฟล์ | marker `PM_LEGEND_V2` / `PM_SYNC_BOOT` | แก้ในสคริปต์; ตรรกะเฉพาะตลาดนอก marker แก้ไฟล์ `.TUE/.THU` ให้คู่กัน |
| `tools/check_js.py` | ตรวจไวยากรณ์ JS ทุก `<script>` **และความสมบูรณ์ของ CSS** (คอมเมนต์ปิดไม่ครบ / วงเล็บปีกกาไม่สมดุล — เพิ่ม v2.4) | — | — |
| `tools/test_responsive.js` | เปิดทุกหน้าด้วย Chromium จริง 4 ขนาดจอ ตรวจ overflow / ปุ่มเล็กเกิน / JS error | — | — |
| `tools/test_map_reach.js` **(ใหม่ v2.5)** | เปิดผังตลาดทุกโซนที่ 360/390/430px แล้วตรวจว่า **ทุกล็อคเลื่อนนิ้วไปหาได้จริง** — จับเคสที่เนื้อหาถูก `body{overflow-x:hidden}` ตัดทิ้งจนแตะไม่ได้ ซึ่ง `test_responsive.js` มองไม่เห็น | — | — |
| `tools/test_map_vendor_sync.js` | จำลองผล `getVendors` จาก Supabase แล้วตรวจว่าล็อคตัวแทนของทุกกลุ่มผังถูกเปลี่ยนเป็นข้อมูลจริง พร้อมตรวจหมายเลขล็อคซ้ำ — กันหน้าหลักแสดงจำนวนผู้ค้าได้ แต่บางโซนในผังยังว่าง | — | — |
| `tools/test_aug2026_bugfixes.js` **(ใหม่ v2.7)** | จำลองบั๊กชุด v2.7 ทั้งสองตลาด: ลำดับคิว/กรองโซน/ข้อความมือถือ/ขายราคา 0/ผู้ค้าพิเศษ/ค่าไฟไม่ค้าง/ผัง F/หน้าส่วนลด | — | — |

> ด่านตรวจผังทั้งสองใช้ Chrome ที่ติดตั้งในเครื่องโดยอัตโนมัติ (หรือกำหนด `PM_CHROME_EXECUTABLE`) จึงไม่ต้องดาวน์โหลด Chromium ของ Playwright เพิ่มก่อนตรวจ deploy.
| `tools/test_offline_map.js` **(ใหม่ v2.5)** | จำลอง 3 สถานการณ์ (ไม่มี API URL / ตอบ error 500 / เน็ตค้างไม่ตอบ) แล้วบังคับว่าผังต้อง **เตือนผู้ใช้** ไม่ใช่แสดงว่าล็อคว่างเงียบ ๆ | — | — |
| `tools/prepare_core.py` | สคริปต์ one-off ที่ใช้สร้าง core ครั้งแรก (เก็บไว้อ้างอิง ไม่ต้องรันอีก) | — | — |

**⚠ กฎเหล็ก:** สคริปต์ inject ทุกตัว **เขียนทับบล็อคเดิมที่คั่นด้วย marker เสมอ** (idempotent) ดังนั้น
**ห้ามแก้โค้ดในบล็อคเหล่านั้นด้วยมือที่ไฟล์ `core/`** เพราะจะถูกลบทิ้งเมื่อรันสคริปต์ครั้งถัดไป

### ลำดับการรันมาตรฐาน (ทุกครั้งที่แก้ระบบ)

```bash
cd ระบบรวมสองตลาด/

# 1) ถ้าแก้สคริปต์ใน tools/ ให้รันฝังใหม่ (ข้ามได้ถ้าแก้แต่โค้ดใน core/ ที่ไม่ใช่บล็อค marker)
python3 tools/inject_mobile_kit.py
python3 tools/inject_theme_kit.py                # ใหม่ v2.4 — ต้องรัน "ก่อน" inject_mobile_kit ถ้ารันทั้งคู่
python3 tools/inject_status_colors.py
python3 tools/inject_report_filter.py
python3 tools/wire_reports.py
python3 tools/patch_market_maps.py
python3 tools/inject_api_client.py                # ใหม่ v3.0 — ต้องรัน "หลัง" patch_market_maps เสมอ
                                                  # (ชิมต้องครอบ apiGet/apiPost/getApiUrl ตัวล่าสุด)

# 2) ด่านตรวจคุณภาพ — ต้องผ่านทั้ง 3 ก่อน deploy (กฎเหล็กข้อ 11)
python3 tools/check_js.py                        # ต้องเห็น ✅ ทุกไฟล์ (JS + CSS)
python3 build.py                                 # ต้องเห็น ✅ build เสร็จ + ✅ verify ผ่าน
node tools/test_responsive.js dist/tuesday       # ต้องได้ 76/76
node tools/test_responsive.js dist/thursday      # ต้องได้ 76/76
node tools/test_map_reach.js  dist/tuesday dist/thursday    # ใหม่ v2.5 — ทุกล็อคต้องเลื่อนไปหาได้
node tools/test_map_vendor_sync.js dist/tuesday dist/thursday # ทุกโซนต้องรับข้อมูลผู้ค้า
node tools/test_offline_map.js dist/tuesday dist/thursday   # ใหม่ v2.5 — ใช้เวลา ~2 นาที (มี timeout จริง)

# 3) เฉพาะงานที่แตะฐานข้อมูล Supabase (ใหม่ v3.0 — ต้องมี PostgreSQL ในเครื่อง ดูหัวข้อ 16.6)
node tools/test_e2e_supabase.js thu              # หน้าเว็บจริง → api() จริง (โหมด supabase ล้วน)
node tools/test_e2e_supabase.js tue
node tools/test_sheets_guard.js                  # โหมดเดิมต้องไม่เปลี่ยนพฤติกรรม + ทางถอยใช้ได้จริง

# 4) deploy ตามหัวข้อ 9 (backend ก่อน แล้ว frontend) แล้วตรวจตามหัวข้อ 9.3
```

`test_responsive.js` ต้องมี Playwright + Chromium ถ้าเครื่องยังไม่มีให้รัน `npm i playwright` หนึ่งครั้ง
(ตัวทดสอบนี้ยังอ่าน `pageerror` ของเบราว์เซอร์จริงด้วย จึงจับ JS error ที่การอ่านโค้ดมองไม่เห็น —
บั๊กหน้าลงทะเบียนของตลาดวันพฤหัสฯ ใน v2.3 ถูกพบด้วยวิธีนี้)

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

## 13. กฎธุรกิจเรื่องค่าธรรมเนียมและส่วนลดค่าเช่า (เพิ่ม v2.3)

ยืนยันกับผู้ดูแลตลาดเมื่อ 26 ก.ค. 2026 — ตรรกะนี้ถูกฝังไว้ 2 ที่ที่ **ต้องตรงกันเสมอ** คือ
`PM_DISCOUNT_ENGINE` ใน `core/pamai_batch_payment.html` (จุดคิดเงินจริง) และ
`core/pamai_report_08.html` (รายงาน) — ถ้าแก้ตรรกะ ต้องแก้ทั้งสองที่พร้อมกัน

### 13.1 ค่าธรรมเนียมแรกเข้า
- เก็บ **ครั้งเดียว** ต่อการเข้าเป็นผู้ค้าประจำรายใหม่ (เก็บตอนลงทะเบียน)
- **(เพิ่ม v2.4)** ใช้ **อัตรากลางทั้งตลาด** เก็บที่ชีต `settings` คีย์ `entry_fee` ตั้งที่
  **เมนูหลัก › ระบบ › ตั้งค่าค่าธรรมเนียม** — หน้าลงทะเบียนผู้ค้าใหม่จะเติมค่านี้ให้อัตโนมัติและ
  **ล็อคช่องไว้ (readonly)** พนักงานแก้ไม่ได้ ต้องเปลี่ยนที่หน้าตั้งค่าเท่านั้น
- หน้าลงทะเบียนจะ **เตือน** ถ้าล็อคนั้นเคยมีรายการ `ค่าแรกเข้า` ในชีต `payments` มาก่อน
  แต่ **ไม่บล็อก** เพราะล็อคเดิมอาจเปลี่ยนเป็นผู้ค้ารายใหม่ซึ่งต้องเก็บค่าแรกเข้าใหม่จริง — ผู้จัดเป็นคนตัดสิน

### 13.2 ค่าธรรมเนียมรายปี
- เก็บครั้งแรกตอนลงทะเบียนเป็นผู้ค้าประจำ แล้วเก็บอีกครั้ง **ทุกเดือนมีนาคมของทุกปี**
- บิลค่าเช่าเดือนมีนาคมจะ **บวกค่าธรรมเนียมรายปีให้อัตโนมัติ** พร้อม **หมายเหตุกำกับในรายละเอียดค่าเช่า**
  ว่าเป็นค่าธรรมเนียมรายปีของปีใด (แสดงทั้งในรายการล็อค ในตารางกรอกเงิน และในใบสรุปก่อนยืนยัน)
- ใช้ **อัตรากลางทั้งตลาด** เก็บที่ชีต `settings` คีย์ `annual_fee` ไม่ต้องไปแก้รายล็อค
  **(เปลี่ยน v2.4)** จุดตั้งค่าอย่างเป็นทางการคือ **เมนูหลัก › ระบบ › ตั้งค่าค่าธรรมเนียม** (สิทธิ์ admin)
  ช่องเดิมในหน้ารับชำระ (ปุ่ม "ส่วนลดค่าเช่า") ยังใช้ได้และเขียนลงคีย์เดียวกัน แต่เป็นทางลัดสำหรับ
  ผู้ที่อยู่หน้านั้นอยู่แล้วเท่านั้น
- ล็อคที่ **จ่ายค่าธรรมเนียมรายปีของเดือนมีนาคมนั้นไปแล้ว** จะไม่ถูกบวกซ้ำ (ตรวจจากชีต `payments`
  type = `ค่าธรรมเนียมรายปี` ในเดือนเดียวกัน) — ครอบคลุมกรณีผู้ค้าที่ลงทะเบียนใหม่ในเดือนมีนาคมเองด้วย
- ตอนบันทึก ระบบ **แยกเป็น 2 แถวในชีต `payments`** (`ค่าเช่าประจำเดือน` + `ค่าธรรมเนียมรายปี`)
  ไม่รวมเป็นยอดเดียว เพื่อให้รายงานรับชำระแยกประเภทได้ถูกต้องและตรวจซ้ำได้

### 13.3 ส่วนลดค่าเช่า
| หัวข้อ | กติกา |
|---|---|
| หน่วย | **จำนวนเงินบาทเท่านั้น** (ไม่มีเปอร์เซ็นต์ — ยืนยัน 26 ก.ค. 2026) |
| ขอบเขต | `market` = ทุกล็อคเท่ากันทั้งตลาด · `lock` = เฉพาะล็อค (กำหนดไม่เท่ากันได้) |
| ลำดับความสำคัญ | **ส่วนลดรายล็อคชนะส่วนลดทั้งตลาดเสมอ** (specific wins over general) |
| ช่วงเวลาที่ใช้ได้ | **เดือนปัจจุบันหรือเดือนในอนาคตเท่านั้น — ห้ามย้อนหลังเด็ดขาด** |
| การบังคับกฎ | ตรวจ **ทั้งที่หน้าเว็บและที่ backend** (`saveDiscount` ปฏิเสธเดือนที่ผ่านไปแล้ว) เพราะหน้าเว็บถูก bypass ได้ |
| ลบส่วนลด | ลบได้เฉพาะเดือนปัจจุบัน/อนาคต — ห้ามลบของเดือนที่ผ่านไปแล้ว เพื่อรักษาความถูกต้องของรายงานย้อนหลัง |
| ล็อคที่กำลังผ่อนชำระ | กรณีส่วนลด **ทั้งตลาด** ต้องเลือกได้ว่าจะให้ล็อคที่มีแผนผ่อนชำระ active ด้วยหรือไม่ (flag `apply_installment`) · กรณีส่วนลด **รายล็อค** ถือว่าผู้จัดเจาะจงเองแล้ว จึงให้เสมอ |
| เพดาน | ส่วนลดไม่เกินค่าเช่าจริงของเดือนนั้น (กันยอดสุทธิติดลบ) |
| การจัดเก็บ | ชีต `rent_discounts` — 1 เดือนมีส่วนลดทั้งตลาดได้ 1 รายการ และส่วนลดรายล็อคได้ล็อคละ 1 รายการ (upsert) |

**ลำดับการคิดเงินต่อล็อคในหนึ่งเดือน (ห้ามสลับลำดับ):**

```
ค่าเช่าเต็มเดือน = ค่าเช่า/วันตลาด × จำนวนวันตลาดของเดือนนั้น
  + ค่าธรรมเนียมรายปี   (เฉพาะเดือนมีนาคม และเฉพาะล็อคที่ยังไม่จ่ายรายปีของมีนาคมนั้น)
  − ส่วนลด              (รายล็อคชนะทั้งตลาด · ไม่เกินค่าเช่าเต็มเดือน)
  + ค่าไฟรายเดือน + ค่าปรับ + ค่าอื่น ๆ
  = ยอดที่เรียกเก็บ
```

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

---

## 14. บทเรียนจาก v2.4: ชื่อคลาสชนกัน และ "ตัวเลือกที่ไม่ได้ผูกกับอะไร" (เพิ่ม v2.4)

รอบนี้ผู้ดูแลตลาดแจ้งบั๊กมา 3 เรื่อง ทั้งสามเรื่องมีลักษณะร่วมกันคือ **ระบบไม่แจ้ง error ใด ๆ เลย**
ไม่มีหน้าขาว ไม่มี JS error ใน console ด่านตรวจเดิม (`check_js.py` + `build.py` verify) ผ่านหมด
แต่ผู้ใช้จริงใช้งานไม่ได้ — จึงบันทึกไว้ที่นี่เพื่อไม่ให้เกิดซ้ำ

### 14.1 บั๊ก "เปิดเมนูแล้วจอดำ" บนมือถือ (ร้ายแรงที่สุดของรอบนี้)

**อาการ:** บนมือถือ กดเมนูอะไรก็ตามที่เป็นโมดูลย่อย (ผังตลาด, รับชำระ, ผ่อนชำระ, ค่าไฟฟ้า,
ผู้ค้าใหม่, ประวัติลา, คิวจร, กฎระเบียบ, รายงาน — 9 เมนูจาก 12) จะเห็นเนื้อหาแค่แถบบาง ๆ ด้านบน
ที่เหลือเป็นพื้นดำทั้งจอ เลื่อนก็ไม่มีอะไร บนเดสก์ท็อปปกติดีทุกอย่าง

**ต้นเหตุ:** shell ของหน้าแอพหลักใช้คลาสชื่อ `.layout` ซึ่งต้องมีความสูงที่แน่นอน
(`height: calc(100vh - 56px)`) เพื่อให้ `.content → .pane → iframe` สืบทอด `height:100%` ต่อกันได้
แต่ `PM_MOBILE_KIT` มีกฎที่เขียนไว้สำหรับ **หน้ารับชำระ** ซึ่งบังเอิญใช้ชื่อคลาส `.layout` เหมือนกัน:

```css
@media (max-width: 640px) { .layout { height: auto !important; } }   /* ← เขียนไว้เพื่อหน้ารับชำระ */
```

เนื่องจาก `inject_mobile_kit.py` ฝังโค้ดชุดเดียวกันลง **ทุกไฟล์** กฎนี้จึงไปลบความสูงของ shell ด้วย
ผลลัพธ์ต่อเนื่อง: `.pane` สูง `auto` → `iframe` ไม่มีความสูงให้สืบทอด → ตกไปใช้ **ความสูงมาตรฐานของ
`<iframe>` คือ 150px** → ที่เหลือของจอกลายเป็นพื้นหลังเปล่า ซึ่งในโหมดมืดคือ "จอดำ"

**ทำไมด่านตรวจเดิมจับไม่ได้:** `test_responsive.js` ตรวจ 3 อย่าง (เนื้อหาล้นขอบจอ / element กว้างเกิน /
ปุ่มเล็กเกิน) — หน้าที่ยุบเหลือ 150px **ไม่ล้นอะไรเลย** จึงผ่านฉลุย 76/76 ทั้งที่ใช้งานไม่ได้จริง

**วิธีแก้:**
1. เปลี่ยนชื่อคลาส shell จาก `.layout` เป็น **`.app-shell`** (ชื่อที่ไม่มีใครใช้ซ้ำ)
2. เปลี่ยนกฎใน mobile kit จาก `.layout` เป็น **`.layout.pm-stack-mobile`** แบบ opt-in แล้วให้
   `core/pamai_batch_payment.html` ติดคลาส `pm-stack-mobile` เอง
3. เพิ่มกฎย้ำใน mobile kit ว่า `.app-shell` ต้องคงความสูงเสมอ และ `.module-frame { height:100% }`
4. **ไม่ใช้ `:has()`** ในการแยกแยะ เพราะถ้าเบราว์เซอร์ไม่รู้จัก `:has()` เบราว์เซอร์จะทิ้ง selector list
   ทั้งก้อน ทำให้กฎที่เหลือในบรรทัดเดียวกันหายไปด้วย

**บทเรียนที่กลายเป็นกฎเหล็กข้อ 12:** โค้ดที่ถูกฝังลงทุกไฟล์ **ห้ามใช้ชื่อคลาสกว้าง ๆ เป็นตัวเลือกลอย ๆ**
ต้องเป็น opt-in ด้วยคลาสเฉพาะเสมอ

### 14.2 ช่องเลือกเดือนที่ hardcode พ.ศ. 2568 (พบ 2 หน้า)

**อาการ:** หัวหน้าจอ "รับชำระเงินรวม" แสดง `ก.ค. 2568` ทั้งที่ตอนนี้คือปี 2569 คนเก็บเงินเข้าใจผิดว่า
กำลังเก็บบิลของปีที่แล้ว (ยอดเงินถูกต้องอยู่แล้ว เพราะทุกฟังก์ชันอ่านรอบบิลจากช่อง "วันที่รับเงิน")

**ต้นเหตุ:** `<option>` สามบรรทัดถูกพิมพ์ตายตัวไว้ตั้งแต่ตอนเขียนหน้า และ `filterChanged()` ที่ผูกกับ
`onchange` ก็ **ไม่เคยอ่านค่าจาก `#monthSelect` เลย** — เป็น "ป้ายที่ไม่ได้ต่อสายกับอะไร"
ตรวจเจอบั๊กเดียวกันอีกจุดที่ **หน้าค่าไฟฟ้า** (`#billMonth` + array `MONTHS` ที่ hardcode 6 เดือนของปี 2568)

**วิธีแก้:** สร้างรายการเดือนด้วย JS จากวันที่จริงเสมอ (ย้อนหลัง 3 เดือน → ล่วงหน้า 1 เดือน, พ.ศ. ถูกต้อง,
`value` เป็น `YYYY-MM` ค.ศ. ให้ตรงกับรูปแบบที่ชีตใช้) แล้ว **ผูกสองทาง** กับช่องวันที่รับเงิน

**บั๊กพ่วงที่เจอระหว่างแก้ (ผู้ใช้ไม่ได้แจ้ง):**
- `thisMonthTuesdays()` คูณค่าเช่า/ค่าไฟด้วยจำนวนวันอังคารของ **เดือนปัจจุบันของเครื่อง** เสมอ
  พอเลื่อนรอบบิลไปเดือนอื่น ตัวคูณจึงผิดเงียบ ๆ (เดือนที่มี 5 วันอังคารถูกคิดเป็น 4)
  → เปลี่ยนเป็น `billMonthTuesdays()` ที่อิงเดือนรอบบิลจริง ทั้งหน้ารับชำระและหน้าค่าไฟฟ้า
- `payDate` ตั้งค่าเริ่มต้นด้วย `toISOString()` ซึ่งเป็น **UTC** — ในไทย (UTC+7) ช่วง 00:00–06:59
  จะได้ "เมื่อวาน" ทำให้รอบบิลข้ามเดือนผิดถ้าเปิดหน้าตอนเช้ามืดของวันที่ 1 → เปลี่ยนเป็นเวลาท้องถิ่น

**บทเรียนที่กลายเป็นกฎเหล็กข้อ 13:** `<select>` เดือน/ปี ห้าม hardcode และต้องมีโค้ดที่อ่านค่าไปใช้จริง

### 14.3 Dashboard ยังนับล็อคที่ยกเลิกการเช่าไปแล้ว

**อาการ:** ยกเลิกการเช่าล็อค A1 แล้ว ผังตลาดขึ้นเป็นล็อคว่างถูกต้อง แต่หน้าหลักยังนับว่ามีผู้ค้าอยู่
(การ์ด "ผู้ค้าในระบบ (ล็อค)" ไม่ลด และ "จำนวนผู้ค้าตามโซน" ยังขึ้นโซน A เท่าเดิม)

**ต้นเหตุ:** `renderDashboard()` คำนวณตัวแปร `live` (ไม่รวม `terminated`) ไว้แล้วและใช้กับ**ยอดเงิน**
อย่างถูกต้อง แต่ **จำนวนนับ** ยังใช้ `VENDORS` ทั้งก้อน ตัวเลขบนการ์ดเดียวกันจึงมาจากคนละฐาน —
มูลค่า/เดือนลดลงแล้วแต่จำนวนรายไม่ลด

**วิธีแก้:** ให้ทุกตัวเลขบนหน้าหลักนับจาก `live` และเพิ่มข้อความ "ยกเลิกแล้ว N ล็อค" ต่อท้ายรายโซน
เพื่อให้ยังตรวจสอบย้อนได้โดยไม่ปนกับจำนวนผู้ค้าที่ยังเช่าอยู่

### 14.4 สรุปสิ่งที่เพิ่มเข้าด่านตรวจหลังรอบนี้

| เพิ่มอะไร | จับอะไรได้ |
|---|---|
| ตรวจความสมบูรณ์ของ CSS ใน `check_js.py` | คอมเมนต์ `/* */` ปิดไม่ครบ และวงเล็บปีกกาไม่สมดุล — สองอย่างนี้ทำให้เบราว์เซอร์ทิ้งกฎที่เหลือทั้งบล็อคโดยไม่มี error (เกิดขึ้นจริงระหว่างแก้บั๊ก 14.1 และทำให้หน้ารับชำระเพี้ยนอยู่หนึ่งรอบ) |
| ทดสอบด้วยการ **เข้าสู่ระบบแล้วกดทุกเมนูจริง** ที่ 390px | หน้าที่ "ยุบ" แต่ไม่ล้น ซึ่ง `test_responsive.js` มองไม่เห็น — ควรทำทุกครั้งที่แตะ CSS ของ layout |

**ข้อควรระวังที่ยังเหลืออยู่ (ยังไม่ได้แก้ รอผู้ดูแลตลาดตัดสิน):**
หน้าลงทะเบียนผู้ค้าใหม่ตอนนี้ **ล็อคช่องค่าธรรมเนียมแรกเข้าไว้** ตามที่สั่ง แต่กฎธุรกิจข้อ 13.1 บอกว่า
ค่าแรกเข้าเก็บครั้งเดียวต่อผู้ค้าหนึ่งราย ถ้าล็อคนั้นเคยเก็บไปแล้วและเป็น **ผู้ค้ารายเดิม** ผู้จัดจะต้อง
ใส่ 0 ซึ่งตอนนี้ทำไม่ได้ ระบบยังคงแสดงคำเตือน "ล็อคนี้เคยเก็บค่าแรกเข้าไปแล้ว" ให้เห็นอยู่
ถ้าเจอเคสนี้จริงหน้างาน ให้แจ้งเพื่อเพิ่มปุ่มยกเว้นเฉพาะรายสำหรับ admin (พร้อมบันทึกลง activity_log)

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

---

## 15. บทเรียนจาก v2.5: แท็กที่ไม่ได้ปิด และ "ความล้มเหลวที่เงียบ" (เพิ่ม v2.5)

รอบนี้ผู้ดูแลตลาดแจ้งมา 4 เรื่อง + ขอแผนย้ายฐานข้อมูล 1 เรื่อง ทั้งหมดถูก **จำลองซ้ำได้จริง
ในเบราว์เซอร์จริง (Chromium + mock Apps Script API)** ก่อนแก้ ไม่ได้เดาจากการอ่านโค้ด
และพบว่าสามในสี่เรื่องมีรากเดียวกันคือ **ระบบล้มเหลวโดยไม่บอกใคร**

### 15.1 บั๊ก A — `<div class="toolbar">` ไม่เคยถูกปิดแท็ก (ทั้งสองตลาด)

**อาการ:** ในโซนที่ผังแคบ (ข้างโลตัส / ข้าง 7-Eleven ของฝั่งวันพฤหัสฯ) แถบสถานะ/ตัวกรอง
ลอยไปอยู่กึ่งกลางแนวตั้งของจอ และผังถูกดันไปชิดขวา เหลือพื้นที่ว่างครึ่งจอด้านซ้าย

**ต้นเหตุ:** บรรทัด 455–467 ของไฟล์ผังทั้งสองตลาด มี `</div>` แค่ตัวเดียวสำหรับกล่องซ้อนกันสองชั้น
เบราว์เซอร์จึงปิด `.toolbar` ให้เองที่ `</body>` ผลคือ **ผังตลาดทั้งผืนกลายเป็น flex item
ตัวหนึ่งของ `.toolbar`** ซึ่งเป็น `display:flex; align-items:center`

- โซนที่ผังกว้าง (A–F) → ผังถูกดันขึ้นบรรทัดใหม่ **ดูเหมือนปกติ** จึงไม่มีใครเห็นบั๊กนี้มาหลายเวอร์ชัน
- โซนที่ผังแคบ → ผังอยู่บรรทัดเดียวกับ legend แล้ว `align-items:center` จับ legend ไปกลางจอ

**วัดจริงก่อนแก้ (Chromium 1440×900 · โซนข้างโลตัส):** `.toolbar` สูง **1757px** ·
legend อยู่ที่ **y=967** · ผังอยู่ที่ **x=975** — หลังแก้: 48px / y=115 / x=0

**ทำไมด่านตรวจเดิมจับไม่ได้:** `check_js.py` ตรวจ JS+CSS · `test_responsive.js` ตรวจ "เนื้อหาล้นขอบจอ"
ซึ่งเคสนี้ **ไม่ล้นอะไรเลย** → เพิ่ม **ด่านตรวจโครงสร้าง HTML** ใน `check_js.py` (`check_html_structure`)
ซึ่งจะฟ้องทันทีถ้ามีแท็กเปิดที่ไม่มีตัวปิด

### 15.2 บั๊ก B — ผังแสดงล็อคผู้ค้าประจำเป็น "ว่าง" ทั้งตลาด (ร้ายแรงที่สุดของรอบนี้)

**อาการที่แจ้ง:** "ในเวอร์ชันมือถือ ล็อคที่มีผู้ค้าประจำถูกแสดงเป็นล็อคว่างทั้งหมด — ทั้งสองตลาด"

**ต้นเหตุ:** ผังสร้างล็อคทุกช่องเป็น `vacant` ก่อน แล้วรอข้อมูลจาก Sheets มาระบายทับ
แต่ฟังก์ชันที่ดึงข้อมูล **ยอมแพ้เงียบ ๆ ถึง 2 จุด** และ `apiGet()` เองก็ `catch(e){return null;}`

```js
if(!getApiUrl()) return;                 // ← ไม่มี URL ในเครื่องนี้ → เงียบ
const vendors = await apiGet('getVendors');
if(!Array.isArray(vendors)) return;      // ← fetch พัง/timeout → เงียบ
```

ผลคือ **"โหลดไม่สำเร็จ" กับ "ตลาดว่างจริง" หน้าตาเหมือนกันเป๊ะ** — ผู้จัดใช้ผังนี้ตัดสินใจขายล็อคจร
หน้างาน จึงเป็นความผิดพลาดที่อันตรายที่สุดของระบบ

**ผลจำลองจริงก่อนแก้ (จอ 390px):**

| สถานการณ์ | ฝั่งวันพฤหัสฯ | ฝั่งวันอังคาร |
|---|---|---|
| API ตอบ 500 | ว่าง 297/297 ล็อค ไม่มีคำเตือน | แสดง**ข้อมูลปลอมที่ hardcode ในไฟล์** (occupied 13 / unpaid 4) |
| เครื่องนี้ยังไม่มี API URL | เหมือนข้างบน | เหมือนข้างบน |

**ทำไมโผล่บนมือถือเป็นหลัก — 3 สาเหตุทับกัน:**

1. Apps Script ช้า + เน็ตมือถือกลางตลาด timeout ง่ายกว่า wifi มาก (หน้าเปิดหนึ่งครั้งยิง 3–4 request)
2. เปิดจากลิงก์ในไลน์ หรือไอคอน Add-to-Home-Screen บน iOS = **localStorage คนละถังกับ Safari**
   ค่า `pm-thu-api-url` ที่เคยตั้งไว้จึงหายไป
3. ไม่มีการแคชข้อมูลผู้ค้าไว้ในเครื่องเลย — โหลดพลาดครั้งเดียวคือไม่เหลืออะไรให้แสดง

**วิธีแก้ — ชุด `PM_VENDOR_SYNC` (สร้างโดย `tools/patch_market_maps.py` ห้ามแก้ในไฟล์ผัง):**

| ชั้น | สิ่งที่ทำ |
|---|---|
| มองเห็นได้ | แถบเตือนใต้ toolbar 4 สถานะ: กำลังโหลด / สำเร็จ / ใช้ข้อมูลแคช / ล้มเหลว / ยังไม่เชื่อมต่อ |
| ไม่โกหก | สถานะที่สาม **"ยังไม่ทราบ"** — ล็อคเป็นลายทางจาง ๆ ข้อความ "ว่าง" หายไป ดูไม่เหมือนล็อคว่างเด็ดขาด |
| ลองใหม่ | `pmFetchVendors()` มี timeout 15 วิ + retry 2 ครั้งแบบ backoff + ปุ่ม "ลองใหม่" ในแถบเตือน |
| แคชสำรอง | เก็บ snapshot ล่าสุดใน `pm-{tue,thu}-vendors-cache` — โหลดพลาดก็ยังเห็นของเก่าพร้อมป้ายบอกเวลา |
| แก้ที่ต้นเหตุ 2 | ปุ่ม **"ตั้งค่าการเชื่อมต่อ"** ในแถบเตือน ให้ผู้จัดวาง Web App URL เองหน้างานได้ใน 15 วินาที |

**และลบข้อมูลผู้ค้าที่ hardcode ในผังฝั่ง TUE ออก 34 จุด** — ข้อมูลชุดนั้นทำให้ผัง "ดูเหมือนมีข้อมูล"
ทั้งที่โหลดจริงไม่สำเร็จ (อันตรายกว่าผังว่างเสียอีก) และเป็นชื่อร้าน/ค่าเช่าของผู้ค้าจริงบน public repo
⚠️ **การลบข้อมูล hardcode ต้องทำพร้อมกับ `PM_VENDOR_SYNC` เท่านั้น ห้ามแยกรอบ**

**ด่านตรวจใหม่:** `tools/test_offline_map.js` จำลอง 3 สถานการณ์แล้วบังคับว่าต้องมีแถบเตือนทุกครั้ง

### 15.3 บั๊ก C — ผังโซนโรงหน้า (SA) ฝั่ง TUE ล้นกรอบและเลื่อนไปดูไม่ได้บนมือถือ

**ต้นเหตุ 2 ชั้น:** (1) แถวล็อค `.lrow` เป็น flex + `.lk{flex-shrink:0}` กว้างกว่าจอเสมอ
แต่การ์ด `.bld` ถูกบีบเท่าความกว้างจอ ล็อคจึงทะลุออกนอกเส้นขอบการ์ด
(2) `PM_MOBILE_KIT` ตั้ง `body{overflow-x:hidden}` ส่วนที่เกินจึงถูก**ตัดทิ้ง** เลื่อนนิ้วไปดูไม่ได้

**วิธีแก้:** บล็อค `PM_MAPFIT` — ให้การ์ดขยายตามเนื้อหาจริง (`width:max-content; min-width:100%`)
แล้วปล่อยให้ `.map-area` (ซึ่งมี `overflow:auto` อยู่แล้ว) เป็น **ตัวเลื่อนตัวเดียว** ของทั้งผัง
พร้อมถอด `style="overflow-x:auto"` ที่ครอบ grid รายโซนฝั่ง THU ออก (กันกล่องเลื่อนซ้อนสองชั้น
และทำให้พฤติกรรมสองตลาดเหมือนกัน)

⚠️ `.bld` / `.map-area` / `.zone-map` มีเฉพาะในไฟล์ผังตลาด (ตรวจแล้วไม่มีในอีก 18 ไฟล์)
จึงเขียนเป็นตัวเลือกตรง ๆ ได้ **ห้ามย้ายกฎก้อนนี้ไป `inject_mobile_kit.py`** เพราะสคริปต์นั้น
ฝังลงทุกไฟล์ — จะซ้ำรอยบั๊ก 14.1 ทันที (กฎเหล็กข้อ 12)

**ด่านตรวจใหม่:** `tools/test_map_reach.js` — ตรวจทุกล็อค ทุกโซน ที่ 360/390/430px ว่าเลื่อนไปหาได้จริง
โดยไล่หา ancestor ที่เลื่อนได้จริง **จงใจไม่ใช้ `scrollIntoView()`** เพราะมันเลื่อนได้แม้ `overflow:hidden`
ซึ่งนิ้วผู้ใช้ทำไม่ได้ (ถ้าใช้จะได้ผลลัพธ์ "ผ่านหมด" แบบหลอก ๆ)

### 15.4 บั๊ก D — ย้ายล็อคไม่ถูกบันทึกลงฐานข้อมูลเลย (ทั้งจรและประจำ ทั้งสองตลาด)

ผู้ดูแลตลาดขอ "ปุ่มย้ายล็อคจร" — ตรวจแล้วพบว่าปุ่ม "ย้ายล็อค" **มีอยู่แล้ว** แต่ใช้จริงไม่ได้:

```bash
$ sed -n '/^function confirmMove/,/^}/p' pamai_market_map.{TUE,THU}.html | grep -c apiPost
0        # ← ศูนย์ครั้ง ทั้งสองไฟล์
```

ฟังก์ชันนั้นแค่สลับ `dataset` ใน DOM และเขียน `localStorage` — รีเฟรชแล้วเด้งกลับ เครื่องอื่นไม่เห็น
และกรณีผู้ค้า **ประจำ** ยังทำให้ชีต `vendors` ผูกล็อคเดิม บิลค่าเช่า/ค่าไฟเดือนถัดไปจึงออกผิดล็อค
— **บั๊กตระกูลเดียวกับ v2.2 ข้อ 2 ที่เหลือตกค้างอยู่อีกจุด**

**สิ่งที่ทำใน v2.5:**

- backend action ใหม่ **`moveDailyBooking`** — ยกเลิกแถวเดิม (ธง `cancelled` ไม่ลบข้อมูล ตามกฎเหล็กข้อ 6)
  + เขียนแถวใหม่ + ซิงก์ `assigned_lock` ในชีต `floating_queue` ถ้ารายการนั้นมาจากคิว
- backend action ใหม่ **`moveVendorLock`** — ย้ายผู้ค้าประจำเป็นขั้นตอนเดียวที่ backend
  (ถ้าให้หน้าเว็บยิงสองคำสั่งแล้วคำสั่งที่สองพลาด จะได้ผู้ค้าคนเดียวกันอยู่สองล็อค = บิลออกซ้ำ)
- **ลำดับที่ห้ามสลับ:** เขียนแถวใหม่ **ก่อน** ยกเลิกแถวเดิมเสมอ · ถ้าขั้นที่สองพลาดให้ย้อนขั้นแรกทิ้ง
  (ลำดับกลับกันจะทำให้ผู้ค้าจรหายทั้งรายการเมื่อพังกลางทาง)
- ตรวจกันย้ายทับล็อคที่มีคนอยู่แล้ว **ที่ backend ด้วย** ไม่เชื่อหน้าเว็บอย่างเดียว
  (หน้าเว็บถูก bypass ได้ และผู้จัดสองคนอาจกดพร้อมกันคนละเครื่อง)
- ฝั่งหน้าเว็บ: `confirmMove()` เป็น async · **ห้ามแตะ DOM ก่อนได้ `status==='ok'`** ·
  ปุ่มขึ้น "กำลังย้าย…" ระหว่างรอ · ล้มเหลวแล้วขึ้น toast แดงและไม่เปลี่ยนอะไรบนจอ
- แยกเป็นสองปุ่มตามที่สั่ง: **"ย้ายล็อคจร"** แสดงเฉพาะล็อคที่ขายจรแล้ว · **"ย้ายผู้ค้าประจำ"**
  แสดงเฉพาะล็อคที่มีผู้ค้าประจำ · ล็อคที่มีทั้งสองอย่างเห็นทั้งสองปุ่ม

⚠️ **กับดักที่เจอตอนทดสอบ:** ห้ามเรียก `updateExpireBanner()` ต่อท้าย `saveMarketState()` ใน
`confirmMove()` — ถ้าวันที่กดย้ายห่างจากวันตลาดเกิน `STATE_EXPIRE_DAYS` (3 วัน) ฟังก์ชันนั้นจะเรียก
`clearMarketState()` ล้าง `dailyBookings` ทั้งก้อนทันทีที่ย้ายเสร็จ — ฐานข้อมูลถูกต้องแต่หน้าจอว่างเปล่า

### 15.5 ช่องโหว่ความปลอดภัยที่พบเอง (ผู้ใช้ไม่ได้แจ้ง)

`core/pamai_apps_script.js` ยังฝัง **รหัสผ่านจริงของทีมงาน 4 บัญชีเป็น plain text** ในฟังก์ชัน
`initSheets()` ทั้งที่ไฟล์นี้ถูก build ลง `dist/` แล้วอัปขึ้น GitHub **public repo** ทุกครั้ง
— บั๊ก #20 เคยแก้ปัญหาเดียวกันที่ `pamai_app.html` ไปแล้วเมื่อ 14 ก.ค. 2026 **แต่ตกหล่นไฟล์นี้**

**แก้:** สุ่มรหัสผ่านด้วย `_randomPassword()` ตอนสร้างชีต แล้วคืนค่ากลับใน response ของ `initSheets`
**ครั้งเดียว** ไม่เก็บลงโค้ด

> 🔴 **การลบโค้ดไม่พอ** — รหัสเดิมอยู่ใน git history ของ public repo ไปแล้ว
> **ต้องเปลี่ยนรหัสผ่านจริงของทั้ง 4 บัญชีทันที** ผ่านหน้า "เปลี่ยนรหัสผ่าน" ในแอพ

### 15.6 สรุปสิ่งที่เพิ่มเข้าด่านตรวจหลังรอบนี้

| เพิ่มอะไร | จับอะไรได้ที่ของเดิมจับไม่ได้ |
|---|---|
| `check_html_structure()` ใน `check_js.py` | แท็กเปิดที่ไม่มีตัวปิด / แท็กปิดที่ไม่มีตัวเปิด — บั๊ก A รอดทุกด่านมาได้เพราะไม่มีใครตรวจเรื่องนี้ |
| `tools/test_map_reach.js` | ล็อคที่ถูก `overflow-x:hidden` ตัดทิ้งจนแตะไม่ได้ — ไม่ "ล้น" จึงรอด `test_responsive.js` |
| `tools/test_offline_map.js` | ผังที่แสดงว่า "ว่าง" ทั้งที่โหลดข้อมูลไม่สำเร็จ |
| ทดสอบ end-to-end การย้ายล็อคกับ mock backend | การกระทำที่เปลี่ยนหน้าจอแต่ไม่เปลี่ยนฐานข้อมูล (บั๊ก D) |

**บทเรียนรวมของรอบนี้:** บั๊กสามในสี่เรื่องคือ **ความล้มเหลวที่เงียบ** — ไม่มี error ไม่มีหน้าขาว
ด่านตรวจอัตโนมัติผ่านหมด แต่ผู้ใช้จริงได้ข้อมูลผิด ด่านตรวจที่ดีจึงต้องถามว่า
**"ถ้าสิ่งนี้พัง ผู้ใช้จะรู้ไหม"** ไม่ใช่แค่ "มันพังหรือเปล่า"

### 15.7 บทเรียนจากการย้ายฐานข้อมูลไป Supabase (28 ก.ค. 2026)

การย้ายฐานข้อมูลถูกออกแบบให้ "แตะที่เดียว" คือเปลี่ยนไส้ใน `apiGet`/`apiPost` เท่านั้น
ระดับ SQL ทดสอบผ่านหมด (อ่าน 18 เคส เขียน 23 เคส) แต่พอเอา **หน้าเว็บจริงมารันในเบราว์เซอร์จริง**
กลับเจออีกสามเรื่องที่ระดับ SQL ไม่มีทางเห็น:

| เจออะไร | ทำไมการทดสอบระดับ SQL มองไม่เห็น |
|---|---|
| ผังขึ้นว่างทั้งตลาด เพราะ `pmFetchVendors()` ยิง `fetch` ไป Apps Script เอง | ฟังก์ชันนั้นไม่ได้เรียก `api()` เลย จึงไม่มีอะไรให้เทสต์ SQL จับ |
| ล็อกอินบัญชีจริงไม่ผ่าน แล้ว **ตกไปใช้บัญชีสาธิตที่ฝังในไฟล์** | `verifyUser` ทำงานถูกต้องทุกประการ ปัญหาอยู่ที่เงื่อนไข `if(apiUrl)` ก่อนหน้านั้น |
| ฟีเจอร์ถูกปิดเงียบ ๆ 89 จุด | เป็นเงื่อนไขในหน้าเว็บล้วน ๆ ไม่แตะฐานข้อมูล |

**กฎที่ได้:** เวลาย้ายแบ็กเอนด์ ให้ค้นหาคำที่แทน "ของเดิม" ทั้งโปรเจกต์ก่อนเสมอ
(ที่นี่คือ `getApiUrl` และ `pm-*-api-url`) แล้วถามทีละจุดว่า *"บรรทัดนี้ถามหา URL จริง ๆ
หรือกำลังถามว่าเชื่อมต่อฐานข้อมูลได้หรือยัง"* — สองอย่างนี้เคยเป็นเรื่องเดียวกัน หลังย้ายแล้วไม่ใช่

**และเรื่องที่ผู้ใช้ไม่ได้แจ้งแต่ต้องแก้:** ข้อมูลเก่ามีแท็ก `<svg>` ปนอยู่ในช่อง "วิธีชำระ" 2 แถว
ซึ่งหน้ารายงานเอาไปต่อกับ `innerHTML` — เป็นช่องโหว่ stored XSS ที่ติดมากับข้อมูล ไม่ใช่ติดมากับโค้ด
ปิดด้วย trigger ที่ฐานข้อมูล (0006) และหน้าสถานะสาธารณะเลิกบอกว่าล็อคไหนค้างชำระ (0007)

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

---

## 16. ย้ายฐานข้อมูลไป Supabase v3.0 — สถานะ สิ่งที่แก้ และงานที่เหลือ (เพิ่ม 28 ก.ค. 2026)

> **อ่านหัวข้อนี้ก่อนแตะอะไรที่เกี่ยวกับฐานข้อมูลทุกครั้ง** — ตอนนี้ระบบอยู่ใน "ช่วงเปลี่ยนผ่าน"
> คือมีฐานข้อมูลสองที่พร้อมใช้งานทั้งคู่ (Google Sheets ของเดิม และ Supabase ของใหม่)
> และเลือกได้ว่าจะใช้ที่ไหนโดยไม่ต้อง deploy ใหม่ ถ้าไม่รู้ว่าเครื่องหนึ่งกำลังใช้ที่ไหนอยู่
> ให้เปิด console แล้วพิมพ์ `PM_API.mode()`

### 16.1 ทำไมต้องย้าย และเลือกสถาปัตยกรรมแบบไหน

ผู้ดูแลตลาดแจ้งว่า "โหลดข้อมูลจาก Google Sheets ช้ามาก" (จริง — Apps Script ใช้เวลา 2–4 วินาที
ต่อคำสั่งหนึ่งครั้ง) และต้องการรองรับข้อมูลไปอีก 3–5 ปี

**เลือกทางเลือก A: แยกเป็น 2 โปรเจกต์ Supabase ตลาดละ 1 โปรเจกต์** ตามที่ผู้ดูแลตลาดตัดสินใจ
เหตุผลเดียวกับกฎเหล็กข้อ 5 — สองตลาดต้องแยกฐานข้อมูลขาดจากกันเสมอ ต่อให้ต้องดูแลสองที่ก็ยอม
เพราะถ้ารวมกันแล้วเผลอ query ผิดตลาดครั้งเดียว ความเสียหายคือข้อมูลเงินของผู้ค้าจริง

| | ตลาดวันอังคาร (TUE) | แพไม้มินิวันพฤหัสบดี (THU) |
|---|---|---|
| Project ref | `jrsuxrzyxiqqvzjftpms` | `atvxajfwazrswdlhnosh` |
| ภูมิภาค | ap-northeast-1 | ap-southeast-1 |
| สถานะข้อมูล | ยังว่าง (รอย้ายตอนตัดจริง) | ย้ายข้อมูลจริงเข้าครบแล้ว |
| คีย์ในหน้าเว็บ | publishable key ของ TUE | publishable key ของ THU |

**publishable key ใส่ในโค้ดบน public repo ได้โดยไม่เป็นช่องโหว่** เพราะทุกตารางถูกถอนสิทธิ์
จาก role `anon` แล้ว (migration 0002) ทางเข้าเดียวคือฟังก์ชัน `api()` ซึ่งบังคับกฎธุรกิจไว้ในตัว
**ห้ามนำ `service_role` key มาใส่ที่ใดทั้งสิ้น — ไม่มีขั้นตอนไหนของระบบนี้ต้องใช้**

### 16.2 สถาปัตยกรรมฝั่งโค้ด — ทำไมย้ายฐานข้อมูลได้โดยไม่ต้องแก้หน้าเว็บ 20 ไฟล์

ตรวจโค้ดแล้วพบว่า **ทุกไฟล์เรียก backend ผ่าน `apiGet()` / `apiPost()` เท่านั้น รวม 119 จุด**
ไม่มีไฟล์ไหน `fetch` ข้ามสองฟังก์ชันนี้ (ยกเว้นจุดเดียวที่เป็นบั๊ก — ดู 16.5)
จึงเปลี่ยนแค่ "ไส้ใน" ของสองฟังก์ชันนี้ก็ย้ายฐานข้อมูลได้ทั้งระบบ

`tools/inject_api_client.py` (marker `PM_API_CLIENT`) ฝังชุดเดียวลงทุกไฟล์ core และรองรับ **3 โหมด**
เก็บใน `localStorage` คีย์ `pm-tue-backend` / `pm-thu-backend`:

| โหมด | อ่านจาก | เขียนลง | ใช้เมื่อ |
|---|---|---|---|
| `sheets` (ค่าเริ่มต้น) | Google Sheets | Google Sheets | ตอนนี้ — พฤติกรรมเดิมเป๊ะ |
| `parallel` | Supabase | **ทั้งสองที่** | ช่วงทดสอบคู่ขนาน 2 วันตลาด |
| `supabase` | Supabase | Supabase | หลังตัดมาใช้จริง |

```js
PM_API.mode()                       // ดูว่าตอนนี้ใช้ที่ไหน
PM_API.setMode('parallel')          // สลับ แล้วรีเฟรชหน้า
await PM_API.compare('getPayments', {date:'23 ก.ค. 2569'})   // เทียบผลสองฝั่ง
```

**ค่าเริ่มต้นเป็น `sheets` โดยเจตนา** — deploy ไฟล์ชุดนี้ขึ้นไปแล้วผู้ใช้จะไม่รู้สึกถึงความเปลี่ยนแปลง
ใด ๆ จนกว่าจะสั่งสลับเอง และ **ค่านี้อยู่ในเบราว์เซอร์ของแต่ละเครื่อง** ดังนั้นตอนตัดจริงต้องไล่สั่ง
ทีละเครื่องที่ทีมงานใช้ (ข้อดีคือทดลองทีละเครื่องได้ ข้อเสียคือลืมเครื่องไหนเครื่องนั้นยังใช้ของเก่า)

### 16.3 ฝั่งฐานข้อมูล — ประตูเดียวคือฟังก์ชัน `api()`

ทุก action วิ่งผ่าน `POST /rest/v1/rpc/api` ด้วย body `{action, payload}` เข้าฟังก์ชัน
`api(action text, payload jsonb) returns jsonb` ตัวเดียว (33 action) ซึ่งเป็น `security definer`
คืนค่ารูปแบบ `{"status":"ok","data":…}` เหมือนที่ Apps Script เคยคืน หน้าเว็บจึงไม่รู้สึกว่าเปลี่ยน

ข้อดีเชิงความปลอดภัยของการมีประตูเดียว: ต่อให้ key หลุด ผู้ที่ได้ key ไปก็อ่าน/เขียนตารางเองไม่ได้
ทำได้แค่เรียก action ที่เราเปิดไว้ ซึ่งบังคับกฎธุรกิจไว้ในตัวแล้ว (เช่น หนึ่งล็อคขายจรได้รายเดียวต่อวัน)

**ไฟล์ migration ที่ `supabase/migrations/` คือแหล่งความจริงแหล่งเดียวของฝั่งฐานข้อมูล**
(หลักการเดียวกับ `core/` ของฝั่งหน้าเว็บ) ทั้งสองโปรเจกต์ใช้ SQL ชุดเดียวกันเป๊ะ:

| ไฟล์ | ทำอะไร |
|---|---|
| `0001_paemai_core_schema_v3.sql` | 11 ตาราง + index + constraint (วันที่เก็บเป็น ค.ศ. เสมอ) |
| `0002_rls_and_auth.sql` | RLS, ถอนสิทธิ์ทุกตารางจาก anon, view สาธารณะ, `verify_user`, `change_password` (bcrypt) |
| `0003_th_normalizers.sql` | `th_date` / `th_phone` / `th_time` — ยาแก้บั๊กตระกูล #28 / #29 / #32 |
| `0004_import_thursday_data.sql` | ย้ายข้อมูลจริงฝั่งวันพฤหัสฯ พร้อม dedupe |
| `0005_api_dispatcher.sql` | ฟังก์ชัน `api()` 33 action + `th_fmt` / `date_matches` / `_auto_log` |
| `0006_sanitize_text.sql` | trigger ตัดแท็ก HTML ก่อนลงตาราง + ล้างข้อมูลเก่าที่ปนมา |
| `0007_public_status_mask_unpaid.sql` | หน้าสาธารณะไม่บอกว่าล็อคไหนค้างชำระ |
| `0011_persist_unpaid_and_block_termination.sql` | คงสถานะค้างชำระจนปิดยอด และห้ามยกเลิกการเช่าขณะยังมียอดค้าง |
| `0012_cancel_daily_booking_sync_queue.sql` | ยกเลิกล็อคจรจากผังแล้วปิดรายการขายใน `floating_queue` พร้อมกัน |
| `0013_public_status_unified_source.sql` | รวมข้อมูลหน้าสาธารณะให้มาจาก Supabase ชุดเดียวกับแอพ พร้อมราคา คิว ลา/ขาด ค้างชำระ และกฎตลาด |

> ⚠️ **บทเรียน:** ไฟล์ `0001` และ `0003` เคยเป็นไฟล์เปล่ามีแต่คอมเมนต์ เพราะตอนย้ายส่ง SQL เข้าไป
> ตรง ๆ ผ่านเครื่องมือแล้วลืมเขียนกลับลงไฟล์ — ถ้าโปรเจกต์เสียหายแล้วต้องสร้างใหม่ จะสร้างไม่ได้เลย
> ตอนนี้ดึงจาก `supabase_migrations.schema_migrations` กลับมาเติมครบแล้ว และเทียบ md5 ของทุกฟังก์ชัน
> กับที่รันจริงบนคลาวด์แล้วตรงกันทั้ง 11 ตัว **กฎใหม่: apply SQL ขึ้นคลาวด์เมื่อไร ต้องมีไฟล์ในโฟลเดอร์
> migrations เมื่อนั้น ห้ามมีอะไรอยู่บนคลาวด์ที่ไม่มีในไฟล์**

### 16.4 ข้อมูลและบัญชีผู้ใช้ที่ย้ายเข้าไปแล้ว (ฝั่งวันพฤหัสบดี)

| ตาราง | ในชีต | ใน Supabase | หมายเหตุ |
|---|---|---|---|
| vendors | 115 | 115 | ครบ |
| payments | 234 | 234 | ครบ |
| activity_log | 202 | 202 | ครบ |
| floating_queue | 7 | 7 | ครบ |
| leave_log | 13 | 9 | ยุบซ้ำตามกฎ "1 ล็อค 1 วัน 1 รายการ" |
| daily_bookings | 7 | 7 | A4 ซ้ำ 5 แถว → เก็บล่าสุด 1 ที่เหลือติดธง `cancelled` **ไม่ลบทิ้ง** (กฎเหล็กข้อ 6) |

บัญชีผู้ใช้ตามที่ผู้ดูแลตลาดสั่ง (รหัสผ่านเก็บเป็น bcrypt cost 12 — **ไม่มีรหัสผ่านอยู่ในโค้ดหรือในเอกสาร**):

| ตลาด | username | สิทธิ์ |
|---|---|---|
| ทั้งสองตลาด | `paemai` | admin |
| วันพฤหัสบดี (เพิ่ม) | `game123` | admin |

บัญชีเดิมทั้งหมด (`tony2568`, `fon12345`, `too56789`, `aew98765`, `game1234`) ถูกลบแล้วตามคำสั่ง

### 16.5 บั๊กที่เจอตอนทดสอบ end-to-end และแก้แล้ว

ระดับ SQL ทดสอบผ่านหมด (อ่าน 18 เคส · เขียน 23 เคส) แต่พอเอา **หน้าเว็บจริงมารันในเบราว์เซอร์จริง**
ในโหมด `supabase` ล้วน กลับเจออีก 6 เรื่อง — รายละเอียดเชิงบทเรียนอยู่ที่หัวข้อ 15.7:

| อาการ | ต้นเหตุ | แก้ที่ | กฎเหล็กที่เกิดจากเรื่องนี้ |
|---|---|---|---|
| ผังตลาดขึ้น "ว่าง" ทั้งตลาด | `pmFetchVendors()` ยิง `fetch` ไป Apps Script เอง ไม่ผ่าน `apiGet` | `tools/patch_market_maps.py` | ข้อ 19 |
| **ล็อกอินบัญชีจริงไม่ผ่าน แล้วตกไปใช้บัญชีสาธิตในไฟล์** | `doLogin()` อ่าน `localStorage['pm-*-api-url']` ตรง ๆ | `tools/inject_api_client.py` | ข้อ 18 |
| ฟีเจอร์ถูกปิดเงียบ ๆ 89 จุดใน 20 ไฟล์ | `if(!getApiUrl())` ใช้ URL ของ Apps Script เป็นตัวตัดสิน | ครอบ `getApiUrl()` ในชิมชุดเดียว | ข้อ 18 |
| แก้สคริปต์ `tools/` แล้วรันใหม่ไม่มีอะไรเปลี่ยน | `add_vendor_sync()` เป็นแบบ "เจอ marker แล้วข้าม" | `tools/patch_market_maps.py` | ข้อ 20 |
| `payments.method` 2 แถวเก็บ `<svg>` แล้วรายงานเอาไปต่อ `innerHTML` (stored XSS) | ข้อมูลสกปรกตกค้างจากยุค Sheets | migration `0006` | ข้อ 21 |
| หน้าสถานะสาธารณะบอกว่าล็อคไหนค้างชำระ | `getPublicStatus` ส่ง status ดิบออกไป | migration `0007` | — |

### 16.6 ชุดทดสอบใหม่ 2 ตัว (เพิ่มเข้าด่านตรวจ)

| ไฟล์ | ตรวจอะไร |
|---|---|
| `tools/test_e2e_supabase.js` | เปิดหน้าเว็บใน `dist/` จริงในเบราว์เซอร์จริง **โหมด supabase ล้วนและไม่ตั้งค่า Apps Script เลย** แล้วล็อกอิน · เปิดผัง · ดึงรายงาน · ขายล็อคจร · ย้ายล็อคจร · เปิดหน้าสาธารณะ · เปิดครบ 10 หน้า — ถ้ามีโค้ดส่วนไหนยังแอบเรียก Sheets อยู่จะพังทันที |
| `tools/test_sheets_guard.js` | ยืนยันว่าโหมดเริ่มต้นยังคุยกับ Apps Script เหมือนเดิมทุกประการ และ **สลับกลับได้ทันที** (ทางถอยใช้งานได้จริง ไม่ใช่แค่ในเอกสาร) |

```bash
node tools/test_e2e_supabase.js thu     # และ tue
node tools/test_sheets_guard.js
```

`test_e2e_supabase.js` ต้องมี PostgreSQL ในเครื่อง (ดักคำขอ `/rest/v1/rpc/api` เข้าฐานข้อมูลในเครื่อง
ที่ลง migration ชุดเดียวกัน) วิธีตั้งฐานทดสอบและข้อมูลจำลองอยู่ที่ `tools/seed_test_db.sql`
และ `supabase/SUPABASE-STATUS.md`

> **ทำไมไม่ยิงไป Supabase จริง:** เครื่องที่ใช้พัฒนารอบนี้ออกอินเทอร์เน็ตได้เฉพาะโดเมนในรายการอนุญาต
> ซึ่งไม่มี `*.supabase.co` จึงยืนยันความเหมือนด้วยการเทียบ md5 ของโค้ดฟังก์ชันทั้ง 11 ตัวแทน
> **สิ่งที่ชุดนี้ยังไม่ครอบคลุมและต้องดูตอน deploy จริง: TLS · CORS · สิทธิ์ของ role `anon`**

### 16.7 งานที่ยังไม่ได้ทำ (เรียงตามลำดับที่ต้องทำ)

| # | งาน | ใครทำ | หมายเหตุ |
|---|---|---|---|
| 1 | deploy `dist/` v2.5 ขึ้น GitHub Pages ทั้งสองตลาด | ผู้ดูแล | ยังเป็นโหมด `sheets` → ผู้ใช้ไม่รู้สึกถึงความเปลี่ยนแปลง |
| 2 | push `.github/workflows/supabase-keepalive.yml` + กด Run ให้เห็นเขียว | ผู้ดูแล | **ต้องทำงานอยู่ก่อนข้อ 4 ห้ามสลับลำดับ** |
| 3 | push `.github/workflows/supabase-backup.yml` (ควรอยู่ repo **private**) + ใส่ secret 6 ตัว | ผู้ดูแล | `SUPABASE_URL_*` · `SUPABASE_ANON_*` · `SUPABASE_DB_URL_*` — **ห้ามใส่ service_role** |
| 4 | ทดสอบคู่ขนาน 2 วันตลาด แล้วเทียบตัวเลขรายงานทุกฉบับ | ผู้ดูแล + ทีมงาน | `PM_API.setMode('parallel')` แล้วใช้งานตามปกติ |
| 5 | ย้ายข้อมูลจริงฝั่ง **วันอังคาร** เข้า Supabase | AI | ยังไม่ทำเพราะตอนนั้นชีตวันอังคารยังไม่มีข้อมูล — ทำก่อนตัดจริงฝั่งนี้ |
| 6 | ตัดมาใช้ Supabase จริง (`PM_API.setMode('supabase')` ทุกเครื่อง) | ผู้ดูแล | เก็บ Apps Script + ชีตไว้เป็นทางถอยอีก 1 เดือน |
| 7 | เปลี่ยนชื่อโปรเจกต์ Supabase ฝั่งวันอังคารเป็น `paemai-market-tue` | ผู้ดูแล | ทำในหน้าเว็บ Supabase (Settings → General) |
| 8 | แก้ข้อความในแอพที่ยังเขียนว่า "Google Sheets" ให้เป็น "ฐานข้อมูล" | AI | ปุ่มตั้งค่าที่หน้าล็อกอิน ฯลฯ — ทำหลังตัดจริงแล้ว |
| 9 | ตัดสินใจเรื่องบัญชีสาธิต `demo_admin` / `demo_viewer` ใน `core/pamai_app.html` | ผู้ดูแล | ตอนนี้ใช้ได้เฉพาะตอนไม่มีฐานข้อมูลเลย แต่รหัสยังอยู่ใน public repo |

ขั้นตอนละเอียดพร้อมช่องติ๊กอยู่ที่ `DEPLOY-v2.5-checklist.md` หัวข้อ 6

### 16.8 ทางถอย (ต้องรู้ก่อนตัดจริง)

1. เปิด console ที่หน้าแอพ → `PM_API.setMode('sheets')` → รีเฟรช = กลับไปใช้ Google Sheets ทันที
   (ทดสอบแล้วด้วย `tools/test_sheets_guard.js` ทั้งสองตลาด)
2. **ห้ามลบ Apps Script deployment หรือ Google Sheet เดิมอย่างน้อย 1 เดือนหลังตัด** — และก่อนลบ
   ต้องมั่นใจว่า cron สำรองข้อมูลรันเขียวติดกันครบ 7 วันแล้ว
3. ข้อมูลที่เกิดขึ้นหลังตัดจะอยู่ใน Supabase ที่เดียว ถ้าถอยกลับไป Sheets ข้อมูลช่วงนั้นจะไม่ตามไปด้วย
   → ถ้าถอยจริงต้องคัดลอกรายการที่เกิดใหม่กลับมือ (อีกเหตุผลที่ต้องผ่านโหมด `parallel` ให้ครบ 2 วันตลาดก่อน)

### 16.9 ข้อจำกัดของแพลนฟรีที่ต้องอยู่กับมันให้ได้

| ข้อจำกัด | ผลกับตลาด | สิ่งที่ทำไปแล้ว |
|---|---|---|
| โปรเจกต์ถูกหยุดถ้าไม่มีคำขอ 7 วัน | ตลาดเปิดสัปดาห์ละครั้ง = เฉียดเส้นตลอด | cron ping รายวัน (ข้อ 16.7 ข้อ 2) |
| ไม่มี backup ให้ (version history ของ Sheets หายไป) | เผลอลบแล้วไม่มีอะไรให้ย้อน | cron `pg_dump` รายวัน เก็บ 90 วัน |
| พื้นที่ 500 MB / เดือนละ 5 GB ขาออก | ข้อมูลปัจจุบันไม่ถึง 1% ของโควตา | ประเมินแล้วพอใช้เกิน 5 ปี ถ้าไม่เก็บรูปภาพ |

> ⚠️ สคริปต์ cron ที่ร่างไว้ตอนแรกในแผน v3.0 **ใช้ไม่ได้จริงทั้งสองตัว** และแก้แล้วในไฟล์ที่ส่งมอบ:
> ตัว keep-alive เดิมยิงไปที่ `/rest/v1/<ตาราง>` ซึ่งถูกถอนสิทธิ์จาก anon ไปแล้ว จะได้ 401 ทุกวัน ·
> ตัว backup เดิมใช้ `pg_dump` ที่มากับ ubuntu (รุ่น 16) ซึ่งปฏิเสธการทำงานกับเซิร์ฟเวอร์รุ่น 17
> — ทั้งสองอย่างนี้จะ "พังทุกวันจนคนเลิกสนใจการแจ้งเตือน" ซึ่งอันตรายกว่าไม่มี cron เลย

### 16.10 กติกาสถานะค้างชำระและการยกเลิกการเช่า (แก้และตรวจจริง 29 ก.ค. 2569)

กติกาที่ถูกต้องของทั้งตลาดวันอังคารและวันพฤหัสบดีคือ **สถานะค้างชำระไม่มีวันหมดอายุ**
และไม่เกี่ยวกับรอบรีเซ็ต 3 วันของผังตลาด สถานะจะเปลี่ยนกลับเป็นปกติได้ต่อเมื่อรับชำระครบทุกส่วนแล้วเท่านั้น
(ค่าเช่า ค่าไฟ ค่าปรับ และยอดค้างอื่น) หากรับชำระเพียงบางส่วน ล็อคยังต้องแสดงสถานะค้างชำระต่อไป

- ปุ่ม `รีเซ็ตรายวัน` ล้างเฉพาะแจ้งลา ขาดล็อค และการขายล็อคจรของรอบตลาด ไม่แตะสถานะค้างชำระ
- ห้ามยกเลิกการเช่าหรือเปลี่ยนผู้ค้าเป็น `terminated` ขณะสถานะยังเป็น `unpaid` หรือยังมียอดค้าง
- หน้าแก้ไขผู้ค้าและผังตลาดตรวจซ้ำก่อนยกเลิก ส่วนฐานข้อมูลมี trigger จาก migration `0011` ป้องกันการข้ามหน้าจอ
- หน้ารับชำระเปลี่ยนสถานะกลับเป็นปกติเฉพาะเมื่อยอดที่รับปิดยอดครบ
- ฝั่ง Apps Script สำรองใช้กติกาเดียวกัน เพื่อไม่ให้สองโหมดทำงานต่างกัน

ผลตรวจ: ชุดทดสอบจำลองยืนยันว่าค้างชำระยังอยู่หลังเรียกรีเซ็ตรายวันในผังทั้งสองตลาด และชุดทดสอบผังทั้งหมดผ่าน
จากนั้นติดตั้ง migration `0011` สำเร็จใน Supabase ทั้งสองโปรเจกต์โดยไม่แก้ข้อมูลผู้ค้าจริง เผยแพร่ GitHub commit
TUE `66398ac` และ THU `8b64058` แล้วตรวจหน้า GitHub Pages จริง พบวันที่รอบตลาดถูกต้องและปุ่มแสดงเป็น
`รีเซ็ตรายวัน` ครบทั้งสองตลาด จึงบันทึกบั๊กนี้หลังการตรวจหน้าเว็บตามลำดับที่กำหนด

### 16.11 การยกเลิกและย้ายล็อคจรจากผัง (แก้และตรวจจริง 30 ก.ค. 2569)

อาการเดิมคือผังทั้งสองตลาดแสดงสถานะล็อคจร แต่กดยกเลิกแล้วฐานข้อมูลไม่เปลี่ยน และผู้ใช้หา
ปุ่มย้ายล็อคจรไม่พบ ต้นเหตุคือ `getPublicStatus` ตั้งใจไม่ส่งวัน/เวลาเพื่อจำกัดข้อมูลสาธารณะ แต่หน้าผัง
ส่งค่า `undefined` กลับไปให้ `cancelDailyBooking` ทำให้ค้นหาแถวตามวันตลาดไม่เจอ นอกจากนี้การยกเลิก
เดิมปิดเฉพาะ `daily_bookings` แต่ปล่อยรายการคู่กันใน `floating_queue` เป็น `sold` ค้างไว้

- ถ้า snapshot สาธารณะไม่มีวันที่ หน้าผังใช้วันตลาดจากนาฬิการะบบ (`marketDayTH()`) เป็นค่าอ้างอิง
- ปุ่ม `ย้ายล็อคจร` และ `ยกเลิกจร` แสดงเฉพาะเมื่อมีรายการขายจรของล็อคนั้นแล้วเท่านั้น
- migration `0012` ทำให้การยกเลิกจากผังปิดทั้ง `daily_bookings` และรายการ `floating_queue` ที่ตรงกับล็อค/วันตลาด
- ฝั่ง Google Apps Script สำรองใช้กติกาเดียวกันผ่าน `_cancelSoldQueueForDailyLock`
- `tools/test_map_statuses.js` จำลอง production contract ที่ไม่ส่งวัน/เวลา และตรวจปุ่ม วันตลาด และผลหลังยกเลิก

ผลตรวจหน้าเบราว์เซอร์จริง: จำลองขาย SA1 ในตลาดวันอังคารและ A5 ในตลาดวันพฤหัสฯ จากหน้าคิว
จากนั้นเปิดผัง พบปุ่มย้าย/ยกเลิกอย่างละ 1 ปุ่ม ขณะที่ล็อคว่างไม่มีปุ่มทั้งคู่ กดยกเลิกจากผังแล้วตัวนับจร
กลับเป็นศูนย์และรายการขายหายจากหน้าคิว ข้อมูลจำลองทั้งสองรายการถูกยกเลิกจนไม่ค้าง และคืนการตั้งเวลา
กลับเป็นเวลาอุปกรณ์แล้ว ก่อนบันทึกหัวข้อนี้

### 16.12 หน้าสถานะสาธารณะไม่แสดงข้อมูลและราคาขายเป็นศูนย์ (แก้และตรวจจริง 30 ก.ค. 2569)

ต้นเหตุหลักคือ `pamai_public_status.html` เรียก Google Apps Script จากพารามิเตอร์ `?api=` โดยตรง
แม้แอพหลักจะเปลี่ยนมาใช้ Supabase แล้ว จึงเกิดฐานข้อมูลคู่ขนาน: ผู้จัดเห็นสถานะในผัง แต่หน้าที่ส่งให้ผู้ค้า
ไม่เห็นคิว รายการขาย ลา/ขาด และค้างชำระ อีกจุดหนึ่งคือ Supabase ส่งเลขล็อคเป็น `lock_id` แต่หน้าเว็บอ่าน
`lock` และ endpoint เดิมไม่ส่ง `price`/`total` ทำให้ผังแสดงราคา `฿0`

การแก้ไขที่ใช้ร่วมกันทั้งสองตลาด:

1. หน้าสาธารณะเลือก backend ตาม `PM_API.mode()` เหมือนแอพหลัก; โหมด Supabase จะไม่ย้อนกลับไปอ่าน Sheets แม้ URL ยังมี `?api=`
2. migration `0013` ส่งข้อมูลสาธารณะที่อนุญาตครบชุด: ผู้ค้า เลขล็อค คิวรอ ขายจร ราคา ลา/ขาด เลขล็อคค้างชำระ และกฎตลาด โดยไม่ส่งเบอร์โทรหรือยอดค้าง
3. normalize เลขล็อคจาก `lock` / `lock_id` / `assigned_lock` และให้ผังใช้ `total` หรือ `price` เป็นค่า fallback
4. วันตลาดของหน้าสาธารณะใช้รอบเดียวกับผัง: หลังวันตลาดไม่เกิน 2 วันยังดูรอบเดิม และรองรับวันที่/เวลาที่ตั้งในเมนูระบบ
5. เรียงหัวข้อเป็น คิวรอ → ขายวันนี้ → ลา/ขาด → ไม่มีผู้ค้าประจำ → ค้างชำระ พร้อมหมายเหตุเรื่องทำความเข้าใจกฎเพื่อรักษาสิทธิผู้ค้าประจำ

ผลตรวจเบราว์เซอร์จริงก่อนเผยแพร่: ตลาดวันพฤหัสฯ สร้างรายการรอคิวและขาย A5 ราคา ฿350
หน้าสาธารณะแสดงคิว รายการขาย 8 รายการ ราคา ฿350 ล็อคลา/ขาด และกฎตลาด; ผัง A5 แสดง ฿350
จากนั้นยกเลิกจนยอดขายกลับเป็น 7 ตลาดวันอังคารสร้างรายการรอคิวและขาย SA1 ราคา ฿350
หน้าสาธารณะและผังแสดงตรงกัน แล้วลบข้อมูลทดสอบทั้งสองรายการจนไม่เหลือ พร้อมคืนเวลาเป็นเวลาอุปกรณ์
ชุด `npm run test:maps` ซึ่งรวม `test_public_status.js` ผ่านครบทั้งสองตลาด

เผยแพร่ GitHub แล้ว: TUE commit `20339a7` และ THU commit `2e97c87` จากนั้นเปิด GitHub Pages จริง
ยืนยันว่าทั้งสองหน้าเรียงหัวข้อ 1–5 และแสดงหมายเหตุใหม่ ตลาดพฤหัสฯ แสดงคิวรอจริง 1 รายการ
ขายจรจริง 7 รายการ ล็อคลา N5 และราคาขาย ฿1 จาก Supabase ส่วนผัง N6 แสดงราคา ฿1 ไม่กลับเป็นศูนย์
ตลาดวันอังคารโหลดหน้าใหม่จาก Supabase ได้โดยไม่ต้องมี `?api=` และโค้ดผังที่เผยแพร่มี price fallback แล้ว

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

## 17. บทเรียนจาก v2.6: "ด่านที่ปิดสวิตช์ตัวเอง" และรอบแก้หน้าสาธารณะ/คิวจอง (เพิ่ม 3 ส.ค. 2026)

ผู้ดูแลตลาดแจ้งมา 5 เรื่อง แต่พอไล่ต้นเหตุจริงกลับพบว่ามี **บั๊กที่ผู้ใช้ไม่ได้แจ้งและร้ายแรงกว่า**
ซ่อนอยู่ข้างใต้ 3 เรื่อง ซึ่งทำให้ **10 ไฟล์เชื่อมต่อฐานข้อมูลไม่ได้เลยตั้งแต่วันตัดมาใช้ Supabase**

### 17.1 บั๊ก A — shim ปิดสวิตช์ตัวเอง ทำให้ 10 หน้าไม่เคยเห็นข้อมูลจาก Supabase (ร้ายแรงที่สุดของรอบนี้)

**อาการที่ผู้ใช้แจ้ง:** "ประวัติแจ้งลา ข้อมูลยังไม่อัพเดตแม้ว่าจะมีการแจ้งลาแล้ว"

**ต้นเหตุจริง:** บรรทัดแรกของบล็อค `PM_API_CLIENT` (`tools/inject_api_client.py`) เขียนไว้ว่า

```js
if (typeof apiGet !== 'function' || typeof apiPost !== 'function') return;
```

หน้าที่ **อ่านอย่างเดียว** ไม่มีเหตุให้เขียนฟังก์ชัน `apiPost` เลย — `pamai_leave_history.html`
และรายงาน `01`–`09` รวม **10 ไฟล์** มีแต่ `apiGet` shim ทั้งก้อนจึง `return` ทิ้งตั้งแต่บรรทัดแรก ผลคือ

- `apiGet` ไม่ถูกครอบ → ยังยิงไป Google Apps Script ทั้งที่ทั้งระบบย้ายมา Supabase แล้ว
- `getApiUrl()` ไม่ถูกครอบ → คืนค่าว่าง (เครื่องใหม่ไม่มี URL ของ Apps Script)
  ทุกหน้าจึงตัดสินว่า "ยังไม่เชื่อมต่อ" แล้วขึ้นข้อความชวนไปตั้งค่า **โดยไม่มี error ให้เห็นแม้แต่บรรทัดเดียว**

เข้าเงื่อนไขกฎเหล็กข้อ 18 เป๊ะ ๆ อีกครั้ง — ต่างจากรอบก่อนตรงที่คราวนี้ **ตัวด่านเองเป็นคนปิดสวิตช์**

**แก้:** บังคับแค่ `apiGet` ส่วน `apiPost` ครอบเมื่อมีจริง

```js
if (typeof apiGet !== 'function') return;
var _get = apiGet;
var _post = (typeof apiPost === 'function') ? apiPost : null;
```

### 17.2 บั๊ก B — ครึ่งหลังของเรื่องเดียวกัน: "ลำดับเวลา" ไม่ใช่ "เงื่อนไข"

แก้ 17.1 แล้วรายงาน 01–09 **ยังว่างอยู่ดี** เพราะคนละสาเหตุ:

`PM_API_CLIENT` ถูกฝังไว้ท้าย `<body>` จึงทำงาน **หลัง** สคริปต์หลักของหน้าเสมอ
หน้าที่โหลดข้อมูลตอน `window.onload` ไม่มีปัญหา (onload มาทีหลัง shim)
แต่รายงานเรียก `render()` แบบ inline ท้ายสคริปต์ของตัวเอง ซึ่งเกิด **ก่อน** shim ติดตั้ง
ตอนนั้น `getApiUrl()` ยังเป็นตัวเดิมที่คืนค่าว่าง → `render()` ขึ้น "ยังไม่ได้เชื่อมต่อ" แล้วจบ
และไม่มีอะไรเรียกซ้ำอีกเลย รายงานจึงว่างถาวรจนกว่าผู้ใช้จะกดปุ่มรีเฟรชเอง

**แก้:** เพิ่ม `kickIfNeverFetched()` ใน shim — ถ้าหลังหน้าโหลดเสร็จแล้ว **ยังไม่เคยมีคำขอผ่าน shim เลย**
แปลว่าหน้านั้นตัดสินใจตอนที่ยังไม่มีข้อมูลจริง → สั่งโหลดใหม่ให้ 1 ครั้ง
(เช็คธง `_everFetched` จึงไม่ทำให้หน้าที่ทำงานถูกอยู่แล้วยิงซ้ำ)

> **บทเรียน:** โค้ดที่ "ฝังท้ายไฟล์แล้วครอบฟังก์ชันของหน้า" ต้องคิดเรื่องลำดับเวลาเสมอ
> การครอบสำเร็จไม่ได้แปลว่าทันใช้งาน — หน้าอาจตัดสินใจไปแล้วก่อนที่เราจะครอบ

### 17.3 บั๊ก C — หน้าสาธารณะไม่เคยปล่อยล็อคที่ยกเลิกการเช่าออกเป็นล็อคว่าง

**อาการที่ผู้ใช้แจ้ง:** "ล็อคว่างไม่มีผู้ค้าประจำ ไม่อัพเดต"

`pamai_public_status.html` สร้างชุดล็อคที่ "มีผู้ค้าประจำ" แบบนี้

```js
const vendorLockSet = new Set((data.vendors||[]).map(v => v.lock));   // ไม่ดู status เลย
```

แต่ backend สองตัวส่งไม่เหมือนกัน:

| backend | ล็อคที่ยกเลิกการเช่าแล้ว |
|---|---|
| Supabase (`migration 0013`) | **ส่งมาด้วย** โดยแปลง `status` เป็น `'vacant'` |
| Apps Script | กรอง `terminated` ทิ้งไปเลย |

ในโหมด Supabase (โหมดที่ใช้จริง) ล็อคที่ยกเลิกการเช่าแล้วจึงถูกนับว่า "ยังมีผู้ค้าประจำ" ตลอดไป

**แก้:** ตัดสินจาก `status` เสมอที่หน้าเว็บ → ถูกต้องทั้งสอง backend โดยไม่ต้องรอ deploy ฝั่งใดฝั่งหนึ่ง
พร้อม normalize `lock` / `lock_id` / `assigned_lock` เหมือนที่ทำกับ `sold` ไปแล้วในหัวข้อ 16.12

### 17.4 บั๊ก D — `floating_queue` ไม่มีคอลัมน์ `line` และ `note` เลย

ตกหล่นตอน migrate มา Supabase (ฝั่ง Google Sheets มีสองคอลัมน์นี้มาตลอด)
ค่า LINE ID และหมายเหตุที่ผู้จัดคีย์ลงไปจึงถูกทิ้งเงียบ ๆ ทุกครั้ง
และช่องหมายเหตุคิวบนหน้าสาธารณะไม่เคยมีข้อความแสดงเลย — `migration 0014` เติมคอลัมน์และต่อสายให้ครบ

### 17.5 สิ่งที่แก้ตามคำสั่งผู้ดูแลตลาด (5 เรื่อง)

| # | คำสั่ง | สิ่งที่ทำ |
|---|---|---|
| 1 | ล็อคว่างไม่มีผู้ค้าประจำ ไม่อัปเดต | ดูหัวข้อ 17.3 |
| 2 | แยกหมวดแต่ละโซนให้ชัดเจน + โซนขึ้นต้น T ใส่หมายเหตุ | จัดกลุ่ม chip ตามโซนทั้ง 3 ช่อง (ลา-ขาด · ไม่มีผู้ค้าประจำ · ค้างชำระ) พร้อมนับจำนวนต่อโซน · หมายเหตุ `โซนเปิดท้าย ของใช้ เสื้อผ้า สินค้าอุปโภค` ติดที่โซนที่ตรงกับ `/^T[A-Z]/` เท่านั้น (TA/TB/TC) — **โซน `T` เดี่ยว ๆ ข้างโลตัสไม่เข้าเงื่อนไข ตามที่ผู้ดูแลยืนยัน** และตลาดวันอังคารไม่มีโซนขึ้นต้น T จึงไม่ได้รับผล |
| 3 | เอาป้าย "ผู้จัดขายตรงไม่ผ่านคิว" · หมายเหตุ "แสดงเฉพาะหมายเลขล็อค..." · ราคาขายล็อคจร ออกจากหน้าสาธารณะ | ลบทั้ง 3 จุด คงป้าย "ขายผ่านคิวรอ" ไว้ตามที่ยืนยัน |
| 4 | เมนู "ประวัติลา/ขาด/จร" → "ประวัติแจ้งลา ขาด ยกเลิก" + ข้อมูลไม่อัปเดต | เปลี่ยนชื่อ 7 จุด · เพิ่มแท็บที่ 3 "ยกเลิก" (คงแท็บผู้ค้าจรไว้ตามที่ยืนยัน) · ต้นเหตุข้อมูลไม่อัปเดตอยู่ที่หัวข้อ 17.1 |
| 5 | คิวจองต้องแก้ไขได้ + เลือกได้หลายโซน | เพิ่มปุ่มแก้ไขในแถวคิวรอ (เดิมมีเฉพาะแถวที่ขายแล้ว) แก้ได้ทุกช่อง · เปลี่ยนช่องโซนเป็นติ๊กได้หลายโซน เก็บลง `zone_pref` คั่นด้วย `,` (ไม่ต้องแก้ schema) |

### 17.6 แท็บ "ยกเลิก" — ทำไมต้องอ่านจาก `activity_log` ด้วย

การยกเลิกกระจายอยู่ 3 ตาราง และมีข้อยกเว้นสำคัญ 1 ข้อ:

| ประเภท | แหล่งข้อมูล |
|---|---|
| ยกเลิกการเช่า (สิ้นสุดสัญญา) | `leave_log` type `lease_cancel` |
| ยกเลิกล็อคจร | `daily_bookings` ที่ `cancelled = true` |
| ยกเลิกคิวจอง | `floating_queue` ที่ `status = 'cancelled'` |
| **ยกเลิกแจ้งลา / ขาดล็อค** | **`activity_log`** — ไม่ใช่ `leave_log` |

เพราะกฎธุรกิจเดิมกำหนดว่า **"ปลดสถานะลา/ขาดแล้ว ประวัติของวันนั้นต้องหายไปด้วย"**
`logLeave` จึงลบแถวใน `leave_log` ทิ้งจริง ไม่เหลือร่องรอยให้ค้นย้อนหลังเลย
รอบนี้จึงให้ `logLeave` เขียนร่องรอยลง `activity_log` ด้วยข้อความรูปแบบตายตัว
`ล็อค <id> · โซน <zone> · ร้าน <shop> · ปลดสถานะ<แจ้งลา|ขาดล็อค>` แล้วให้ `getCancellations` อ่านกลับ

> ⚠️ **ข้อจำกัดที่ต้องรู้:** `activity_log` ถูกล้างอัตโนมัติเมื่อเกิน 30 วันตามกฎเดิม
> รายการ "ยกเลิกแจ้งลา/ขาดล็อค" ในแท็บนี้จึงย้อนดูได้ 1 เดือน ส่วนอีก 3 ประเภทย้อนได้ไม่จำกัด

### 17.7 migration `0014` — สิ่งที่เปลี่ยนที่ฐานข้อมูล

1. เติมคอลัมน์ `line` และ `note` ให้ `floating_queue` (หัวข้อ 17.4)
2. `saveFloatingQueueEntry` บันทึก `line`/`note` ที่หน้าเว็บส่งมาจริง
3. `getFloatingQueue` ส่ง `line`/`note` กลับไปให้หน้าเว็บ
4. `editFloatingQueueEntry` เปลี่ยนจาก `coalesce(nullif(...,''), เดิม)` เป็นเช็ค `d ? 'field'`
   → **ล้างค่าให้ว่างได้จริง** (เดิมส่งค่าว่างมาก็ไม่เปลี่ยน) และรับ `line`/`note`/`market_date` เพิ่ม
   โดยฟิลด์ของรายการที่ "ขายแล้ว" (ล็อค/ราคา/ช่องทาง) ยังใช้กติกาเดิม กันการแก้คิวรอเผลอล้างข้อมูลการขาย
5. `logLeave` เขียนร่องรอยการปลดสถานะลง `activity_log` (หัวข้อ 17.6)
6. เพิ่ม action `getCancellations`
7. `getPublicStatus` ส่ง `note` ของคิวไปด้วย
8. เพิ่ม `getCancellations` เข้า `read_actions` ของ `api()` — **ถ้าลืมข้อนี้ บัญชี viewer จะโดนปฏิเสธว่า
   "ไม่มีสิทธิ์แก้ไขข้อมูล" ทั้งที่แค่เปิดดูประวัติ**

migration นี้ตรวจผลตัวเองหลัง apply (บล็อค `$verify$`) และ **รันซ้ำได้**

> ⚠️ **ราคาขายล็อคจรยังต้องอยู่ใน payload ของ `getPublicStatus` ต่อไป — ห้ามตัดออก**
> ผู้ดูแลสั่งให้ "ไม่แสดง" ราคาบนหน้าสาธารณะ ซึ่งแก้ที่หน้าเว็บแล้ว
> แต่ **ผังตลาด (หน้าผู้ดูแล) อ่านราคาจาก endpoint เดียวกันนี้** ถ้าตัดออกจาก payload
> ผังจะกลับไปแสดง `฿0` ซึ่งคือบั๊กที่เพิ่งแก้ไปในหัวข้อ 16.12
> (เคยลองตัดออกในรอบนี้แล้วและถอยกลับ — บันทึกไว้กันคนถัดไปทำซ้ำ)

### 17.8 ด่านตรวจใหม่ 3 ตัว

| สคริปต์ | จับอะไร |
|---|---|
| `tools/test_api_shim_coverage.js` **(สำคัญที่สุด)** | เปิด **ทุกหน้า** ในเบราว์เซอร์จริง แล้วบังคับว่า (ก) shim ถูกติดตั้ง (ข) `getApiUrl()` ต้องไม่คืนค่าว่างในโหมด supabase (ค) หน้าที่ต้องโหลดข้อมูลต้องยิงคำขอไปฐานข้อมูลอย่างน้อย 1 ครั้ง — ด่านนี้จับบั๊ก 17.1 และ 17.2 ได้ทั้งคู่ และจะจับซ้ำได้ทันทีถ้ามีคนเผลอทำให้หน้าไหนหลุดวงจรอีก |
| `tools/test_queue_and_history.js` | ปุ่มแก้ไขคิวรอ · payload ตอนบันทึก (ล้างค่าว่างได้ · หลายโซน) · แท็บยกเลิกครบ 4 ประเภท · ทุกหน้าตอบสนอง `pm-refresh` · ชื่อเมนู |
| `tools/verify_v26_browser.js` | ตรวจแบบ end-to-end ด้วย **ข้อมูลจำลอง + `api()` ตัวจริงใน PostgreSQL** ไล่จาก "ทำจริงในแอพ" → "ดูผลจริงในหน้าเว็บ" ครบทั้ง 5 เรื่อง แล้วลบข้อมูลจำลองทิ้งเองตอนจบ |

ทั้งสามตัวถูกผูกเข้า `npm run test:maps` แล้ว

### 17.9 สิ่งที่ซ่อมให้ชุดทดสอบเดิมกลับมาใช้ได้

- `tools/seed_test_db.sql` — `truncate` ชุดเดิมล้มเหลวตั้งแต่ migration `0008` เพิ่มตาราง `api_sessions`
  ที่มี FK ชี้มาที่ `app_users` (`cannot truncate a table referenced in a foreign key constraint`)
  → **`tools/test_e2e_supabase.js` รันไม่ได้เลยตั้งแต่รอบนั้น** แก้โดยใส่ `api_sessions` เข้าไปในรายการ
- `tools/test_e2e_supabase.js` — เดิมเรียก `chromium.launch()` เปล่า ๆ จึงบังคับให้ต้องดาวน์โหลด
  Chromium ของ Playwright ใหม่ทุกครั้งที่เวอร์ชันขยับ → รองรับ `PM_CHROME_EXECUTABLE` เหมือนด่านตรวจตัวอื่น

> ⚠️ **`test_e2e_supabase.js` ยังไม่ผ่านครบ (tue 7 ข้อ · thu 8 ข้อ) และ "ไม่ใช่" ผลจากรอบนี้**
> ตรวจเทียบกับโค้ดก่อนแก้แล้ว ได้จำนวนและรายการเดียวกันเป๊ะ — ชุดทดสอบนี้เขียนก่อน migration `0008`
> จึงยังไม่ส่ง `sessionToken` ทำให้ทุก action ที่เขียนข้อมูลถูกปฏิเสธว่า "ต้องเข้าสู่ระบบก่อนใช้งาน"
> และข้อ "ห้ามหลุดยอดค้างชำระ" ก็ขัดกับ `unpaidLocks` ที่ผู้ดูแลอนุมัติให้แสดงตั้งแต่ v2.3
> **งานที่ต้องทำต่อ: ปรับชุดทดสอบตัวนี้ให้ล็อกอินก่อนแล้วส่ง token ทุกคำขอ**

### 17.10 กฎเหล็กที่เพิ่มจากรอบนี้

22. **โค้ดกลางที่ฝังทุกไฟล์ ห้ามมีเงื่อนไข "ต้องมีของครบทุกอย่างถึงจะทำงาน"** — ถ้าบล็อคหนึ่งทำหลายหน้าที่
    (ครอบ `apiGet` · ครอบ `apiPost` · ครอบ `getApiUrl` · รับสัญญาณ refresh) ต้องแยกเงื่อนไขของแต่ละหน้าที่
    ออกจากกัน ไฟล์ที่ขาดของชิ้นเดียวต้องเสียเฉพาะหน้าที่นั้น ไม่ใช่เสียทั้งบล็อค (บั๊ก 17.1)
23. **โค้ดที่ฝังท้ายไฟล์แล้วครอบฟังก์ชันของหน้า ต้องคิดเรื่องลำดับเวลาด้วยเสมอ** — หน้าอาจเรียกฟังก์ชันนั้น
    ไปแล้วก่อนที่เราจะครอบ ต้องมีกลไกตรวจว่า "หน้านี้ได้ข้อมูลจริงหรือยัง" แล้วสั่งใหม่ให้ ไม่ใช่ครอบแล้วจบ (บั๊ก 17.2)
24. **ห้ามตัดฟิลด์ออกจาก payload ของ endpoint ที่มีผู้ใช้มากกว่าหนึ่งหน้า** — ต้องไล่ดูให้ครบก่อนว่าใครอ่านอยู่บ้าง
    การซ่อนข้อมูลจากผู้ใช้ทำที่ "หน้าที่แสดง" ไม่ใช่ที่ต้นทาง (ดูกล่องเตือนในหัวข้อ 17.7)
25. **ทุกครั้งที่แก้เงื่อนไขใน `tools/inject_*` ต้องรัน `tools/test_api_shim_coverage.js`** — ด่านนี้เป็นตัวเดียว
    ที่พิสูจน์ว่า "ทุกหน้ายังคุยกับฐานข้อมูลได้จริง" ซึ่งการอ่านโค้ดอย่างเดียวไม่มีวันเห็น

### 17.11 ผลตรวจรอบนี้

**ด่านตรวจมาตรฐาน — ผ่านครบ**

```
python3 tools/check_js.py                      ✅ 22 ไฟล์
python3 build.py                               ✅ build + ✅ verify
node tools/test_responsive.js dist/tuesday     ✅ 84/84
node tools/test_responsive.js dist/thursday    ✅ 84/84
node tools/test_map_reach.js                   ✅ ทุกล็อคเลื่อนไปหาได้
node tools/test_map_vendor_sync.js             ✅ TUE 216 ล็อค · THU 298 ล็อค
node tools/test_map_statuses.js                ✅ ทั้งสองตลาด
node tools/test_public_status.js               ✅ ผ่านครบ 18 ข้อ ทั้งสองตลาด
node tools/test_api_shim_coverage.js           ✅ ทุกหน้าเชื่อมต่อฐานข้อมูลได้ครบ
node tools/test_queue_and_history.js           ✅ ผ่านครบทั้งสองตลาด
node tools/test_offline_map.js                 ✅ ทุกสถานการณ์เตือนผู้ใช้ชัดเจน
```

> ระหว่างทาง `build.py` จับได้ 1 จุดที่คอมเมนต์ในสคริปต์ `inject_*` มีคำว่า "วันอังคาร"
> แล้วรั่วไปอยู่ในไฟล์ผังฝั่งวันพฤหัสฯ (ไฟล์ผังเป็นข้อยกเว้น ไม่ผ่านกฎแปลงคำ) — แก้แล้ว
> เป็นตัวอย่างว่า verify ของ build ทำงานจริงแม้กับข้อความในคอมเมนต์

**ตรวจบนเบราว์เซอร์จริงด้วยข้อมูลจำลอง (`tools/verify_v26_browser.js`) — ผ่านครบทั้งสองตลาด**

จำลองบนจอมือถือ 390px โดยยิงผ่าน `api()` ตัวจริงใน PostgreSQL ที่ลง migration ชุดเดียวกัน:

1. ยกเลิกการเช่า → หน้าสาธารณะขึ้นล็อคนั้นในช่อง "ไม่มีผู้ค้าประจำ" ทันที
   (TUE `SB2` 215→216 ล็อค · THU `A2` 293→294 ล็อค) และล็อคที่ยังมีผู้ค้าไม่ถูกนับ
2. แยกหมวดโซนสำเร็จ (TUE 11 โซน · THU 20 โซน) · หมายเหตุโซนเปิดท้ายขึ้นที่ TA/TB/TC เท่านั้น
   โซน "ข้างโลตัส T" ไม่ติดหมายเหตุ · ตลาดวันอังคารไม่มีหมายเหตุเลย (ถูกต้อง)
3. ขายล็อคจรแล้วหน้าสาธารณะแสดงล็อคโดย **ไม่มีราคา · ไม่มีป้ายผู้จัดขายตรง · ไม่มีหมายเหตุเดิม**
4. แจ้งลาแล้วหน้าประวัติขึ้นจาก 0 → 1 รายการเองโดยไม่ต้องกด F5
5. คิวจอง: เลือก 3 โซนบันทึกได้ · LINE/หมายเหตุลงฐานข้อมูลจริง · แก้ชื่อได้ · ล้างหมายเหตุให้ว่างได้จริง ·
   ช่องที่ไม่ได้แก้คงค่าเดิม · ยกเลิกแล้วขึ้นครบทั้ง 3 ประเภทในแท็บ "ยกเลิก" ·
   ปลดสถานะแล้วประวัติของวันนั้นหายจริงตามกฎธุรกิจเดิม

**ลบข้อมูลจำลองทั้งหมดแล้ว ตรวจยืนยันเหลือ 0 แถว ทั้งสองตลาด**

### 17.12 งานที่เหลือหลังรอบนี้

| # | งาน | ใครทำ |
|---|---|---|
| 1 | **apply `migration 0014` ในโปรเจกต์ Supabase ทั้ง 2 ตัว** — ต้องทำก่อนหรือพร้อมกับ deploy frontend | ผู้ดูแล / AI |
| 2 | deploy `dist/` ขึ้น GitHub Pages ทั้งสอง repo | ผู้ดูแล |
| 3 | ปรับ `tools/test_e2e_supabase.js` ให้ล็อกอินและส่ง `sessionToken` (หัวข้อ 17.9) | AI |
| 4 | แก้ข้อความในแอพที่ยังเขียนว่า "Google Sheets" ให้เป็น "ฐานข้อมูล" (ค้างจากหัวข้อ 16.7 ข้อ 8 — พบเพิ่มที่หน้าคิวจองล็อคจร) | AI |

> **ลำดับที่ปลอดภัย:** apply `0014` ก่อน แล้วค่อย deploy frontend
> ถ้า deploy frontend ก่อน แท็บ "ยกเลิก" จะขึ้นข้อความว่าฐานข้อมูลยังไม่รองรับ (ตั้งใจให้บอกชัด
> ตามกฎเหล็กข้อ 15 — ไม่ใช่แสดงว่าว่างเปล่าเงียบ ๆ) ส่วนหน้าอื่นทำงานได้ตามปกติ

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

---

## 18. รอบ v2.7: คิวจร ค่าไฟ ผู้ค้าพิเศษ และขายล็อคฟรี (เพิ่ม 3 ส.ค. 2026)

### 18.1 กติกาธุรกิจที่ยืนยันแล้ว

- **ผู้ค้าพิเศษจากผู้บริหาร (ฟรี):** ค่าเช่ารายวัน ค่าเช่าเดือนแรก ค่าธรรมเนียมแรกเข้า และค่าธรรมเนียมรายปีเป็น `0` ทั้งหมด
- **ค่าไฟฟ้าและค่าใช้จ่ายอื่นที่ไม่ใช่ค่าเช่าหรือค่าธรรมเนียมยังคิดตามจริง** และต้องแสดงแยกในใบสรุป
- ผู้ค้าพิเศษบันทึก `type = special_admin_free` และ `dailyRate = 0` เพื่อให้ค่าเช่ารอบถัดไปยังเป็นศูนย์ ไม่ใช่ลดเฉพาะหน้าลงทะเบียน
- การขายล็อคจรอนุญาตราคา `0` ทั้งขายตรงและขายผ่านคิว; ช่องว่างไม่เท่ากับศูนย์ ผู้ใช้ต้องกรอก `0` อย่างชัดเจนเพื่อป้องกันให้ล็อคฟรีโดยไม่ตั้งใจ

### 18.2 สิ่งที่แก้

1. `pamai_batch_payment.html` — เอาช่องตั้งค่าธรรมเนียมรายปีออกจากหน้าต่างส่วนลด แต่คงการคิดรายปีเดือนมีนาคมและหน้าหลัก **ระบบ › ตั้งค่าค่าธรรมเนียม**
2. `pamai_floating_queue.html` — เรียงหมวดเป็น **คิวรอ → ขายแล้ว → ล็อคว่างขายได้ตอนนี้**, เพิ่ม checkbox กรองล็อคว่างหลายโซน และแก้หัวข้อเลือกโซนให้ตัดบรรทัดได้บนมือถือ
3. `pamai_electric_system.html` — รอ `status === ok` ก่อนเปลี่ยนหน้าจอ, ไม่แสดงข้อมูลตัวอย่าง/ข้อมูลเก่าระหว่างโหลด, กรอง `terminated`, รองรับผลลัพธ์ว่างจริง, มีสถานะโหลด/ลองใหม่ และคำนวณค่าเช่า/ค่าไฟตามเดือนบิลที่เลือก
4. `market_specific/pamai_market_map.TUE.html` — วาดแถว F1–F6 จากซ้ายไปขวาเป็น `F6,F5,F4,F3,F2,F1` ทำให้ F1 อยู่ขวาสุด โดยไม่แตะผังวันพฤหัสบดี
5. `pamai_vendor_registration.html` — เอาช่องผู้จัดรับผิดชอบออก เพิ่ม checkbox ผู้ค้าพิเศษ และล็อกค่าเช่า/ค่าธรรมเนียมเป็นศูนย์ทันทีโดยไม่ล้างค่าไฟ/ค่าใช้จ่ายอื่น
6. `test_sheets_guard.js` — ปรับด่านตรวจให้ตรงกับระบบจริงที่ค่าเริ่มต้นเป็น Supabase และตรวจว่าสลับไป Sheets/กลับ Supabase ได้จริง

### 18.3 ผลตรวจทดสอบก่อนเผยแพร่

- `python3 tools/check_js.py` — ผ่าน 22 ไฟล์ HTML
- `python3 build.py` — build 22 ไฟล์ + logo ต่อหนึ่งตลาด และ verify แยกตลาดผ่าน
- `npm run test:responsive` — ผ่านทั้งสองตลาด
- `npm run test:maps` — ทุกล็อคเข้าถึงได้ทุกขนาดจอ, สถานะผัง/หน้าสาธารณะ/API/คิว/ประวัติ/โหมดออฟไลน์ผ่านทั้งหมด
- `node tools/test_map_vendor_sync.js dist/tuesday dist/thursday` — TUE 216 ล็อค และ THU 298 ล็อค รับข้อมูลผู้ค้าครบทุกโซน
- `npm run test:aug2026` — ผ่านครบ 12 กรณีต่อตลาด รวมขายราคา `0` และผู้ค้าพิเศษ
- `npm run test:sheets` — โหมด Supabase เริ่มต้นและทางถอย Google Sheets ผ่านทั้งสองตลาด

### 18.4 การเผยแพร่และตรวจหน้าเว็บจริง

- ตลาดวันอังคาร: commit [`76bcea9`](https://github.com/TonyThejoss/Paemai-Market-Tuesday/commit/76bcea9ac8e8b347f32c018eac888f781603af5f)
- ตลาดวันพฤหัสบดี: commit [`76da78c`](https://github.com/TonyThejoss/Paemai-MiNi-App-THU/commit/76da78c25b2721c476909c4ddb96f4a38b47b8f9)
- หน้า GitHub Pages ของทั้งสองตลาดอัปเดตแล้ว: หน้าคิวเรียง **1 คิวรอ → 2 ขายแล้ว → 3 ล็อคว่างขายได้ตอนนี้** และแสดงตัวกรองโซนแบบ checkbox
- จำลองกรอกหน้าลงทะเบียนจริงโดย **ไม่กดบันทึก**: ขั้นตอน 2 ไม่มีช่องผู้จัด, มีตัวเลือกผู้ค้าพิเศษพร้อมข้อความกำกับว่าค่าไฟ/ค่าใช้จ่ายอื่นคิดตามจริง และเมื่อเลือกแล้วค่าเช่ารายวันเปลี่ยนจาก `300` เป็น `0` ทันที
- ผังตลาดวันอังคารบนหน้าเว็บจริงแสดงลำดับ DOM ของแถว F1–F6 เป็น `F6,F5,F4,F3,F2,F1` จึงวาง F1 ขวาสุดตามข้อกำหนด
- เมื่อเปิดหน้าค่าไฟโดยตรงโดยไม่มี session ผู้ใช้จริง ทั้งสองตลาดแสดงข้อความโหลดไม่สำเร็จและล้างตาราง ไม่แสดงข้อมูลตัวอย่างหรือข้อมูลเก่าหลอกผู้ใช้; เส้นทางที่มี session และผลตอบกลับฐานข้อมูลจำลองตรวจผ่านใน `test:aug2026`

> การตรวจหน้าเว็บจริงรอบนี้ไม่สร้าง ไม่แก้ และไม่ลบข้อมูลผู้ค้าจริง การทดสอบที่ต้องเขียนข้อมูลใช้ mock backend แยกต่างหาก

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

---

## 19. รอบ v2.8: บัญชียอดค้างและการรับชำระผู้ค้าประจำ (เพิ่ม 4 ส.ค. 2026)

> หัวข้อนี้แทนกติกาเดิมเรื่องการรับเงินและการผ่อนชำระทั้งหมด หากข้อความในหัวข้อก่อนหน้าขัดกันให้ยึดหัวข้อนี้

### 19.1 กติกาธุรกิจ

- การลงทะเบียนผู้ค้าประจำเป็นเพียงการเปิดรอบการเช่าและตั้งยอดค้าง ห้ามสร้างรายการรับชำระอัตโนมัติ
- รอบการเช่าไม่มีวันสิ้นสุดล่วงหน้า จะสิ้นสุดเมื่อผู้ดูแลกด **ยกเลิกการเช่า/สิ้นสุดสัญญา** เท่านั้น และปิดรอบไม่ได้หากยังมียอดค้าง
- ระบบผ่อนชำระถูกยกเลิก ไม่มีเมนู ไม่มีหน้ารับเงิน และ API ปฏิเสธคำสั่งผ่อนชำระเดิม
- รับชำระผู้ค้าประจำได้จากเมนู **รับชำระหลายล็อค** หรือปุ่ม **รับชำระ** ในป๊อปอัปผังตลาดเท่านั้น เลือกเงินสดหรือโอนเงิน
- ผู้ใช้กรอกเพียงยอดรับ ระบบหักยอดค้างรวมจากรายการเก่าสุดก่อนโดยอัตโนมัติ ไม่ต้องเลือกเดือน และฐานข้อมูลปฏิเสธยอดที่เกินยอดคงค้าง
- เงินของผู้ค้าจรรับผ่านหน้า **คิวจร/ล็อคจร** เท่านั้น
- ประวัติทุกการรับชำระผูกกับรอบการเช่าและเก็บชื่อผู้ค้า ณ เวลารับเงิน เมื่อเปลี่ยนผู้ค้าในล็อคเดิม รอบใหม่จึงไม่เห็นประวัติของผู้ค้าเก่า แต่หลักฐานการเงินรอบเก่ายังเก็บไว้ตรวจสอบได้

### 19.2 หน้าจอและรายงาน

- ป๊อปอัปผังตลาดจัดปุ่มเป็นหมวดข้อมูลผู้ค้า การเงิน สถานะ และสัญญา แสดงยอดค้างรวม ประวัติ 5 รายการล่าสุด และลิงก์ดูประวัติทั้งหมดของรอบการเช่านั้น
- เพิ่ม **รายงาน 10 — ประวัติรับชำระผู้ค้าประจำรายล็อค** กรองล็อค รอบการเช่า และช่วงวันที่ได้ พร้อมชื่อผู้ค้า ณ เวลารับเงิน
- รายงานยอดค้างและรายงานรับชำระเดิมอ่านยอดจากบัญชีลูกหนี้ชุดเดียวกัน จึงตรงกับหน้าผังและหน้ารับชำระหลายล็อค
- ผังวันอังคารคืนล็อค `BX` ใต้ `B11`, `C11` ใต้ `C12` และ `CX` ใต้ `C10`

### 19.3 ฐานข้อมูลและการตรวจ

- migration `0015_vendor_accounts_and_payments.sql` เพิ่มรอบการเช่า รายการตั้งหนี้ การจัดสรรเงินรับ และ `lease_id` ในรายการรับชำระ; apply แล้วทั้ง TUE และ THU
- สถานการณ์จำลองแบบ transaction/rollback ผ่านทั้งสองตลาด: ตั้งหนี้ 1,000 → รับ 300 → เหลือ 700 → ปฏิเสธ 701 → รับ 700 → ปิดรอบ → เปิดผู้ค้าใหม่ยอด 500 และยืนยันว่าประวัติรอบใหม่เป็นศูนย์รายการ
- ป๊อปอัปผังต้องแสดงสินค้าจาก `product` ของผู้ค้าโดยตรง ห้ามใช้ข้อความย่อบนตัวล็อค เพราะข้อความนั้นอาจเป็นชื่อผู้ค้า; ตรวจจริงแล้ว TUE B11 แสดง “แกงเขียวหวานปลากราย” และ THU A1 แสดง “กระเพาะปลา” แยกจากชื่อผู้ค้าถูกต้อง
- ด่านตรวจโค้ด การสร้างไฟล์ทั้งสองตลาด ชุดทดสอบหน้าจอ ผัง และกติกาบัญชี ต้องผ่านก่อนเผยแพร่

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

---

## 20. รอบ v2.9: สิทธิ์ผู้ใช้ ยกเลิกรายการรับเงิน และรวมค่าไฟ (เพิ่ม 5 ส.ค. 2026)

### 20.1 บัญชีผู้ใช้และสิทธิ์

- `paemai` เป็นบัญชีหลัก (`Admin`) และมีทุกสิทธิ์เสมอ ใช้สร้าง/แก้ไข/ลบบัญชีอื่น แต่ห้ามแก้สิทธิ์หรือลบบัญชี `paemai` จากหน้าจัดการผู้ใช้
- เมนู **ระบบ › บัญชีผู้ใช้และสิทธิ์** ใช้สร้าง Username, ชื่อแสดง, Password, ประเภทบัญชี และกำหนดสิทธิ์แยกรายฟังก์ชัน
- สิทธิ์ครอบคลุมหน้าหลัก ผังตลาด รับชำระ ค่าไฟ ลงทะเบียน/แก้ไขผู้ค้า ลา คิวจร กฎ รายงาน ตั้งค่า บันทึกกิจกรรม จัดการผู้ใช้ และ **ลบธุรกรรมรับเงิน**
- การซ่อนเมนูเป็นเพียงชั้นหน้าจอ ฐานข้อมูลตรวจ session, role และสิทธิ์สดทุกคำสั่งอีกชั้น แก้สิทธิ์หรือลบบัญชีแล้ว session เดิมถูกยกเลิก
- สิทธิ์ลบธุรกรรมรับเงินให้ได้เฉพาะบัญชีประเภท `Admin` ที่ได้รับ `delete_payments`; การใส่คีย์นี้ให้ viewer โดยตรงก็ไม่สามารถลบได้

### 20.2 การยกเลิกรายการรับเงินผิด

- รายงาน 10 แสดงปุ่ม **ยกเลิกรายการ** เฉพาะ Admin ที่ได้รับสิทธิ์ ผู้ใช้ต้องระบุเหตุผลอย่างน้อย 4 ตัวอักษรและยืนยันก่อนทำรายการ
- ระบบคืนยอดที่เคยจัดสรรกลับเป็นยอดค้างของรอบการเช่าเดิมโดยอัตโนมัติ แล้วคำนวณสถานะผู้ค้าใหม่
- ห้ามลบหลักฐานแบบถาวร: รายการเดิมและรายละเอียดการจัดสรรถูกย้ายไป `voided_payments` พร้อมเหตุผล ผู้ยกเลิก และเวลา และมีบันทึกใน activity log
- migration ที่ใช้คือ `0016_user_permissions_and_payment_reversal.sql` และติดตั้งแล้วทั้งโปรเจกต์ TUE/THU

### 20.3 รับชำระหลายล็อคและหน้าลงทะเบียน

- หน้ารับชำระหลายล็อคตั้งหนี้ **ค่าเช่า/อื่น** และ **ค่าไฟฟ้า** แยกประเภท แต่แสดงยอดรวมที่ต้องรับและเติมจำนวนเต็มรวมค่าไฟให้อัตโนมัติ
- เดือนแรกตรวจรายการซ้ำตามประเภทหนี้ จึงไม่สร้างค่าเช่าซ้ำจากหน้าลงทะเบียน และไม่ทำให้รายการค่าเช่าไปขวางการสร้างค่าไฟ
- หน้าลงทะเบียนขั้นตอน 3 บนมือถือห่อ label/input ลงบรรทัดใหม่ ไม่ดันตัวอักษรออกนอกกรอบ; ด่าน responsive ผ่านที่ 360, 390 และ 430px
- ขั้นตอน 4 ใช้ข้อความ **ยอดค้างตั้งต้น (ยังไม่ได้รับชำระ)**, สถานะ **ค้างชำระ** และปุ่ม **บันทึกข้อมูลผู้ค้า** ไม่มีข้อความที่ทำให้เข้าใจว่าเป็นการบันทึกลง Google Sheet หรือรับเงินแล้ว

### 20.4 ผลตรวจรอบเผยแพร่

- ทดสอบ transaction/rollback ทั้งสองตลาด: ตั้งหนี้ค่าเช่า 800 + ค่าไฟ 200 → รับเงิน 300 → ยอดเหลือ 700 → ยกเลิกรายการ → ยอดกลับ 1,000 → พบหลักฐานใน `voided_payments` → viewer ถูกปฏิเสธ
- ตรวจหน้า Browser จริง: หน้าบัญชีผู้ใช้แสดง `paemai` เป็นบัญชีหลักทุกสิทธิ์; รายงาน 10 แสดงชื่อผู้ค้าขณะรับเงินและปุ่มยกเลิก; ล็อค A1 แสดงค่าเช่า/อื่น 1,600 + ค่าไฟ 360 และเติมยอดรับ `1,960` (max `1,960`)
- `python3 tools/check_js.py`, `build.py`, `test:responsive` (88/88 ต่อตลาด), `test:maps`, `test:aug2026`, `test:sheets`, `test:accounts` และ `test:permissions` ผ่านทั้งหมด

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

---

## 21. รอบ v3.0: บัญชีกลาง ป้องกันค่าเช่าซ้ำ และจัดเมนูการเงิน (เพิ่ม 5 ส.ค. 2026)

### 21.1 สาเหตุและกติกาใหม่

- ต้นเหตุยอดค้างผิดคือผังตลาดเคยสร้างหนี้ชนิด `ยอดเรียกเก็บประจำเดือน` ขณะที่หน้ารับชำระสร้าง
  `ค่าเช่าประจำเดือน` ฐานข้อมูลจึงมองเป็นคนละรายการและรวมค่าเช่าสองครั้ง ทั้งที่เป็นหนี้องค์ประกอบเดียวกัน
- หน้าแสดงผลทุกหน้าอ่านยอดจาก `getVendorAccounts` เป็นแหล่งจริงเดียว ได้แก่ `outstanding`,
  `outstanding_non_electric` และ `outstanding_electric`; ผังตลาดและหน้าค่าไฟไม่มีสิทธิ์สร้างหนี้เอง
- การออกหรือปรับหนี้รายเดือนทำที่หน้ารับชำระผ่าน `ensureVendorCharges` เพียงทางเดียว ฐานข้อมูลแปลงประเภท
  เป็น `charge_code` (`rent` / `electric` / อื่น ๆ) และบังคับหนึ่งองค์ประกอบต่อรอบการเช่า+เดือน
- รายงานค่าไฟอ่านยอดรับจริงจาก `payment_allocations` ไม่เดาจากข้อความใน `payments.type` ทำให้ยอดหน้า
  รับชำระ หน้าค่าไฟ Dashboard และรายงานสัมพันธ์กัน

### 21.2 การซ่อมข้อมูลจริงและการป้องกัน

- migration `0017_canonical_vendor_charges.sql` เก็บสำเนาแถวที่รวมและการจัดสรรเงินไว้ใน
  `vendor_charge_repair_archive` ก่อนลบแถวซ้ำทุกครั้ง
- ตรวจตลาดวันอังคารก่อนแก้พบ **166 กลุ่ม / 166 แถวเกิน / ยอดถูกบวกเกิน 158,920 บาท** ทุกกลุ่มมี
  2 แถวและยังไม่มีการรับชำระผูกอยู่ จึงรวมโดยเก็บยอดที่ควรเรียกสูงสุดหนึ่งครั้งได้โดยไม่กระทบเงินรับ
- หลังติดตั้ง migration ตลาดวันอังคารเหลือรายการซ้ำ 0 กลุ่มและมีหลักฐานใน archive 166 แถว;
  ตลาดวันพฤหัสบดีไม่พบรายการซ้ำ (0 กลุ่ม) แต่ติดตั้ง index/trigger/ฟังก์ชันป้องกันชุดเดียวกันแล้ว
- ทดสอบ migration แบบ `BEGIN ... ROLLBACK` บนฐานข้อมูลจริงก่อนรันถาวร และตรวจหลังรันว่า
  `vendor_charge_component_once` กับ `ensure_vendor_charges_split(jsonb)` พร้อมใช้งานทั้งสองโปรเจกต์

### 21.3 หน้าจอที่เปลี่ยน

1. ผังตลาดวันอังคารเรียงแถว SF จากซ้ายไปขวาเป็น `SF9, SF8, …, SF1` จึงทำให้ SF1 อยู่ขวาสุด
2. เพิ่มหน้า **การเงิน › กำหนดส่วนลดค่าเช่า** ในแอพหลัก และถอดทางเข้าการกำหนดส่วนลดออกจากหน้า
   รับชำระหลายล็อค เพื่อแยกหน้าที่ชัดเจน
3. หน้ารับชำระหลายล็อคเปิดตัวกรอง **ค้างชำระ** เป็นค่าเริ่มต้น และเรียงล็อคแบบธรรมชาติ
   ตามหมวดอักษรก่อนเลข เช่น `A2` ก่อน `A10` และ `SA2` ก่อน `SA10`
4. หน้าค่าไฟแสดงยอดค้างค่าเช่า/ค่าไฟจากบัญชีกลาง และส่งผู้ใช้ไปหน้ารับชำระแทนการบันทึกเงินในระบบเก่า
5. รายงานยอดค้างแยกค่าเช่า/ค่าอื่นกับค่าไฟจากองค์ประกอบจริง และรายงานรับค่าไฟใช้ยอดจัดสรรเงินจริง

### 21.4 ผลทดสอบและการเผยแพร่

- `python3 tools/check_js.py` — ผ่าน 23 ไฟล์ HTML
- `python3 build.py` — สร้าง 23 ไฟล์ + logo ต่อตลาด และ verify การแยกตลาดผ่าน
- `npm run test:billing`, `test:accounts`, `test:permissions`, `test:sheets`, `test:aug2026` — ผ่านทั้งหมด
- `npm run test:responsive` — ผ่าน 88/88 ต่อตลาด; `npm run test:maps` — ผัง สถานะ หน้าสาธารณะ
  การเชื่อม API คิว ประวัติ และโหมดออฟไลน์ผ่านทั้งสองตลาด
- ตรวจ GitHub Pages จริงแล้ว: ทั้งสองแอพมีเมนู **กำหนดส่วนลดค่าเช่า** ในหมวดการเงิน,
  หน้ารับชำระเริ่มที่ตัวกรองค้างชำระ และผัง TUE แสดงลำดับ DOM `SF9 … SF1` ถูกต้อง
- ตลาดวันอังคาร: commit [`d6f4726`](https://github.com/TonyThejoss/Paemai-Market-Tuesday/commit/d6f4726a348075a059949b87e87da794bcba603a)
- ตลาดวันพฤหัสบดี: commit [`d50f4c9`](https://github.com/TonyThejoss/Paemai-MiNi-App-THU/commit/d50f4c9b238e1add1c2ee2d111a52d25e689c3b1)

> การทดสอบหน้ารับชำระที่ต้องจำลองข้อมูลใช้ backend จำลองและไม่สร้างรายการรับเงินจริง ส่วนการซ่อม
> ยอดซ้ำทำบนฐานข้อมูลจริงหลังสำรองหลักฐานทุกแถวใน archive แล้ว

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**

---

## 22. รอบ v3.0.1: ซ่อมรายงานผู้ค้าปัจจุบันและตัวกรองหลายโซน (เพิ่ม 5 ส.ค. 2026)

### 22.1 อาการและต้นเหตุ

- รายงาน 09 เปิดจากหน้า **รายงาน PDF** ซึ่งทำงานอยู่ใน iframe แล้วสร้างหน้าต่างรายงานใหม่
  แต่ session ที่หน้าหลักส่งมาเดิมเก็บไว้เพียงในตัวแปรของ iframe ไม่ได้อยู่ใน `sessionStorage`
  ของ browsing context ที่รายงานใช้ คำขอ `getVendors` จึงไม่มี token และแสดงว่าโหลดข้อมูลไม่สำเร็จ
  ทั้งที่ข้อมูลผู้ค้ายังอยู่ครบ
- WebView/PWA บางรุ่นบล็อก `window.open()` ทำให้กดเปิดรายงานแล้วเหมือนเด้งกลับหน้าเดิม

### 22.2 วิธีแก้และพฤติกรรมใหม่

1. ตัวเชื่อม API กลางบันทึก session ที่ได้รับจากหน้าหลักไว้ใน `sessionStorage` ของโมดูล และรายงาน
   อ่าน session จาก `window.opener` แบบ same-origin เป็นทางสำรอง จึงไม่ส่ง token ผ่าน URL และไม่ทำให้หลุดล็อกอิน
2. ถ้าเบราว์เซอร์บล็อกหน้าต่างใหม่ หน้า launcher จะเปิดรายงานในหน้าเดิมแทน ปุ่ม **กลับ** ใช้ย้อนกลับมา
   รายการรายงานได้ตามปกติ
3. รายงาน 09 สร้างตัวติ๊กโซนจากข้อมูลผู้ค้าปัจจุบันจริง เลือกพร้อมกันได้หลายโซน
4. เมื่อไม่ติ๊กโซนใด ระบบแสดงผู้ค้าปัจจุบันทุกล็อคเป็นค่าเริ่มต้น; ปุ่ม **ล้างตัวเลือก (ดูทั้งหมด)**
   คืนค่าดังกล่าวทันที
5. ผู้ค้าที่มีสถานะ `terminated` ไม่แสดงในรายการและไม่ถูกนับในยอดสรุป ส่วนล็อคที่ไม่มีค่า `zone`
   ใช้อักษรนำหน้ารหัสล็อคเป็นค่าทดแทนเพื่อไม่ให้ข้อมูลหาย

### 22.3 ผลตรวจ

- `python3 tools/check_js.py` และ `python3 build.py` ผ่าน การแยกชื่อไฟล์/สี/ฐานข้อมูลของสองตลาดยังถูกต้อง
- ชุดทดสอบจำลองรายงาน 09 ผ่านทั้ง TUE/THU: ค่าเริ่มต้นทุกล็อค, เลือกหนึ่งโซน, เลือกหลายโซน,
  ล้างตัวเลือก และตัดสัญญาที่สิ้นสุด
- `test:billing`, `test:accounts`, `test:permissions`, `test:sheets`, `test:aug2026`,
  `test:responsive` และ `test:maps` ผ่าน โดยไม่มีการเขียนข้อมูลผู้ค้าหรือรายการรับเงินจริง
- ณ เวลาบันทึกคู่มือ เครื่องทำงานไม่มี local Git repository และไม่มี GitHub CLI จึงยังไม่ได้สร้าง commit/
  deploy รอบ v3.0.1; ต้องเผยแพร่ `dist/tuesday/` และ `dist/thursday/` ก่อนตรวจ GitHub Pages จริง

**[⬆ กลับไปที่สารบัญ](#สารบัญ)**
