# สถานะการย้ายฐานข้อมูลไป Supabase — อัปเดต 29 ก.ค. 2026

**สถาปัตยกรรม:** ทางเลือก A — 2 โปรเจกต์แยกกัน (ตามที่ผู้ดูแลตลาดเลือก)

## โปรเจกต์ที่สร้างแล้ว

| | ตลาดวันอังคาร (TUE) | แพไม้มินิวันพฤหัสบดี (THU) |
|---|---|---|
| Project ref | `jrsuxrzyxiqqvzjftpms` | `atvxajfwazrswdlhnosh` |
| ชื่อในแดชบอร์ด | `TonyThejoss's Project` ⚠️ *ควรเปลี่ยนชื่อเป็น `paemai-market-tue`* | `paemai-market-thu` |
| Region | ap-northeast-1 (โตเกียว) | ap-southeast-1 (สิงคโปร์) |
| API URL | `https://jrsuxrzyxiqqvzjftpms.supabase.co` | `https://atvxajfwazrswdlhnosh.supabase.co` |
| Publishable key | `sb_publishable_qef_AOM39IMJfO-_QuYwow_IY4h5qY3` | `sb_publishable_kGSsjrQvDa9EKaw7CDPeQg_UnkKtCLa` |

> **publishable key ใส่ในโค้ดบน public repo ได้อย่างปลอดภัย** — Supabase ออกแบบให้เปิดเผยได้
> เพราะด่านจริงคือ RLS + GRANT ที่ปิดตารางทั้งหมดไว้ (ดูหัวข้อ "ทดสอบแล้ว")
> ⚠️ **ห้ามนำ `service_role` key มาใส่ในโค้ดหรือ repo เด็ดขาด** ไม่มีขั้นตอนไหนของระบบนี้ต้องใช้

> หมายเหตุ region: ฝั่ง TUE ใช้โปรเจกต์เดิมที่มีอยู่แล้ว (โตเกียว) ส่วน THU สร้างใหม่ที่สิงคโปร์
> ต่างกันราว 45 มิลลิวินาที — ไม่มีนัยสำคัญเมื่อเทียบกับ Google Sheets ที่ใช้ 2–4 **วินาที**
> ถ้าต้องการให้เหมือนกัน ย้าย TUE มาสิงคโปร์ได้ภายหลัง (สร้างใหม่ + migrate ซ้ำ)

## ทำเสร็จแล้ว ✅

1. **สร้างโปรเจกต์ครบ 2 ตัว** — ค่าใช้จ่าย **฿0** (แพลนฟรี ใช้ 2/2 โปรเจกต์)
2. **schema 11 ตาราง** เหมือนกันทั้งสองโปรเจกต์ พร้อม index/constraint:
   - `vendors · leave_log · daily_bookings · payments · installment_plans · floating_queue`
     `rent_discounts · settings · market_rules · activity_log · app_users`
   - **ชนิดข้อมูลจริง** (`date`, `numeric`, `jsonb`, `text`) → **ปิดบั๊กตระกูล #28/#29/#32 ถาวร**
     (Postgres ไม่แปลง `"0812345678"` เป็นตัวเลข และไม่ตีความ 2569 เป็น ค.ศ.)
   - **บังคับกฎธุรกิจที่ฐานข้อมูล:** `leave_one_per_lock_per_day` (1 ล็อค 1 วัน 1 รายการ) ·
     `daily_one_active_per_lock` (ห้ามขายจรซ้ำล็อคในวันเดียวกัน) · ส่วนลด 1 รายการ/เดือน/ขอบเขต
3. **RLS + ปิด GRANT ทุกตารางจาก anon/authenticated** — เข้าถึงตารางตรง ๆ ไม่ได้เลย
4. **view `public_lock_status`** สำหรับหน้าสถานะสาธารณะ — whitelist คอลัมน์ที่ตัว schema
   (`lock, zone, product, status`) ทำให้ **เบอร์โทร/ยอดเงินหลุดไม่ได้แม้โค้ดหน้าเว็บเขียนผิด**
5. **RPC `verify_user` / `change_password`** — รหัสผ่านเก็บเป็น **bcrypt cost 12** (แข็งแรงกว่า
   SHA-256 ของระบบเดิมมาก) · เป็น `security definer` จึงอ่าน `app_users` ได้โดยที่ anon อ่านตารางเองไม่ได้
6. **`pg_cron`** ลบ `activity_log` เก่ากว่า 30 วัน ทุกวัน 03:00 (แทนการสุ่ม 5% ต่อ request ของเดิม)
7. **บัญชีผู้ใช้ใหม่** ตามที่ผู้ดูแลตลาดสั่ง — ทั้งใน Supabase **และ** ในชีต Google ที่ยังใช้งานอยู่

## บัญชีผู้ใช้ (ลบของเก่าออกหมดแล้วทั้ง 4 บัญชี)

| ตลาด | username | สิทธิ์ | ชื่อแสดง |
|---|---|---|---|
| วันอังคาร | `paemai` | admin | ผู้ดูแลตลาด |
| วันพฤหัสบดี | `paemai` | admin | ผู้ดูแลตลาด |
| วันพฤหัสบดี | `game123` | admin | เกม |

บัญชีที่ลบไปแล้ว: `tony2568` · `fon12345` · `too56789` · `aew98765` · `game1234`
(ลบทั้งใน Supabase และในชีต `users` ของทั้งสองไฟล์)

> 🔴 **รหัส `123456` ของ `game123` เดาได้ในวินาทีเดียว** และบัญชีนี้เป็น admin เต็มสิทธิ์
> (รับเงิน แก้ข้อมูลผู้ค้า ขายล็อคจร ตั้งค่าธรรมเนียม) — ผู้ดูแลตลาดยืนยันให้ใช้ตามนี้
> ถ้าเปลี่ยนใจ เปลี่ยนได้เองที่เมนู "เปลี่ยนรหัสผ่าน" ในแอพ หรือบอกผมให้เปลี่ยนให้

## ทดสอบแล้ว — 22/22 ผ่าน (ยิง REST API จริงด้วย publishable key)

| การทดสอบ | TUE | THU |
|---|---|---|
| อ่าน `vendors` / `app_users` / `payments` / `leave_log` ตรง ๆ → ถูกปฏิเสธ 401 | ✅ | ✅ |
| เขียนลง `vendors` ตรง ๆ → ถูกปฏิเสธ 401 | ✅ | ✅ |
| อ่าน `public_lock_status` → ได้ 200 | ✅ | ✅ |
| ขอคอลัมน์ `phone` จาก view สาธารณะ → ไม่มีคอลัมน์ (400) | ✅ | ✅ |
| `paemai`/`tony8029` → ได้โปรไฟล์ 1 แถว | ✅ | ✅ |
| `game123`/`123456` → ได้โปรไฟล์ 1 แถว | — | ✅ |
| รหัสผิด → ได้ `[]` (ไม่บอกว่า username มีจริงหรือไม่ = กัน user enumeration) | ✅ | ✅ |
| บัญชีเก่า `tony2568`/`pm246810`, `fon12345`/`fn135790` → ได้ `[]` | ✅ | ✅ |

## ย้ายข้อมูลจริงแล้ว ✅ (28 ก.ค. 2026)

**ตลาดวันพฤหัสบดี — ย้ายครบทุกตาราง**

| ตาราง | ในชีต | ใน Supabase | หมายเหตุ |
|---|---:|---:|---|
| vendors | 115 | **115** | ครบ |
| payments | 234 | **234** | ครบ |
| activity_log | 202 | **202** | ครบ |
| floating_queue | 7 | **7** | ครบ |
| leave_log | 13 | **9** | ยุบซ้ำตามกฎ "1 ล็อค 1 วัน 1 รายการ" (H12 ซ้ำ 4 · M9 ซ้ำ 2) |
| daily_bookings | 7 | **7** (ยังไม่ยกเลิก 1) | A4 ซ้ำ 5 แถว → เก็บล่าสุด 1 ที่เหลือ mark cancelled **ไม่ลบทิ้ง** |
| settings · market_rules | 1 · 1 | **1 · 1** | ครบ |

> ข้อมูลซ้ำที่พบตรงกับที่คู่มือหัวข้อ 10 จ.3 บันทึกไว้เป๊ะ (H12, M9, A4) —
> ตอนนี้ฐานข้อมูล **บังคับไม่ให้ซ้ำได้อีก** ด้วย unique constraint

**ตลาดวันอังคาร:** ไม่มีข้อมูลให้ย้าย (ผู้ดูแลตลาดยืนยัน) — เริ่มใช้จากฐานว่าง

### ตรวจคุณภาพหลังย้าย

| ตรวจ | ผล |
|---|---|
| ช่วงวันที่ payments / leave_log / activity_log | ✅ 2026-07-06 → 2026-07-28 — **ไม่มีปี พ.ศ. หลุดมาแม้แถวเดียว** |
| เบอร์โทรขึ้นต้นด้วย 0 | ✅ 114/115 (อีก 1 รายคือล็อค K12 ที่ผู้ใช้พิมพ์มั่ว `หกอกหอห` มาแต่ต้น — คงไว้ตามเดิม ไม่แต่งข้อมูลเอง) |
| ผู้ค้าประจำที่ยังเช่าอยู่ | ✅ 115 ตรงกับชีต |
| ยอดรับชำระรวม | ฿5,685.00 (ค่าเช่า 232 รายการ ฿5,485 · ค่าไฟ 2 รายการ ฿200) |
| ตัวแปลง th_date / th_phone / th_time | ✅ **16/16 เคส** รวมทุกรูปแบบที่เคยทำระบบพัง (#28/#29/#32) |
| ความปลอดภัยหลังมีข้อมูลจริง | ✅ **10/10** — อ่าน/ลบ vendors·payments·activity_log·app_users ตรง ๆ ถูกปฏิเสธ 401 ทั้งหมด |
| ช่องทางนำเข้าชั่วคราว | ✅ ลบทิ้งแล้ว (RPC + ตารางพัก) — ยืนยันด้วย 404 |

> ตลอดการย้าย **ไม่ใช้ `service_role` key เลยแม้แต่ครั้งเดียว** และข้อมูลผู้ค้าจริง
> ไม่ไหลผ่านตัวกลางใด ๆ — เบราว์เซอร์ดึง CSV แล้วส่งเข้า Supabase ตรง ๆ

## ต่อสายหน้าเว็บเข้ากับ Supabase แล้ว ✅ (29 ก.ค. 2026)

| # | งาน | สถานะ |
|---|---|---|
| 1 | `tools/inject_api_client.py` (marker `PM_API_CLIENT`) | ✅ ฝังครบ 20 ไฟล์ · สามโหมด `sheets`/`parallel`/`supabase` · ค่าเริ่มต้นเป็น `supabase` |
| 2 | RPC ครบทุก action | ✅ ฟังก์ชัน `api()` ตัวเดียว 33 action — ทดสอบอ่าน 18 เคส เขียน 23 เคส ผ่านทั้งหมด |
| 3 | ทดสอบ end-to-end หน้าเว็บจริง | ✅ `tools/test_e2e_supabase.js` — ผ่านทุกข้อทั้ง TUE และ THU |
| 4 | ทดสอบว่าโหมดเดิมไม่เปลี่ยนพฤติกรรม | ✅ `tools/test_sheets_guard.js` — ยืนยันทางถอยกลับ Sheets ใช้ได้จริง |
| 5 | ไฟล์ cron 2 ตัว | ✅ เขียนแล้วที่ `.github/workflows/` (ยังไม่ได้ push ขึ้น GitHub) |

## ด่านความปลอดภัยก่อนเปิดใช้จริง — ต้อง apply `0008` ก่อน ✅ (29 ก.ค. 2026)

พบว่า `api()` เดิมเป็น `security definer` และเปิด execute ให้ `anon` แต่ยังไม่มีการตรวจ
session/role ภายในฟังก์ชัน จึงมีโอกาสให้ผู้ที่มี publishable key เรียกข้อมูลธุรกิจหรือ action
เขียนข้อมูลได้โดยไม่ต้องล็อกอิน แม้ตารางจะเปิด RLS ครบแล้วก็ตาม

`0008_api_sessions_and_authorization.sql` แก้ไขดังนี้:

- เปลี่ยน dispatcher เดิมเป็น `api_legacy` และถอนสิทธิ์เรียกตรง
- `verifyUser` ออก token แบบสุ่ม 256-bit อายุ 12 ชั่วโมง; ฐานข้อมูลเก็บเพียง SHA-256 hash
- `api()` ใหม่ยอมให้ไม่ล็อกอินเฉพาะ `getPublicStatus`; action อื่นต้องแนบ session token
- viewer อ่านข้อมูลและเปลี่ยนรหัสของตัวเองได้ แต่ action แก้ข้อมูลต้องเป็น admin

**ผลการ apply จริง (29 ก.ค. 2026):** ใช้ migration นี้กับทั้ง TUE
(`jrsuxrzyxiqqvzjftpms`) และ THU (`atvxajfwazrswdlhnosh`) แล้ว; `api_sessions`
เปิด RLS และ anon ไม่มีสิทธิ์เรียก `api_legacy` โดยตรง

**ลำดับ deploy ที่ห้ามสลับ:** apply `0008` ทั้งสองโปรเจกต์ → deploy `dist/` รุ่นที่มี token
→ ทดสอบล็อกอินและหน้าสาธารณะ → จึงเปิด `parallel` หรือ `supabase` ได้

### บั๊กที่เจอจากการทดสอบ e2e และแก้แล้ว (ไม่มีทางเจอถ้าทดสอบแค่ระดับ SQL)

| อาการ | ต้นเหตุ | แก้ที่ |
|---|---|---|
| ผังตลาดขึ้น "ว่าง" ทั้งตลาดในโหมด supabase | `pmFetchVendors()` ยิง `fetch` ไป Apps Script เอง ไม่ผ่าน `apiGet` | `tools/patch_market_maps.py` |
| ล็อกอินด้วยบัญชีจริงไม่ผ่าน แล้วตกไปใช้บัญชีสาธิตในไฟล์ | `doLogin()` อ่าน `localStorage['pm-*-api-url']` ตรง ๆ ไม่ผ่าน `getApiUrl()` | `tools/inject_api_client.py` (normalize + ปิดทางบัญชีสาธิต) |
| ปุ่ม/ฟีเจอร์ถูกปิดเงียบ ๆ ทั้งระบบ | `if(!getApiUrl())` 89 จุดใน 20 ไฟล์ ใช้ URL ของ Apps Script เป็นตัวตัดสิน | ครอบ `getApiUrl()` ในชิมชุดเดียว |
| แก้สคริปต์ patch แล้วรันใหม่ไม่มีอะไรเปลี่ยน | `add_vendor_sync()` เป็นแบบ "เจอ marker แล้วข้าม" | เปลี่ยนเป็น "เขียนทับระหว่าง marker เสมอ" |
| `payments.method` 2 แถวเก็บ `<svg …>` แล้วรายงานเอาไปต่อ innerHTML | ข้อมูลสกปรกตกค้างจากยุค Sheets = stored XSS | migration `0006` (trigger ล้างแท็ก + ล้างของเก่า) |
| หน้าสถานะสาธารณะบอกว่าล็อคไหน `unpaid` | `getPublicStatus` ส่ง status ดิบออกไป | migration `0007` (ยุบ `unpaid` → `active`) |

## รอบสถานะผังตลาดและสิทธิ์หน้าสาธารณะ ✅ (29 ก.ค. 2026)

apply `0009_public_status_and_market_state.sql` สำเร็จแล้วทั้ง TUE (`jrsuxrzyxiqqvzjftpms`) และ THU (`atvxajfwazrswdlhnosh`). ตรวจ `api('getPublicStatus')` แล้วพบทั้งคีย์ `leaveStatus` และ `unpaidLocks` ในทั้งสองโปรเจกต์

- `unpaidLocks` ส่งเป็นรายการหมายเลขล็อคเรียงลำดับเท่านั้น ตามการอนุมัติของผู้ดูแล; ไม่ส่งชื่อ เบอร์โทร หรือยอดค้างชำระ
- `leaveStatus` ส่งสถานะลา/ขาดตามล็อคให้หน้าสาธารณะได้ โดยไม่เปิดข้อมูลผู้ค้า
- ฝั่งผังตลาดแก้ให้โหลดสถานะล่าสุดแบบแทนที่ทั้งชุด, ยกเลิกผ่านฐานข้อมูลก่อนปรับหน้าจอ, และกำหนดวันหมดอายุ 3 วันหลังวันตลาด
- เพิ่มรายงาน 09 ผู้ค้าปัจจุบัน (โซน/ล็อค/ชื่อ/โทรศัพท์/สินค้า/ค่าเช่า/ค่าไฟ/รวม) ในทั้งสองแอพ
- client v3.1 รีเซ็ตค่าโหมดเก่าที่ค้างเป็น `sheets` ไป `supabase` อัตโนมัติครั้งเดียวต่อเครื่อง เพื่อให้ผู้ใช้ที่เปิดลิงก์เดิมไม่ถูกค้างอยู่กับ Apps Script ที่เลิกใช้แล้ว
- migration `0010_fix_api_session_ambiguity.sql` ถูก apply ทั้งสองโปรเจกต์: แก้ตัวแปร `expires_at`/`token_hash` ชื่อชนกับคอลัมน์ ซึ่งเดิมทำให้ทุกคำสั่งหลังล็อกอินตอบ HTTP 400
- แก้บั๊กผังใน iframe ที่อ่าน session token ไม่ได้หรือเผลอลบ token ของหน้าแม่ระหว่าง migration: ตัวเชื่อมต่อใช้ token ของหน้าแม่เป็น fallback และไม่ล้าง session จาก iframe อีก จึงทำให้ยอด 115 รายบนหน้าหลักกับข้อมูลในผังใช้ session เดียวกันจริง
- เพิ่มการส่ง session แบบ `postMessage` จำกัด origin จากหน้าหลักไป iframe ผัง: จำเป็นสำหรับ WebView/Safari ที่แยก `sessionStorage` ของ iframe แม้โดเมนเดียวกัน; token ไม่ถูกใส่ใน URL และไม่ส่งออกนอก `github.io` ของตลาด
- เมื่อ iframe รับ session แล้ว จะสั่งโหลดผู้ค้าซ้ำทันทีเพื่อไม่ให้ retry ที่เริ่มก่อน token มาถึงจบด้วย 401
- **ตรวจเว็บจริง 29 ก.ค. 2569:** ปิดบั๊ก iframe-session ด้วยการให้ iframe ขอ session จากหน้าแม่เองและใช้ session bridge เป็น fallback; THU แสดงผู้ค้าจริง 115 รายในผัง และ TUE โหลดผัง 216 ล็อคว่างโดยไม่มี error ตามฐานข้อมูลว่าง.

## ขั้นต่อไป (ยังไม่ได้ทำ)

| # | งาน | หมายเหตุ |
|---|---|---|
| 1 | deploy v2.5 ขึ้น GitHub Pages ทั้งสองตลาด | ยังเป็นโหมด `sheets` → ผู้ใช้ไม่รู้สึกถึงความเปลี่ยนแปลงใด ๆ |
| 2 | push cron 2 ตัวขึ้น GitHub + ใส่ secret 6 ตัว | `SUPABASE_URL_*`, `SUPABASE_ANON_*`, `SUPABASE_DB_URL_*` · ไฟล์ backup ควรอยู่ repo แบบ private |
| 3 | โหมดคู่ขนาน 2 วันตลาด | `PM_API.setMode('parallel')` → เทียบตัวเลขรายงานทุกฉบับด้วย `PM_API.compare('getPayments',{...})` |
| 4 | ตัดมาใช้ Supabase จริง | `PM_API.setMode('supabase')` · เก็บ Apps Script ไว้เป็นทางถอย 1 เดือน |
| 5 | เปลี่ยนชื่อโปรเจกต์ `TonyThejoss's Project` → `paemai-market-tue` | ทำในหน้าเว็บ Supabase (Settings → General) — MCP ไม่มีคำสั่งนี้ |

**ลำดับที่ห้ามสลับ:** ข้อ 2 (cron กันหลับ) ต้องทำงานอยู่ **ก่อน** ข้อ 4 (ตัดมาใช้จริง)

### วิธีรันชุดทดสอบ e2e ซ้ำ (ต้องมี PostgreSQL ในเครื่อง)

```bash
# 1) สร้างฐานทดสอบแล้วลง migration ชุดเดียวกับของจริง (0001,0002,0003,0005,0006,0007,0008)
# 2) ใส่ข้อมูลจำลอง + รันเทสต์
node tools/test_e2e_supabase.js thu     # และ tue
node tools/test_sheets_guard.js
```

ชุดทดสอบนี้ยิงหน้าเว็บใน `dist/` จริงในเบราว์เซอร์จริง โดยดักคำขอ `/rest/v1/rpc/api`
ไปเข้า Postgres ในเครื่องที่ลงฟังก์ชัน `api()` **ตัวเดียวกันทุกตัวอักษร** กับของจริง
(ตรวจด้วย md5 ของ `pg_get_functiondef` หลังตัดคอมเมนต์/ช่องว่าง)
สิ่งที่ชุดนี้ยังไม่ครอบคลุมและต้องดูตอน deploy จริง: TLS · CORS · สิทธิ์ของ role `anon`

## ไฟล์ migration

เก็บไว้ที่ `supabase/migrations/` — ทั้งสองโปรเจกต์ใช้ SQL ชุดเดียวกันเป๊ะ
(หลักการเดียวกับ `core/` ของระบบเดิม: แหล่งความจริงแหล่งเดียว ไม่ให้สองตลาด drift กัน)

- `0001` `paemai_core_schema_v3` — 11 ตาราง + index + constraint
- `0002` `rls_and_auth` — RLS, revoke grants, public view, verify_user, change_password
- `0003` `th_normalizers` — th_date / th_phone / th_time (ทดสอบ 16/16)
- `0004` `import_thursday_data` — ย้ายข้อมูลจริงฝั่งวันพฤหัสฯ พร้อม dedupe
- `0005` `api_dispatcher` — ฟังก์ชัน `api()` 33 action + th_fmt / date_matches / _auto_log
- `0006` `sanitize_text` — trigger ตัดแท็ก HTML ก่อนลงตาราง + ล้างข้อมูลเก่าที่ปนมา
- `0007` `public_status_mask_unpaid` — เวอร์ชันก่อนหน้าที่ซ่อนสถานะค้างชำระทั้งหมด
- `0008` `api_sessions_and_authorization` — session token อายุ 12 ชั่วโมง + บังคับสิทธิ์ใน API
- `0009` `public_status_and_market_state` — คืนลา/ขาดและหมายเลขล็อคค้างชำระเฉพาะที่อนุมัติให้หน้าสาธารณะ
- `0010` `fix_api_session_ambiguity` — แก้ชื่อตัวแปร session ที่ชนคอลัมน์ จนคำสั่งหลังล็อกอินตอบ HTTP 400
- `0011` `persist_unpaid_and_block_termination` — คงค้างชำระจนปิดยอด และห้ามยกเลิกการเช่าขณะยังมียอดค้าง

## กติกาค้างชำระถาวร ✅ (29 ก.ค. 2026)

apply `0011_persist_unpaid_and_block_termination.sql` สำเร็จแล้วทั้ง TUE
(`jrsuxrzyxiqqvzjftpms`) และ THU (`atvxajfwazrswdlhnosh`) โดยไม่แก้ข้อมูลผู้ค้าจริง

- รอบรีเซ็ต 3 วันล้างเฉพาะลา/ขาด/ล็อคจร ไม่แก้ `vendors.status='unpaid'`
- trigger `vendors_guard_unpaid_state` ปฏิเสธการเปลี่ยนเป็น `terminated` หากสถานะเดิมยังค้างชำระหรือยอดค้างมากกว่าศูนย์
- การเปลี่ยน `unpaid` กลับ `active` ทำได้เมื่อ `unpaid_penalty` และ `unpaid_other` เป็นศูนย์แล้ว
- หน้าเว็บทั้งสองตลาดใช้กติกาเดียวกันและเผยแพร่แล้ว: TUE `66398ac`, THU `8b64058`

> ⚠️ `0001` และ `0003` เคยเป็นไฟล์เปล่า (มีแต่คอมเมนต์) เพราะตอนย้ายส่ง SQL เข้าไปตรง ๆ
> ผ่านเครื่องมือ แล้วลืมเขียนกลับลงไฟล์ — ดึงจาก `supabase_migrations.schema_migrations`
> กลับมาเติมครบแล้ว **ถ้าวันหนึ่งต้องสร้างโปรเจกต์ใหม่ ไฟล์ชุดนี้คือทางรอดเดียว**
> เทียบ md5 ของทุกฟังก์ชันกับของที่รันจริงบนคลาวด์แล้ว — ตรงกันทั้ง 11 ตัว
